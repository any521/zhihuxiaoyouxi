# 参与贡献

感谢愿意帮忙！这是黑客松作品，代码风格偏「能跑 + 有据可查」，请配合几点约定。

## 动手之前

1. **先读 交接文档.md** —— 里面记着这个项目的全部踩坑史（八十多条），
   很多「看起来该这样写」的写法其实都翻过车，文档里写清了为什么。
2. 跑一遍体检（会启无头浏览器，比较慢）：

       cd liukanshan-career
       node ../工具/体检全部.mjs          # 全部
       node ../工具/体检全部.mjs --快      # 快跑

## 硬性约定

- **像素完美**：只允许整数倍缩放 + 最近邻，禁止平滑插值
- **文字一律 DOM 渲染**，不要画进 canvas
- **离线可通关**：外部服务只能做增强，断网也要能把故事走完
- **不编造来源**：知乎相关文案必须来自真实检索结果
- **AI 只生成台词**：不得让模型影响评分 / 剧情走向 / 数据

## 提交前自查

       cd liukanshan-career
       npx tsc --noEmit       # 类型（注意：查不出 JSX 解析错误）
       npm run build          # 构建（这一步才会暴露真正的解析错误）

> ⚠️ **构建过了才算过**：tsc 单独通过不代表能跑（历史上多次出现 tsc clean 但整站白屏）。

## 提交信息

建议格式：类型(范围): 说明

       fix(关卡): 工单超时后不再残留红框
       feat(地图): 支持把稿子扔进垃圾桶丢弃
       docs(交接): 补坑表 71~83

## 部署

见 README 的「部署」一节。三条硬规矩：构建失败不打包、上传要重试、部署后核对哈希。
