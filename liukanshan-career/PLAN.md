# 开场与 AVG 实现计划

> 目标：把游戏的**开头**做出来 —— 开场动画 + 剧情以「插图 + 卡片对话」的 AVG 形式展开。
> 素材已 45/45 齐备；音频暂缺，先留接口。

## 设计决定

### 界面形式：微信式（2026-09-13 改）

剧本本来就是私聊 + 群聊，所以 AVG **做成真正的微信界面**：
功能栏 58px + 会话列表 238px + 聊天窗口 flex（手机端列表/聊天二选一）。

两个直接好处：
- 玩家一眼就懂怎么操作（不用学新交互）
- **选项落在输入框的位置**，点选项 = 发消息，语义完全对得上

### 为什么 AVG 用 DOM 而不是 Phaser

沿用既有原则「**像素感来自画框，清晰度来自文字**」：
- **插图/头像/贴纸**：PNG 精灵 + `image-rendering: pixelated`
- **所有文字**：DOM 渲染。Phaser 在 512×288 上画中文必然糊

所以 AVG 是一层**盖在画布上的 DOM 层**（和现有 `StationLabels`、`StartOverlay` 同一套路）。

### 屏幕状态

现有 `phase` 只管关卡。新增一个正交的 `screen`（**2026-09 更新为四个**）：

```
screen: 'opening' | 'avg' | 'map' | 'level'
   opening  开场动画（黑屏 → 镜头 → 标题卡），可跳过
   avg      微信式对话，推进剧情
   map      俯视办公室地图（走动 / 开门 / 找人 / 坐椅子）
   level    工作关（把活做出来）—— 由剧本的「开小游戏」节拍切进来
```

### 素材映射

| 用途 | 素材 |
|---|---|
| 场景插图 | `背景/办公室工位` · `出租屋_横版` · `招聘会_横版` · `章节封面_横版`（512×288）|
| 对话头像 | `角色/*_表情*`（刘看山 6 · 学长 4）+ `角色动画/角色_*_四表情_32x32`（6 个 NPC）|
| 头像卡片框 | `界面v2/头像框_圆形` · `头像框_方形`（96×96）|
| 贴纸 | `界面v2/贴纸_*`（12 枚，48×64）|
| 对话框 | `界面v2/界面_气泡_对方`（九宫格，切片 6 / border-width 2）|
| 选项卡 | `界面v2/列表项_普通` · `列表项_选中`（256×46）|
| 转场遮罩 | `特效/沙漏_10帧_640x64`（640×64，10 帧）|
| 结局 | `结局/结局_转正|跳级升职|结束实习`（512×288）|

### 音频接口（先留空）

```ts
// src/story/audio.ts —— 现在只记录事件名，不出声
// 素材补齐后把 play() 接到真实音频即可，调用点不用改
type 音效名 = '消息弹出' | '按钮' | '选择' | '翻页' | '转场'
```

---

## 里程碑

### M1 · 开场动画 ✅ 独立可跑
- 文件：`src/ui/Opening.tsx`
- 内容：黑屏 → 章节封面淡入 + 标题 → 三格镜头（出租屋 → 街上 → 写字楼门口）→ 标题卡「第一章 · 投出去的第 47 份简历」→ 自动进 AVG
- 验收：能看到完整动画；点任意处/按空格可跳过；双端都不溢出
- 截图：`tools/快照.mjs`

### M2 · AVG 引擎骨架 ✅ 独立可跑
- 文件：`src/ui/Avg.tsx` + `src/story/types.ts`
- 内容：插图底 + 左侧头像卡 + 底部卡片对话框 + 逐条播放（0.8~4 秒间隔）+ 点击加速/推进
- 验收：喂一段假脚本能逐条播完；文字不糊；双端排版正确

### M3 · 开场剧情（事件一前半）✅ 独立可跑
- 文件：`src/story/opening.ts`
- 内容：按剧本「事件一」——林总私聊 2 条 → 群聊邀请卡 → 群聊 8 条（小鹿/周岚/阿麦/韩策）→ 群里安静
- 验收：完整播完，说话人头像正确，贴纸正确插入

### M4 · 选项与三项指标
- 内容：A/B/C 三选一（卡片式按钮）→ 选定播放结果 → 三项指标（老板信任/团队协作/职业成长）变动
- 验收：三选一都能走通；指标面板数值正确；不可撤销

### M5 · 收束与进入关卡
- 内容：任务评价 → 两张知乎卡占位 → 无限追问占位 → 「继续实习生活……」→ 切到 `screen='level'`
- 验收：能一路走到办公室关卡，关卡能正常玩

### M6 · 双端与打磨
- 内容：移动端排版（竖版 288×512）、跳过/回看、音频接入点
- 验收：双端截图

---

### M7 · 跑团 + 开小游戏（事件四）✅ 独立可跑

> 用户要求：「增加开启小游戏的剧情」「跑团剧情增加知乎 AI 不偏离剧情的丰富有趣对话」
> 「小游戏改为工作上的事不是投简历了」「小游戏画面和操作方式都要改为和现在主题一样」。

- 文件：
  - 剧本 `src/story/events3.ts`（事件四，含「跑团」「开小游戏」两个新节拍）
  - 跑团 `src/story/跑团.ts`（引擎）/ `跑团脚本.ts`（15 回合 × 4 档）/
    `llm.ts`（要镜头 + 台词）/ `src/screens/跑团面板.tsx`（UI）
  - 工作关 `src/game/assets.ts` + `greybox/level.ts` + `scenes/OfficeScene.ts` + `src/screens/关卡.tsx`
- 验收：
  - `node tools/跑团自检.mjs`（69 条，不需要浏览器）
  - `node tools/事件四验收.mjs`（20 条断言 + 7 张实拍图，需要 dev server）
- 三条不能破的线：
  1. **判定顺序不能反**：引擎先算完 → 兜底立刻上屏 → 才去要 AI 的话（`贴AI话` 只改文字）；
  2. **谁能说话由作者写死**（`回合.在场`），AI 只能让在场的人开口；
  3. **成败不改主线**：通关 / 翻车 / 超时三个收束都往下走。

---

### M8 · 工作关：全屏 / 主题 UI / 工序图标 / 帮手 ✅ 独立可跑

> 用户要求：「小游戏改为**全屏**，上下 1:4，上面 1/5 显示需要的任务，
> 不要这样的 ui，做成**主题样式**」「每个文件的状态在每个阶段都要能肉眼看出来，
> 像胡闹厨房一样每添加一样就显示特定图标」「ai 帮手是**相关的同事**……自己干活但很慢……
> 有自己的工作逻辑要不然会卡住」「都做成和主角一样**有动画有物理实体碰撞**」。

- 文件：`src/screens/关卡.tsx` + `关卡.css`（全屏 + 上 1/5 任务条 + 主题 UI）
  / `game/greybox/level.ts`（`造关卡`：按格数现造）/ `game/scenes/OfficeScene.ts`（工序图标 + 帮手 AI）
  / `ui/Toasts.tsx`（主题飘字）；旧 `ui/App.tsx`、`Hud.tsx`、`usePixelFit.ts` 已删
- 验收：`node tools/事件四验收.mjs`（40+ 条：含手机竖版、任务条不裁切/不跑出屏幕、帮手 20 秒真跑一遍）
- 补充（用户第二轮）：**队友只留一个真干活的**（删掉站桩同事）；
  **画布上不许有任何浮层**（文字全排进顶部任务条，机器/名牌/素材必须露得出来）
- 三条不能破的线：
  1. **像素完美**：全屏靠"内部格数 × 整数倍"，绝不拉伸画布；
  2. **关卡按格数现造**：全屏 ⇒ 格数随窗口变，别再写死 16×9；
  3. **帮手永远抢不过玩家**：走路慢、工序慢（2.4s vs 0.9s）、干完要歇。

---

## 约定（不许破坏）

- 🔒 **不改动**现有 Phaser 关卡的任何玩法逻辑；AVG 只是多一个 screen
- 🔒 **文字一律 DOM**，不许塞进 Phaser 画布
- 🔒 内部分辨率保持 **横 512×288 / 竖 288×512**，整数倍缩放
- 🔒 字号只用 **12 / 24 / 36** 三档（原生 12 的整数倍）
- 每完成一个里程碑：`npx tsc --noEmit` → `node tools/快照.mjs` → 看截图
