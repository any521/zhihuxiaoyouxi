# 《刘看山打工日记》交接文档

> 写于 2026-09-13 · 对应提交 `5346418` · **最后更新 2026-09-14（提交 `64f0ec7` 之后）**
> 本文档的目标：**换一个人（或换一个 AI 会话）拿着它就能接着干**。

---

## ⓪ 新会话从这里开始（**先读这一节，5 分钟进入状态**）

> ### ⚠️⚠️ 先看这一段：工作区里**可能同时有别的会话在改**
>
> 本文档描述的稳定状态对应提交 **`bcf0dd8`**。
> 2026-09-14 稍后发现工作区里有**另一路会话正在做「事件四 + 跑团界面」**（未提交）：
>
> ```
> 新增：src/screens/关卡.tsx · src/screens/跑团面板.tsx
>       src/story/events3.ts · src/story/跑团脚本.ts
> 改写：state/story.ts · story/跑团.ts · story/llm.ts · story/types.ts
>       screens 的 UI 全家桶（ui/*.tsx）· game/scenes/OfficeScene.ts（旧 Godot 参考件）
> ```
>
> **给新会话的规矩：**
> 1. **动手前先 `git status`** —— 有未提交的改动就问清楚是不是别人的活，别 `git add -A` 一把梭。
> 2. **上面这批文件不属于本文档（⓪~九节）描述的范围**，别按本文档去"修"它们。
> 3. 那批改动当时**类型检查是干净的**（`npx tsc --noEmit` 0 错），
>    dev server 日志里出现的语法错误是**写到一半的中间态**，不是坏文件。
> 4. 本文档第七节（踩坑）第九节（建议）**仍然全部适用** —— 那些是通用的。

### 现在能玩到哪

事件一 / 二 / 三 / **四** 全部可玩：
开场 → 工位 → 老板办公室 → 回工位 → 茶水间群聊 → 前台下班
→ **事件四：群里派活 → 会议室跑团（15 轮，知乎 AI 写镜头与台词）→ 工作关（把稿子做出来）→ 交稿收束**。
**事件五~十还没写**（§八 有优先级；候选剧本已经写在 `设计/剧本-新剧情参考.md`）。

### 本会话刚做完的五件事（都在最新提交里，别再改回去）

1. **事件四 · 开小游戏的剧情**（`src/story/events3.ts`）——
   林总丢一句"你自己扛一版"，玩家进会议室把客户那句模糊需求问清楚，再回工位把稿子做出来。
2. **跑团会写台词了**（`src/story/跑团.ts`）——
   从"一句描写"升级成**一个镜头 + 一句台词**。**谁能说话由作者写死**（`回合.在场`），
   AI 只能让在场的人开口；镜头和台词各过各的闸，不过就退作者的兜底台词（15 回合共 60 档兜底台词）。
3. **跑团面板**（`src/screens/跑团面板.tsx`）——
   掷骰 → 档位 → **作者兜底立刻上屏** → AI 的话回来了再换掉那一格文字
   （`贴AI话` 只改文字，分数一个不动）。
4. **小游戏挂上了，而且改成"工作上的事"**（`屏幕 = 'level'`）——
   工序从 排版/打印/盖章 改成 **查资料 / 写稿 / 校对**，交付从"投递箱"改成"**交稿箱**"，
   帮手从"学长"改成**阿麦**。机制一个字没改（还是《胡闹厨房》那套）。
5. **小游戏的画面与操作对齐地图**（`src/game/assets.ts` + `greybox/level.ts` + `scenes/OfficeScene.ts`）——
   瓦片、主角精灵表、同事立绘、家具**全部改用 `public/assets/map/` 那一套**，
   主角用地图那张 `chr_lks_walk.png` 的四方向行走（4 行 × 4 帧），操作也和地图一致。

### 最容易踩的三件事（新会话务必先看）

| 坑 | 一句话结论 | 详见 |
|---|---|---|
| **`座位表` 和 `道具表` 必须对得上** | 座位表说这儿能坐，那儿就得真有件家具。曾登记两张**不存在的单人沙发** → 人坐到空地上（用户报"没座位却能坐"）| §3.11 + `node 工具/查座位.mjs` |
| **`走()` 每帧重设主角深度** | 任何"特殊深度"（正面的 +40）都必须能从状态推出来，否则下一帧被覆盖 | §3.11 |
| **Space 是浏览器激活键** | 焦点在按钮上时，空格会去点那个按钮；`preventDefault()` 必须最先调 | §4.0 |
| **dev server 会给你旧代码** | 长时间开着的 vite 可能**不再重新 transform 改过的模块**（`Avg.tsx` 加了新组件却死活不显示、DOM 里查不到）。**别怀疑自己写错**：先杀掉 5273 上的进程重启一次再看。⚠️ 用 PowerShell 匹配中文来校验"服务的是不是新代码"会**误判**（编码问题），要么看 ASCII 标记，要么直接在浏览器里查 DOM | §4.1 |
| **CSS 别写进别人的 `@media` 里** | 往 `screens.css` 末尾追加样式时，文件最后一段是 `@media (max-width: 720px) { ... }` 的**内部**；追加在它后面 = 写进了媒体查询 → 大屏完全不生效（跑团面板第一次就是**一堆无样式的块**） | §4.1 |
| **同一个判断写在两处 = 迟早打架** | 「按空格该坐还是该开会」写在 `MapScreen`（键盘）和 `story.ts`（`地图交互`）两处，键盘那处漏了 `!坐着` → 空格在"坐↔站"里死循环，**「我的工位」的主线被彻底锁死**（用户报"触发不了剧情"）。**看到"表 A 说…表 B 也说…"就该加一致性检查** | §3.11 |
| **dev server 发旧模块会让测试莫名失败** | 长时间开着的 vite 有时不再 transform 改过的模块 → "新剧情 + 旧界面"混着跑（跑团面板不渲染、进入工作台的按钮不出来），代码其实是对的。现在 `事件四验收` **启动先 fetch 源码模块验 `LKS_REV` 标记**，不对就直接提示"重启 dev server"。改这块代码时**顺手改一下那个标记** | §4.1 |
| **"品红背景"不一定真的是 #ff00ff** | 出图工具这次把背景画成了 **[227,5,143] 的洋红**，蓝只有 143 —— `是品红`（原判据 `r>170 && b>170 && g<120`）判不出来 → **整张图被当成一整块内容** → `切件.mjs` 只切出 1 个块（3 枚图标变 1 枚）。已补上"洋红族"那一段（32 色板里没有任何颜色落在该区间，所以放宽是安全的）。**症状：工具报"连通块 1 个 / 最大件 = 整张图尺寸"** | §4.1 |
| **别用 `\| Select-Object -First N` 接长命令** | PowerShell 凑够 N 行就**关掉管道**，上游 node 进程会**被提前掐断**（我据此误判"会议室插图没拷进去"，其实拷好了）。要截断就用 `-Last` 或先写文件再读 | §4.1 |
| **存档有防抖，"存档 vs 当前"直接比就是在测竞态** | 自动存档防抖 700ms，刚推完一拍时存档慢半拍。断言要**等它追上来**再比（实测第一版就是这么误报的）| §4.1 第八轮 |
| **整站共用一份 localStorage = 多标签页互相覆盖** | 测试里另一个还在跑的页面把刚存的进度覆盖了。当前不做多标签协调，但排查"存档不对"时先想想是不是开了两个页面 | §4.1 第八轮 |
| **"等待玩家操作"的状态不要存** | 待选择/待去房间这些标记本来就写在第 N 拍上，**重播那一拍会自己重新设上**；存下来会读出"半等待"的怪状态 | §4.1 第八轮 |
| **用写死的字符串数引用，会漏掉三元/变量形态** | `rg "播放\('消息收到'"` 数出来 0 次，实际代码是 `播放(条.我方 ? '消息发出' : '消息收到')` —— 差点删掉 4 个正常的音效。**数引用要搜名字，别搜调用形态** | §4.1 第七轮 |
| **像素画的缩放必须是整数** | 小地图的"一格几像素"只能取整数（3/4/6），非整数会让格线糊掉；**同事头像的尺寸下限也要跟着格像素走**（写死 8px 在 3px/格上会占掉 3 格）| §4.1 第七轮 |
| **地图的 y 是"贴底"的，随手 `floor(y/32)` 会多算一行** | `格到像素().y = 行*32+32`（脚底），反算必须是 `floor((y-1)/32)`。我在点地寻路的测试里写错了一次，`找路()` 直接返回 null，看起来像"寻路坏了"。**场景里有现成的 `像素到格()`，别自己写** | §4.1 第六轮 |
| **一套玩法、两种输入，必须共用同一个入口** | 摇杆不自己实现移动，只写 `虚拟输入`；场景每帧读，和键盘共用 `tickPlayer`/`干活`/`interact`。地图的「交互」键和键盘空格也共用一个 `按下交互`。**两套各写一份 = 迟早一边有 bug 一边没有** | §4.1 第六轮 |
| **布局规则一旦"依赖某个具体排列"，改排列就会连带失效** | "工位区隔断"原来是**按相邻工位的中点**算的；机器一散开（用户要求别摆一排），这个规则就什么都不生成了。改排列时要顺手检查"还有哪些结构是在假设这个排列"。现在隔断改成**固定比例位置 + 避开资料库列** | §4.1 第五轮 |
| **能走开的活动，状态就不能只画在原地** | 打印机改成"放进去等出来"之后，倒计时必须同时挂到**顶部任务条**（`store.打印`）。原地提示对"已经走开的人"没有任何用 | §4.1 第五轮 |
| **`setDisplayOrigin` 不是"只挪画面"，它改的是原点语义** | `sortDepth()` 里无条件 `setDisplayOrigin(16, 48 + 浮动)`，把助手的原点从 `(0.5,0.5)` 改成了 `(0.5,1)` → **y 从"中心"变成"脚底"** → 拿东西的偏移整体下移 24px（"隔空取物"）+ 人凭空高 24px。兜底路径才动它 | §4.1 第四轮 |
| **同一行相邻帧差得很小 = 素材"走一下卡一下"** | 走表常是 **A A B A**（相邻帧差 11 / 33 / 32 / 12）。代码侧能靠"去近似重复帧"救（`取不同帧`），但要彻底好就得重出图。**量帧差的脚本值得留着** | §4.1 第四轮 |
| **Phaser 的键盘事件依赖画布焦点** | 用户报"按 Esc 没出设置"。ESC 现在统一挂 React 的 `window` 监听；**同一功能只能有一个入口**，否则两边都响应 = 开了又立刻关 | §4.1 第四轮 |
| **移动端"隐藏一个入口"之前，先问"那它还能不能回去"** | 手机上 `.wc-rail` 被 `display:none`，同时 `回地图` 只绑 Tab 键 → **进了聊天就出不来**（手机没有 Tab）。隐藏任何导航元素时，必须同时确认"另一条路还在" | §4.1 第十轮 |
| **"给某个控件让位"容易把别的东西挤进死角** | 上一轮为了给摇杆让位，把道具卡设成 `bottom: 132px` —— 正好压住「交互」键和提示条。**挪一个元素时要把同一片区域的东西一起重排**，别只挪一个 | §4.1 第十轮 |
| **"没设深度"= 深度 0，会被一切盖住** | 站着的同事漏了 `setDepth` → 深度 0 → 头被会议长桌吃掉（用户报"主管没有头"）。**坐下的同事没事（走 `放到座位上()`），只有站位不在座位表上的那几个人中招**。以后凡是 `add.sprite` 都要立刻想"它的深度谁给" | §3.6 |
| **交互点要写在"人真正会站的位置"** | 用户报"去林总办公室应该定位到进门口往前一点、不是在中间"。`交互点表` 里的坐标既是**指引线终点**也是**触发位置**，写在房间中部会把人往房间深处带。现在 `林总办公室` 放在 **(28,15)**：进门往里两格 | §3.11 |
| **重写一个屏幕组件，容易漏掉挂在它里面的东西** | 把关卡屏从 `App.tsx` 重写成 `关卡.tsx` 时，**工位名牌整个漏掉了**（它原来是 App 里的一个小组件）—— 截图上不容易发现（工位还在，只是没有名字）。现在钉了一条断言："5 个名牌都在、都有字、都在画布范围内" | §4.1 |
| **加墙之前先给 AI 加寻路** | 帮手原来只会朝目标**直线走**，地图一加玻璃他就**撞墙卡死**（实测 2 格玻璃就够）。正确顺序是**先有 BFS 寻路、再加结构**；看门狗只是最后的保险 | §4.1 |
| **HUD 不许浮在画布上** | "现在该做什么"和飘字浮在画布上、正好压住最上面那排工位（文件柜/资料库/我的工位/文印区）连名牌 —— 用户报"机器看不见、提示盖住字体和素材"。**文字一律排进任务条**，并加了断言：关卡根节点下除画布容器外，任何块与画布矩形**不许重叠** | §4.1 |
| **屏幕上"排不下"要专门量，不能只看截图** | 1/5 的任务条里塞三张工单卡：第一版纵向裁掉第三张，改成横向滚又把"手上这一版"推出屏幕。**"不裁切"量 `scrollHeight ≤ clientHeight`，"没跑出屏幕"量每个块的 `getBoundingClientRect`** | §4.1 |
| **测试跨场景串味会报假失败** | 同一张页面里连着验多个地图场景会串味：`MapScreen` 卸载时**不清** `window.__lksMap`（那是已销毁的旧场景）、坐着时"瞬移"被坐姿钉住、上一步的座位状态还留着 → 一堆假失败，产品其实是好的。**一个新页面 = 一个干净世界** | §3.11 |

### 干活前的固定动作

```powershell
cd E:\projects\知乎小游戏\liukanshan-career
npx vite --host 127.0.0.1 --port 5273 --strictPort   # 起服务（背景任务）
# 改完任何东西，收尾跑一条：
cd E:\projects\知乎小游戏; node 工具/体检全部.mjs --快
```

⚠️ **`体检全部` 最后会报 `exit 1`** —— 那是有的脚本"查出问题"也用非 0 退出，
**不代表失败**；看它打印的 `✅/✗` 和最后一行「全部通过」。

---

## 一、这是什么项目

一个**绑定知乎内容**的小游戏：2D 拟像素美术 + 卡牌式对话 AVG + 俯视办公室地图。
主角是**刘看山**（知乎的白色北极狐吉祥物），故事是"职场新人打工日记"，共 10 个事件。

三大块：

| 块 | 形态 | 技术 |
|---|---|---|
| **AVG** | 微信式聊天界面，逐条播放对话 | React + DOM |
| **地图** | 俯视像素办公室，能走、能开门、能找同事 | Phaser 4 |
| **知乎卡** | 剧情里嵌真实知乎回答（作者/标题/摘要/链接可跳转）| React + DOM |

**硬要求**（用户反复强调过的，别丢）：
- 美术**全部 AI 生成**，提示词由助手写（用户只负责跑图）
- **不能写负面提示词**（用户的出图工具没这个输入框，所有"不要 XX"都要写成"排错检查：如果…就是错的"）
- **不用纯黑背景**，要米白 + 绿色，符合知乎调性
- **字体要清晰不模糊** —— 所以所有文字走 DOM，不走 canvas
- PC 和手机**都要能玩**
- 知乎内容**不是广告**，是固定入口的卡片，**不做弹窗**

---

## 二、五分钟上手

```powershell
cd E:\projects\知乎小游戏

# 1) 起开发服务器（背景任务，端口 5273）
cd liukanshan-career
npx vite --host 127.0.0.1 --port 5273 --strictPort

# 2) 类型检查（改完代码必跑）
npx tsc --noEmit

# 3) 看页面
#    http://127.0.0.1:5273/
```

**目录地图**

```
E:\projects\知乎小游戏\
├─ liukanshan-career\          ← 游戏本体（唯一在跑的）
│  ├─ src\
│  │  ├─ game\map\             ← 地图：level.ts(数据) / OfficeMapScene.ts(Phaser场景) / 位置总线.ts
│  │  ├─ screens\              ← MapScreen / 小地图 / Panels / Avg
│  │  ├─ story\                ← 剧本 / 人物档案 / 知乎卡 / 音效 / 素材索引
│  │  └─ state\story.ts        ← 全局状态机（zustand）
│  ├─ public\assets\map\       ← 地图用的全部素材（54 个文件）
│  └─ public\assets\avg\       ← AVG 用的头像/表情（34 个 face_*.png）
├─ 工具\                        ← ★ 诊断与处理脚本（这个项目最值钱的资产，见第六节）
├─ 美术\
│  ├─ 素材库\原图\              ← 用户生成的原始素材（批次 00~19）
│  ├─ 素材库\成品\              ← 处理好的成品
│  └─ 提示词-*.md               ← 给用户的出图提示词
├─ 设计\剧本-新剧情参考.md        ← ★ 事件四全文 + 事件五~十的候选本子（挑本子看这个）
├─ 计划书-地图重做.md            ← 地图设计文档
└─ liukanshan-game\            ← 旧的 Godot 工程，**只当参考**，不维护
```

**Git**：`https://github.com/any521/zhihuxiaoyouxi.git`，分支 `main`

### ⚠️ 后台任务报 `exit 1` ≠ 服务器挂了

本会话遇到过两次，**两次都是假警报**：

| 现象 | 真相 |
|---|---|
| job 报 exit 1，日志末尾是历史 transform 错误 | vite **不会因为 transform 错误退出**，那只是它报过的错<br>（那两个错误当时已经修掉，`tsc --noEmit` 是干净的）|
| 重启后又报 exit 1，日志里是 `Port 5273 is already in use` | **旧进程还活着并在正常服务**，新进程只是抢不到端口 |

**排查顺序**（别急着重启）：

```powershell
# ① 端口上有没有活的进程
Get-NetTCPConnection -LocalPort 5273 -State Listen |
  ForEach-Object { Get-Process -Id $_.OwningProcess }
# ② 页面是不是真能用（比看 job 状态可靠得多）
Invoke-WebRequest http://127.0.0.1:5273/ -UseBasicParsing | Select-Object StatusCode
```

**只有"端口没人监听 + HTTP 打不开"才是真挂了。**

---

## 三、地图系统（本次会话重做的重点）

### 3.1 坐标系

- **瓦片坐标**（1 格 = 32px）是唯一的数据坐标，`level.ts` 里全用它
- 转像素：`像素x = 格x*32+16`（水平居中）、`像素y = 格y*32+32`（贴底）
- **贴底**是为了按"脚底 y"排序遮挡 —— 这是俯视游戏的标准做法

### 3.2 平面图 `45×30` 格 = 1440×960px

```
行 0-1   外墙
行 2-9   北区 ┌─会议室A─┬玻璃┬──开放办公区──┬玻璃┬─会议室B─┐
行 10    北区南墙（4 个门）
行 11-12 主走廊（横向贯通）
行 13    南区北墙（4 个门）
行 14-21 南区 ┌─茶水间─┬─文印区─┬竖廊┬─总监办公室─┬─储藏间─┐
行 22    南区南墙
行 23-27 前台接待区
行 28-29 外墙（**没有大门**：按要求删掉了，公司对外封闭）
竖廊 x21-22（**2 格宽**）：把前台和主走廊连起来，"前台→长廊→各房间"的动线才通
```

> ⚠️ **南区四间房全部 9 格宽，以第 22 格为镜面左右逐一镜像**（用户明确要求"左右对称"）：
> ```
> x0-1 墙 ‖ x2-10 茶水间 ‖ x11 墙 ‖ x12-19 文印区 ‖ x20 墙 ‖ x21-22 长廊 ‖ x23 墙 ‖ x24-32 总监办公室 ‖ x33 墙 ‖ x34-42 储藏间 ‖ x43-44 墙
> ```
> - 镜面在**第 22 格**（x ↔ 44 - x）：茶水间↔储藏间、文印区↔总监办公室，房间宽度逐一相等
> - 长廊正好 **2 格**（x21-22），**左右各一堵实墙**（x20 / x23）
> - ⚠️ **2 格走廊的中心是 704px，而地图几何中轴是 720px**，差 16px。
>   这是算术必然（4 间房 ×9 + 走廊 2 + 墙 3 = 41 = 内宽），"四间房等宽"和"走廊中心压 720"不可兼得。
>   用 3 格走廊能让中心压 720，但左右房间会变成 8/9 格**不等宽**。用户要的是左右对称 → 选 2 格。
> - ⚠️ **结构必须镜像，地板材质不必**：左房防滑砖（文印区）、右房深灰地毯（总监办公室）
>   是房间功能不同，不算不对称。
> - 📌 **"长廊左边补一堵墙"那次改动的连带影响**（用户要求 x20 砌墙，原建筑没有）：
>   ```
>   文印区 x12-20 → x11-19   （整间房连带道具一起左挪一格）
>   茶水间东墙 x11 → x10      （跟着挪，不然和文印区重叠）
>   文印区北墙的门 x17-18 → x16-17（门的中心要跟着房间中心）
>   交互点「打印机」(15,18) → (14,18)
>   ```
>   ⚠️ **挪房间时必须一起挪里面挡路的道具**，否则会"打印机压墙"。
>   查它：`node 工具/查压墙.mjs`（在仓库根跑，改完布局必跑，要 0 才算过）。
>   搬的时候注意：`投递箱(49px)`、`资料架(51px)` 这类**宽过 1.5 格**的道具，
>   贴到墙边会压进墙里 —— 它们比其他道具多让一格（摆在 x13 而不是 x12）。
> - 量它：`node tools/镜像体检.mjs 18`（按区段打印 + 镜像比对）、
>   `node tools/读运行时网格.mjs 13 22`（读**游戏里真实那份**网格）
> - 看它：`node tools/正门检查.mjs` → `tools/shots/工位/南区-带中轴.png`（带中轴红线）
> - ⚠️ **大门已删**：`门表` 里"公司正门"那行被去掉，外墙圈（行 28-29）完整闭合。
>   要恢复就加回 `[布局.正门行, 22, 瓦片.门横左]`，
>   并且**开门代码必须排在砌外墙之后**，否则门会被外墙整格盖掉（3.4 有记录）。

**动线必须保持通畅** —— 改布局时最容易犯的错就是把门堵上（真发生过：文化墙摆在了门口）。

### 3.3 瓦片编号（**必须和 `工具/生成瓦片.mjs` 顺序一一对应**）

```
0  浅灰地毯   1  深灰地毯   2  防滑砖     3  抛光砖
4  走廊地砖   5  木地板     6  白墙       7  玻璃
8  门横左     9  门横右     10 门竖上     11 门竖下
12 门横左关   13 门横右关   14 门竖上关   15 门竖下关
16 桌面
```

瓦片**全部程序生成**（`工具/生成瓦片.mjs`），不靠 AI 出图 —— 因为 AI 画不好可平铺的瓦片。

### 3.4 门（这套机制踩坑最多，改之前先读）

- **宽门 = 2 格 = 64px = 主角宽度的 2 倍**（用户明确要求）
- 门分**左右（上下）两半**，各一张瓦片。**不能简单并排放两张 1 格门** ——
  那样两格各画一片门扇，看着像两扇小门
- `门另一半` 映射给出另一半的相对坐标，**开关时必须两格一起改**
- **阻挡靠门自己的物理挡板，不靠瓦片**：
  - 门框：左右（横门）/上下（竖门）各 6.5px
  - `关挡`：关门时填满门洞（51px）
  - `开挡`：开门时门扇立着占的那条（14px）
  - `开关门()` 改完瓦片调 `刷新门体()`，用 `body.enable` 启停
- `挡路瓦片` 里**只有 白墙 + 玻璃**，**没有门**
  → 好处：**寻路可以穿过关着的门**（线一路指到门口，玩家开门），不用绕路
  （⚠️ 注意：**现在的指引线是直线穿墙**，连墙都不绕了，见 3.7；
  这条"门可走"只对 `找路()` 那套 A* 有效）

> ⚠️⚠️ **造网格里的顺序是硬约束**（2026 修订，踩过）：
> ```
> ① 铺各房间地面  ② 砌所有墙（含外墙圈）  ③ 铺竖廊（吃掉两格墙）  ④ 最后开门
> ```
> 原来的顺序是"**先开门、后砌外墙**"，结果**正门被外墙那一遍整格覆盖成白墙** ——
> 游戏里前台尽头是一堵死墙，`门表` 里那扇"公司正门"根本不存在。
> 实测办法：`node tools/读运行时网格.mjs 26 29`（读**游戏里真实那份网格**，
> 不是抄源码的逻辑 —— `tools/读网格.mjs` 是转述版，只作参考）。
> 正门还要**把南边几格也改成门瓦片**（`行 >= 地图高 - 外墙` 时继续往下打通），
> 否则门画上去了、人还是走不出外墙圈。

### 3.5 家具与 NPC

- `道具表`：每件道具的瓦片坐标 + `桌面?`（桌面小件不挡路）+ `挂墙?`（允许压墙）+ `偏移Y?`
- `占地表`：每件道具的**落地碰撞盒**（宽×高，底边对齐）。
  精灵是 44×48 的整块画面（含桌面以上部分），直接拿整张图做碰撞会出现空气墙
- **`座位表`**：能坐的瓦片坐标清单（转椅 + 会议椅）。
  `是椅子()` **查这张表**，不查 `道具表` —— 只认转椅的话，
  坐在**茶水间/会议室**的会议椅上的同事会站着（踩过）。**新加椅子要记得往这里补一格。**
- `NPC排布`：**按段号分组**，剧情换房间时同事就靠这几行坐标"传送"过去

#### 🎬 「同事传送 + 主角走过去」这套机制怎么工作（用户明确要的行为）

```
剧情播完 → 点「继续实习生活……」→ 点暂停()
   ① 段号 +1，队列清空，屏幕回地图
   ② 地图目标 = 下一段的 地图入口（指引线指向它）
   ③ MapScreen 的 useEffect 发现段号变了 → 场景 换NPC(段号)
      → 按 NPC排布[段号] **销毁旧精灵、在新坐标重建** = 同事"瞬移"到新房间
   ④ 玩家自己按方向键走过去（指引线是"直线穿墙"的方向标，见 3.7）
   ⑤ 走到入口按空格 → 地图交互() → 播那一段
```

**所以"同事已经在茶水间了"不用写任何逻辑**，只要把坐标写进对应段号那一套里。
现在的三套：段1 各就各位 / 段2 周岚去会议室、林总站窗边 / 段3 **小鹿·阿麦·韩策在茶水间**（群里那场），
周岚在会议室排期、林总在自己办公室。

⚠️ 写坐标时的三条硬要求（`node tools/NPC站位体检.mjs` 会替你查）：
1. **要让人坐着，坐标就必须正好落在 `座位表` 的格子上**，否则他会**站着**摆在那儿
2. **别站进家具里**（吧台/桌子/饮水机）：人的占地是 22×14，
   站到"家具占地 + 半个身位"以内就穿模
3. **同一套里不能两个人同一格**

> 看它：`node tools/房间实拍.mjs 茶水间`（出图到 `tools/shots/房间/`）
> —— 相机**跟随主角**、把主角传送到房间中心即成，**别手动 setScroll**（会被 bounds 夹到别处）。

### 3.6 深度（depth）规则 —— 视觉 bug 的高发区

```ts
普通道具     depth = y
桌面小件     depth = y + 0.5     // 必须在桌子之后画，否则被桌子吃掉
转椅         depth = y + 2       // 必须画在人前面，才是"坐在椅子里"而不是"浮在椅子上"
主角/NPC     depth = y
墙（遮挡层） depth = (行+1)*32 + 1  // ★ 见下
```

⚠️⚠️ **"NPC depth = y" 这条对**站着的**同事也必须执行**（2026-09 补，用户报的 bug）：

`建NPC批()` 里有两条分支 —— 能坐的走 `放到座位上()`（那里会设深度），
**不能坐的只 `setOrigin(0.5,1)`、忘了 `setDepth`** → 深度停在 **0** → 被画在**所有道具后面**。
症状：**会议室里站着的周岚，头被会议长桌整个吃掉**（用户报的"会议式的主管显示没有头"）。
⚠️ 为什么难发现：坐下的同事都是好的，只有"站位不在座位表上"的那几个人（周岚 (6,7)、
林总 (31,16)、韩策 (3,20)）中招，而**截图上看不出来是被谁挡的**。
量它的办法：`node tools/查深度.mjs`（把某个区域所有对象的深度从后往前打出来）；
`tools/NPC站位体检.mjs` 现在也**逐个数每个同事的深度**，`深 <= 0` 直接判失败。

**墙为什么要单独一层**：主角碰撞体只有 16px 宽（脚下那一小块），他能贴到墙边、
精灵上半身压到墙上。墙原来在 tilemap 里是整层 `depth -100`，永远被人盖住 → 看着像穿墙。
`建墙遮挡()` 把墙和玻璃**另外用一层精灵重画**，深度按行算，于是：
- 人在墙南边 → 人画在墙前面 ✅
- 人在墙北边 → 墙把人挡住 ✅
- 人和墙同一行（侧边的墙）→ 墙深度 +1，墙挡住人 ✅（原来穿墙的就是这里）

### 3.7 寻路指引线（**已改成直线穿墙**）

> 🔁 **本次改动**：用户要求指引线**做成直线、可以穿墙**。
> 现在的 `画指引线()` **不查通行表、不绕墙、不绕家具**，就是主角 → 目标拉一条直线，
> 沿线上铺流动的绿点，终点一个呼吸绿圈。
> - 好处：永远是两点之间最短的一条，玩家一眼知道"目标在哪个方向"
> - 代价：线会横穿墙体 / 办公桌（**这是用户要的**，别再当 bug 修回去）
> - 验证脚本：`node tools/指引线测试.mjs`（长线 + 短线两张图，并打印"直线 vs A* 绕路"的长度差）

**旧实现（A\* 沿地形寻路）还留着**，函数名 `找路()`，故意做成 **public**（不是 private）：

- 为什么留：以后要做「点地自动寻路走过去」「NPC 自己走到某处」时直接调它；
  调试时也能从控制台 `__lksMap.找路(起x,起y,终x,终y)` 问"这条路到底通不通"
- 做法：四方向 A*，`造通行表()` 把**墙 / 玻璃 + 家具占地**都标成不可走，**门可走**
- 两个关键设计（改之前先读）：
  1. **家具必须算进去**。只查瓦片层的话，线会从文化墙、接待台、办公桌里穿过去
  2. **转弯惩罚 0.4**。不加的话四方向代价都是 1，等代价路径有无数条，
     A* 会返回**阶梯状**的路（右一格、下一格、右一格…），看着像在乱绕。
     加了才会走长直段 + 直角转弯
- 它不再有调用方，所以**没有缓存了** —— 谁要用谁自己缓存（主角换格或目标变了才重算），
  别每帧跑一次

### 3.8 地图上的两张卡片（道具卡 / 人物卡，**一靠近就弹**）

用户要求：「增加全部道具的作用解释卡片，一靠近就显示」+「做 npc 的卡片对话」。

| 卡 | 数据在哪 | 位置 | 靠什么触发 |
|---|---|---|---|
| **道具卡** | `src/story/道具卡.ts` | 屏幕**左下**（蓝边） | 主角脚下点和道具**落地矩形**的距离（横向半格+10px、纵向 1.25 格）|
| **人物卡** | `src/story/人物卡.ts` | 屏幕**右下**（绿边 + 职位标签） | 离 NPC 精灵 < **40px**（量出来的数：同事坐的椅子格与上一格正好差 32px，写 30px 就永远弹不出来）|

场景侧：`OfficeMapScene` 里 `查附近物()` / `查附近人(换格了)`，**只在玩家换格时重算**
（道具表 90 多件，每帧全扫是白烧 CPU），变了才回调 React（`设附近物` / `设附近人`）。
门和玻璃是**瓦片不是道具**，所以单独按格子认（见 `格子卡`）。

**几个必须记住的坑**：
- ⚠️ **两张卡不能写成"二选一"**：工位格上本来就有桌面小件（名牌/键盘），
  写成 `卡片 ? ... : 人卡 ? ...` 的话**人物卡永远弹不出来**（实测踩过）。所以各占一角。
- ⚠️ **`prop_desk_笔筒` 故意不配卡**：它摆在不可走的那格上，玩家站不到，配了也永远触发不了。
- ⚠️ 站**工位**时优先给「办公桌」那张卡（桌/椅/键盘/名牌挤在一格半里，
  纯比"谁近"会弹到椅子）—— 见 `同格优先()`。
- ⚠️ 大件（沙发/前台）的 `占地表` 高度别写太高：写 18px 会让**两格外的玩家**也算"靠近"，
  实测站在前台前面弹的是「双人沙发」的卡。
- 量它：`node tools/道具卡体检.mjs`（抽查 + 每件道具周围 3×3 格全站一遍 + 五位同事逐一验）
- 看它：`node tools/卡片截图.mjs`（出图到 `tools/shots/卡片/`）

### 3.9 会议室两个房间的陈设（2026 改动）

- **讲台挪到会议室 B 的白板旁边**（用户要求）：`讲台 x35,y3` ← 原来在 `x3,y9`（会议室 A 西南角）。
  白板在 `x37,y3`，讲台就在它左边紧挨的一格，同一排。
- 会议室 A 里讲台原来的位置**空着**，没补别的东西（房间中间已经有长桌 + 四把椅子）。
- ⚠️ **会议室 A 里那个"白板"其实叫「投影幕布」**（`prop_meeting_投影幕布`，在 x6,y3）。
  如果用户想的是"把讲台摆到房间上方那块大屏旁边"，那就是挪到 A 的 (4,3)（实测该格是空的）。
  现在按字面理解放在了 B 的白板旁。
- 量它：`node tools/查重叠.mjs`（道具 vs 道具）+ `node 工具/查压墙.mjs`（道具 vs 墙），都要 0。

### 3.10 墙面元素怎么"贴"在墙上（**这段踩坑最多，改之前先读**）

用户的要求原话是：「**贴在墙上、重叠在墙上、显示在上面**，多放几个位置要起到指引作用」。

三条**缺一不可**（少一条就会出问题，我逐条踩过）：

| 要什么 | 怎么做 | 不这么做会怎样 |
|---|---|---|
| **压在墙上**（视觉上是贴在墙面） | 摆在**墙那一行**（不是墙下面那行） | 摆下一行 → 整块落在走廊地板上，**看着像掉在地上** |
| **显示在墙前面** | 给 `深度加: 40` | 不给 → 墙的遮挡层深度 = 行底+1、元素深度更小 → **被墙吃掉，完全看不见** |
| **整件落在墙带里** | 偏移要让"上边贴墙顶、下边**别越过墙底**"：墙带 ≈ 行13 的 y 416~448，锚点在行 13 时脚底像素 = 448，所以 `偏移Y ≈ -道具高度 - 12`（挂钟 28→**-40**、灭火器 30→**-42**、牌子 16→**-6/-8**）| 提得不够 → 下端压到墙下面的地砖上，用户会说"**位置不对**"（挂钟就是这么被报的两次）|

> ⚠️ **量位置别靠截图**：截图上很难判断"这个钟到底压在哪条砖缝上"。
> 正确做法是 `page.evaluate` 读 sprite 的 `y` 和 `height`，算出它的**像素区间**，
> 再和墙带区间（行 13 = 416~448、行 10 = 320~352、外墙 = 0~64）比。
> 我就是靠这个确认"挂钟 380~408 落在 416 以上的墙带里"的。

`深度加` 是 2026 新加的字段（`道具位.深度加`，见 `OfficeMapScene.建道具`）：
深度 = 脚底像素 y + 桌面/转椅修正 + 深度加。**专门用来让"某件东西画在墙前面"。**

**摆位要起到指引作用**（不是随便贴）：
- 出口牌对齐**门洞旁边**和**走廊岔口**——人顺着牌子走就能找到出口
- 主走廊**两侧都摆**：只摆一侧的话，回程全程背对牌子
- 竖廊里也摆（出口方向的最后一段），前台进门第一眼也有一块
- ⚠️⚠️ **只准贴"白墙"格**（用户明确要求："把玻璃上的去掉"）。
  主走廊北侧（行 10）看着像墙，其实 **x13-30 整段是玻璃**、还夹着 18-19 / 25-26 两个门；
  所以北侧只剩 **4 / 33** 两个白墙格能贴。
  **别靠眼睛看，跑 `tools/墙面元素体检.mjs`** —— 它按"元素和墙带的重叠比例"逐件判。
- ⚠️ **挂钟只放"房间角落那一面墙"**（用户要求"钟表只能出现在房间的角落里、不能遮挡"）：
  现在两处在 **会议室 A 的西南角（4,13）** 和 **前台大厅西侧（7,28）**；
  外墙那条（x9,y1）已经撤掉 —— 外墙是建筑最外圈，不算任何房间的角落。
- ⚠️ **家具不许堵通道**：跑 `node 工具/查挡路.mjs`（它把 10 组关键连通对都 A* 走一遍）。
  这套工具的必要性：`查压墙` 只查"道具 vs 墙"、`查重叠` 只查"道具 vs 道具"，
  **"家具把走道堵住"这两件都查不出来**（家具明明合法地放在地板上）。

现在一共 **9 枚出口牌 + 1 块楼层指示牌 + 消防栓 + 3 扇窗**（挂钟和灭火器按用户要求撤掉了）。
看它：`node tools/房间实拍.mjs 20 11 1`（主走廊）、`node tools/房间实拍.mjs 12 13 1`（南侧墙）、
`node tools/房间实拍.mjs 20 22 1`（竖廊）
量它：`node tools/墙面元素体检.mjs`（**改完墙面元素必跑**，应为 0 件问题）

---

### 3.11 坐姿系统（**玩家和同事共用一套逻辑，改之前先读这节**）

#### 一张表说了算：`level.ts` 的 `座位表`

```ts
interface 座位 {
  x: number; y: number;
  家具: string;          // 只用于说明/体检
  偏移Y: number;         // 坐下时人往上抬多少（负数=往上）
  套: 'back' | 'front';  // 背面（工位，对着电脑）/ 正面（其它，对着人）
}
```
**所有和"坐"有关的行为都从这张表读**，包括：
- 主角能不能坐（`OfficeMapScene.找空座位()`）
- 坐哪件、抬多高、用哪套姿势、深度多少（`放到座位上()`）
- **同事站在椅子上时也走同一个 `放到座位上()`** ——
  所以"和同事对齐"是**结构上保证**的，不是靠两边各调一遍。

#### 三个规则（用户逐条要求过，别改回去）

| 规则 | 实现 | 为什么 |
|---|---|---|
| **四个方向都能坐**（同格 + 上下左右，**不含斜角**）| `找空座位()`：`|dx|+|dy| === 1` | 斜角会把**隔壁那件家具**抓过来，人"啪"地横移过去（踩过）|
| **有人的座位不能坐** | `座位有人(座)` 拿同事的**逻辑格**比 | 不然会坐到同事身上 |
| **同格优先** | 脚下那格登记了座位就只认它（被占 → 直接判坐不下）| 不然会溜到旁边那件（站周岚椅子、坐到隔壁沙发）|

⚠️ 主角**没有**任何特殊待遇：他坐下的规则和同事摆位用的是同一套数据。

#### 三个数调"视觉对齐"（**改椅子素材就要重调**）

| 数 | 作用 | 现在取值 |
|---|---|---|
| `座位.偏移Y` | 往上抬到椅子座面 | 转椅 **-6** · 会议椅 **-12** · 沙发 **-4** |
| `x = 座位格中心` | 横向挪到椅子上 | 不挪的话人只在原地升高、像浮在旁边 |
| `深度` | `front` → 人**拎到家具前面**（+40）；`back` → 家具画在人前面 | 见 `放到座位上()` |

- **背面（转椅）**：椅子 44px 比人宽，**会把整张脸挡住** —— 偏移量**调过五次**
  （18→14→10→6），用户连续说"太高"，最后定 **-6**。
- **正面（会议椅/沙发）**：正面坐姿是全身，被家具盖住只剩一个头 → **+40 拎到前面**。

⚠️⚠️ **`走()` 每帧会重设主角深度**（`setDepth(y)`）。所以"正面坐要 +40"这件事
**必须能从状态推出来**，`座位.套` 就是为这个存的；不在 `走()` 里判一下，
坐下瞬间深度对了、**下一帧就被覆盖回去**，家具又把整个人盖住（实测 700 → 660）。

#### 怎么调、怎么验

```powershell
node tools/拍坐姿.mjs            # 三张放大图 + 打印 人y/人深度/同格家具深度
node tools/拍坐姿.mjs 4 20 3     # 指定格 + 缩放
node tools/坐姿体检.mjs          # 能坐哪 / 背面还是正面 / 有人的椅子不坐 / 四方向
node 工具/查座位.mjs             # ★ 座位表 vs 道具表（改名/挪家具后必跑）
```

⚠️ **改完一定要拍一眼**：这个数是"宁小勿大 / 宁大勿小"来回试出来的，光看代码判断不出来。

---

## 四、AVG 系统

### 4.0 会话切换与显示规则（**2026 改动，用户明确要求**）

#### ⌨️ Tab 切屏：**一次按键只能切一次**（用户报的"打开微信被强制关闭"）

三个原因叠加，都修了：

1. **切屏瞬间两个 keydown 监听器会同时存在** —— React 的卸载/挂载发生在
   **同一个键盘事件之后**，所以按一次 Tab：地图那个监听器切到微信 →
   微信那个紧接着又切回地图（实测：一次 Tab 之后屏幕停在 map）。
   **修法：两个监听器都先判"我现在还在不在这一屏"**（`useStory.getState().屏幕`），
   不是就 return。`MapScreen` 和 `Avg` 里各一道闸，成对出现，别只改一边。
2. **自动推进会在玩家看别的会话时也在后台跑** —— 玩家点开林总私聊，
   剧情却在后台把群消息一路播下去。
   **修法：`Avg` 的自动推进加 `是活跃` 条件**（玩家正在看的就是脚本在发的那个会话）。
3. ⚠️⚠️ **自动推进不能直接播进「去房间」节拍**（这是"被强制关闭"最直接的原因）：
   那条节拍会把屏幕切到地图，玩家刚掏出手机，界面自己就跳回办公室了。
   **修法：`去房间` 节拍改成"只登记 `待去房间`、不切屏"**，聊天区底下出现一个
   **「去 林总办公室 ▸」按钮**，玩家点一下才真的切地图走。
   自动推进的条件里也加了 `待去房间` —— **它会停在那儿等玩家**。

#### 📱 打开微信要落在"正在发消息的那个会话"

用户报的"**主线不发消息了**"其实是这个：玩家上次停在"林总私聊"（空的），
一掏手机还是看那个空会话 —— 而剧情此刻正在**群聊**里发消息，
玩家看到一整屏空白，以为剧情停了（实测：群里有 4 条未读，界面却停在 lin 私聊）。
**修法：`掏手机()` 里优先落到活跃会话**（**它还有未读时**才跳；没未读就尊重玩家当前看的那条）。

#### 🔴 未读红点只算"真消息"（**时间 / 系统提示不算**）

用户要求：「时间和提示不要算作未读消息」。

`追加()` 里的判据：
```ts
const 算未读 = (条) => 条.种类 !== '时间' && 条.种类 !== '系统';
未读: 在看 || !算未读(条) ? c.未读 : c.未读 + 1
```
- **不算**：`时间`（`day-03 · 下午 16:11` 这种分隔符）、`系统`（"你已添加 小鹿…"这类提示条）
- **算**：`消息` / `贴纸` / `邀请` / `知乎卡`（"有人跟你说话"的那些）

⚠️ 算错了会很难看：一句时间戳就让小红点跳出来，玩家点进去发现**没有新消息**。
量它：`node tools/微信开关体检.mjs`（第 ⑥ 组：时间→0、系统→0、第一条消息→1、第二条→2）



用户报的「**不切换到相应的对话框就不进行**」就是这个：
原来的自动推进挂在 `Avg` 的 `useEffect` 里，条件是"玩家正在看的会话 == 正在发消息的会话" ——
于是剧情在**群聊**里说话、玩家点开的是**林总私聊**时，**定时器根本不挂，剧情卡死**，
玩家得自己点回群聊才继续。

**修法：把自动推进抽成 `StoryClock`，挂在 `Root` 上**：
- 条件只有"**屏幕 === 'avg'**" —— 在微信界面就按自己的节奏播，跟你看哪条无关
- 不是当前会话的消息就进**未读红点**（这才是微信的行为）
- 四个"等玩家"的状态（选项/邀请/暂停/去房间）和播完都要停
- `切会话` 之后那一拍**多等一会儿**（≥1.4s），给玩家时间看见未读红点

⚠️ **`Avg` 里不能再留第二份定时器** —— 两个定时器会互相打架、剧情一次跳两步。

#### ␣ 空格（Space）是"激活键"，`preventDefault()` 必须最先调

用户报的「按空格坐下没反应」有两层原因：

1. **空格在"没有交互点的地方"以前什么都不做。**
   `MapScreen` 的按键处理是"先看门口 → 再看 `附近交互点` → 有才 `地图交互(id)`"，
   **空椅子那一格根本没有交互点** → 按空格直接落到最后、无声无息。
   **修法：座位判断提到最前面**（脚下是椅子/沙发 → 空格就是坐/站），
   不用等交互点。判据用 `站在座位上`（场景每帧回报）。
2. **`preventDefault()` 调得太晚。** Space 是浏览器的**激活键**：
   焦点如果在某个 `<button>` 上（右下角"微信"很容易拿到焦点），
   浏览器会用空格去点那个按钮。**修法：进 Space 分支第一件事就 `preventDefault()`**，
   早于所有分支。

量它：`node tools/坐姿体检.mjs` 第 ①b 组（**故意把焦点放在按钮上**再按真键盘空格）

#### 🚫 不能 `<button>` 套 `<button>`

React 会刷一屏 `In HTML, <button> cannot be a descendant of <button>`（用户报过）。
本项目里踩到两处，都是"头像既自己可点、又被放进了按钮里"：

| 位置 | 修法 |
|---|---|
| 功能栏顶上"我的资料"按钮里的头像 | 头像组件加 `静态` 参数 → 渲染成 `<span>` |
| 会话列表 `wc-row`（**整行就是按钮**）里的头像 | 同上，`<头 谁={...} 静态 />` |

`头()` 现在是：`静态` 就输出 `<span className="wc-face" aria-hidden>`（里面只有 `<img>`），
不静态才输出可点的 `<button>`（聊天气泡里那种独立头像仍然可点看资料卡）。

⚠️ 自查：`document.querySelectorAll('button button').length` 必须是 **0**。



用户要求：「主角可以坐工位上、坐**空椅子**上、坐**空沙发**上，并且判断是背面坐还是正面坐」。

**能坐的地方 = `level.ts` 的 `座位表`**（一张格坐标清单）：
```
开放办公区 8 把转椅 + 两把单人沙发（17,4)(23,4)
总监办公室 转椅(28,17) + 双人沙发(30,20)(31,20)
茶水间 会议椅(4,20)(8,20)      会议室A 4 把       会议室B 2 把
前台大厅 双人沙发(17,26)(18,26)
```
⚠️ **沙发是"大件横着摆"，不是一格** —— 要把它占的**每一格都登记**，
只登记中心格的话，玩家站沙发边上会坐不下去。

**背面还是正面 = `OfficeMapScene.坐哪套()`，只看"坐在哪个房间"**：
| 坐在哪 | 用哪套 | 为什么 |
|---|---|---|
| **开放办公区**（13≤x≤30, 2≤y≤9）的椅子/沙发 | `坐_*` **背面** | 自己工位，对着电脑 |
| 其它地方（茶水间 / 会议室 / 总监办公室 / 前台沙发）| `坐正_*` **正面** | 坐着跟人说话，要看到脸 |

⚠️ 不按"椅子朝向"算是**有意的**：椅子朝向在 `道具表` 里没记（都是同一张图），
按朝向要新建一套朝向数据；而"哪儿背对、哪儿面对"**看房间就够了**。

#### 🪑 坐姿的"对齐"是**三个数**在配合（改椅子素材就要重调）

用户报过两次坐姿不对（"只有两只耳朵露在椅子外面"、"正面坐被椅子挡住"）：

| 数 | 作用 | 现在取值 |
|---|---|---|
| `座位.偏移Y` | 坐下时把人**往上抬**到椅子座面 | 转椅 **-14** · 会议椅 **-12** · 沙发 **-10** |
| `座位.套` | `'back'` 背面（工位）/ `'front'` 正面（其它）| 见 `座位表` |
| `坐姿深度()` | `front` → 人**画在家具前面**（+40）；`back` → 椅子画在人前面 | `OfficeMapScene` |

- **背面（工位）**：椅子 44px 宽、比人宽，**会把整张脸挡住** —— 所以人要抬到
  "脸和上半身露在椅背上面、下半身被椅背挡住"。这个数**调过三次**：
  6 → 只剩耳朵 · 18 → 太高（用户报"动画太高"）· **14 正好**。
- **正面（茶水间/会议室/沙发）**：正面坐姿本来就是全身，被家具盖住就只剩一个头 ——
  **整个人拎到家具前面**（深度 +40）。
- 主角和同事**共用同一套数**：这就是"和同事们一样对齐"的做法，改一处两边同时生效。

#### 🚫 有人的座位不能坐（`座位有人()` + "同格优先"）

用户要求：「可以坐**有人的位置上**解决」—— 即别坐到同事身上。
- `座位有人(座)`：拿同事的**逻辑格**（`NPC所在格`）和座位比，占了就跳过。
- ⚠️ **同格优先**：玩家**正好站在**某件座位上时，只认那一件 ——
  它被占了就**直接判"坐不下"**，不能"溜到旁边那件"。
  踩过：站在周岚的工位椅上按空格，人坐到了隔壁 17,4 的**单人沙发**上，
  看着像"坐到别人位置上去了"。
- ⚠️ 体检脚本里的"空工位"**必须动态找**（`找空工位()`）：
  写死 `(21,4)` 会撞上小鹿坐着的椅子 → 断言"空工位能坐"必然失败（踩过）。

⚠️⚠️ **`走()` 每帧会重设主角深度**，所以"正面坐要 +40"这件事必须能**从状态推出来**——
`座位.套` 就是为这个存的。不在 `走()` 里判一下，坐下瞬间深度对了、
**下一帧就被覆盖回去**，家具又把整个人盖住（实测：700 → 660）。

⚠️ 调完一定要拍一眼：`node tools/拍坐姿.mjs`（会把三种家具各拍一张放大图到 `tools/shots/坐/`）

#### 🚶 "在椅子附近就能坐"（不是只能站正那一格）

用户要求：「只要是在椅子附近就能坐下，不是只能在椅子前面或后面」。
- 判定：`找附近座位(x, y, 半径 1.6)` —— 切比雪夫距离（斜着也算 1）
- ⚠️ **排序必须两段**：先比格间距、再比**欧氏距离**、最后优先"同一格"。
  只比格间距会挑错 —— 玩家站在工位椅上时，它和**旁边的单人沙发**格间距都是 0，
  先遍历到的那件（沙发）会被选中，人就坐到沙发上去了（实测踩过）。
- 坐下时**把人挪到选中那把椅子上**（x 和 y 都要改）。

⚠️⚠️ **抬高之后，所有"按像素反推格号"的地方都会差一行**（实测 4,20 → 4,19）：
- 体检脚本读同事位置要用 `NPC所在格()`（返回**逻辑格**），不能用 `(sprite.y-32)/32`
- `查附近人()` 判距离也要按**逻辑格的脚底**算，不能按精灵画在哪 ——
  不然坐着的同事离玩家"远 20px"，站在旁边弹不出卡（实测 5 位同事全漏）

#### ␣ 按空格的优先级（**这个判断写在两个地方，改一处必须改另一处**）

规则本身（作者定的，别改回去）：

1. 「去房间」续播 → 2. **座位（只有"还没坐下"时才吃）** → 3. 门 → 4. 交互点（主线/支线）→ 5. 站起来

- 座位**必须排在主线入口前面**：`我的工位` 既是交互点、又是他工位那把椅子 ——
  主线入口先判的话，站在工位上按空格只会开主线、**永远坐不下去**。
- 但它**不能挡住主线**：所以只有"**还没坐下**"（`!坐着`）时才吃掉这次按键 ——
  第一下坐上椅子、**第二下才开主线**（像"坐好了再看手机"）。
- 空椅子上没有交互点，所以第 2 步**必须留着**（不然"按空格坐下没反应"）；
  而"坐着、这格又没别的事"要走第 5 步站起来，否则坐下去就下不来。

⚠️⚠️ **这个判断写在两个地方**（踩过，用户报的 bug 就是它）：

| 在哪 | 干什么 | 判据 |
|---|---|---|
| `screens/MapScreen.tsx` 的 keydown（真键盘）| 决定这一次按键给谁 | `站在座位上 && !坐着` |
| `state/story.ts` 的 `地图交互` | 决定"交互时先坐还是先开会" | `(站在座位上 \|\| 近座) && !坐着` |

**两处条件必须一致。** 曾经 `MapScreen` 那处**漏了 `!坐着`** —— 于是空格在"坐 ↔ 站"里
无限循环，`地图交互` 永远走不到，**「我的工位」上的主线被彻底锁死**
（用户报："按空格触发主线，但按空格被坐座位抢死了，导致触发不了剧情"）。
`地图交互` 里那句"第二下开主线"当时其实是**死代码**。

⚠️ 为什么键盘那道闸不能再往前收（比如干脆"主线优先"）：空椅子那一格没有交互点，
   收了就退回到"按空格坐下没反应"那个老 bug。**两个特性都得留，靠 `!坐着` 分先后。**

量它：`node tools/空格优先级体检.mjs`（**改空格/座位相关逻辑必跑**）——
它用真键盘走三遍，每遍开一张干净页面：
① 工位上"坐下 → 再按开主线"（被锁死的就是这一步）② 空沙发上"坐下 → 再按站起来" ③ 干净地板不误触。

⚠️ "坐/站"走的是**一次性任务**（`剧情状态.请求坐` = `'坐' | '站' | null`），
不是计数器也不是布尔：计数器会在重渲染时重复触发，布尔分不清这一次点的是坐还是站。
MapScreen 执行完立刻 `坐完了()` 清掉。

量它：`node tools/坐姿体检.mjs`（工位背面 / 空工位背面 / 茶水间正面 / 沙发正面 / 走廊坐不下）

#### 🚶 剧情"去房间"时，那个房间的同事要**先传送过去**

用户报的：「切换到茶水间时相关人员并没有传送过来」——
走到茶水间，房间里摆的还是**上一段那套**（大家都在工位上），空无一人。

原因：同事站位原来只跟 `段号` 走，但**"去房间"和"分段"不是一回事** ——
事件二中间要去老板办公室、还要去茶水间，而 `段号` 要等整段播完才推进。

**修法：`level.ts` 加了一张 `房间对应排布`（房间 → NPC 排布下标）**，
`去房间()` 那一拍就查它、把 `剧情状态.NPC排布号` 定下来 →
**人先"传送"到位、玩家再走过去**（正好就是用户要的效果）。

⚠️ 这张表的三个坑（都踩过）：
1. **`我的工位` 故意不写** —— 那套"各就各位"本来就该跟着 `段号` 走。
   写死成某一套会把段里的同事从工位上撤掉（事件二"小鹿在工位上等你"，
   结果小鹿被换走、工位上空无一人）。
2. **`NPC排布号` 要"粘"到这一段结束**，不能一续播就清 —— 否则你在茶水间说完话、
   掏出手机又回地图，小鹿立刻被换回工位（"工位上没有小鹿、茶水间也没解决"就是这个）。
   清的地方**只有两处**：`点暂停`（这一段演完）和走"主线入口"开下一段。
3. **加新房间/新剧情时记得补一行**，否则那个房间会沿用段号那套（通常是错的）。

量它：`node tools/传送体检.mjs`（断言"第二趟去茶水间""小鹿/阿麦 已经坐在那两把椅子上、
而且是正面坐姿""排布号保留到段末、换段才复位"）

#### 🗺️ 剧本结构：谁该在哪个房间，是**剧本作者写的**，不是引擎猜的

判断"这段戏在哪个房间"看两处：
- `剧本段.地图入口`（**这一段**从地图的哪个点启动）
- 段里的 `{ 类型: '去房间' }`（**这一段中间**要走去哪儿）

⚠️ **两段不能指向同一个房间**，否则玩家会**连着走两趟**（用户报的"茶水间连续触发两次"）。
现在是：

| 段 | 地图入口 | 段内的 `去房间` |
|---|---|---|
| 事件一 | 无（开场直接播）| — |
| 事件二 | 我的工位 | ① 林总办公室（被老板叫去）→ ② **我的工位**（回工位，小鹿在）→ ③ **茶水间**（那场群聊戏）|
| 事件三 | **前台**（下班打卡）| — |

⚠️ 顺序是用户定的：**被老板训完 → 先回工位 → 再去茶水间**。
茶水间那场戏的台词（拉群 / 群聊 / 吐槽 / 三选项）原来在 `事件三`，
因为 `事件三` 的入口也是茶水间 → 连着走两趟，所以**搬到了事件二**，
`事件三` 改成"茶水间聊完去前台下班"。

#### 🚶 房间内换场景 = 回地图走过去（`去房间` 节拍）

用户要求：「这种切换场景的情况要切换到 2D 模式、走到对应的房间场景再继续，就像走到自己的工位上」。

剧本里凡是要**换个房间**的，都插一条：
```ts
{ 类型: '去房间', 去哪: '林总办公室', 提示: '去林总办公室' }
```
引擎行为（`state/story.ts`）：
- `去房间` → `屏幕='map'`、`地图目标=去哪`、`续播={去哪, 从这: 位置+1}`、**位置不动**
- 玩家走到那儿按空格 → `地图交互` 先查 `续播`：命中就 `位置=从这` 切回微信**接着播**
- 走错地方按空格 → 该放支线小片段的照常放，**不影响** `续播`（回来还能接着走）

⚠️ **`去房间` 和 `暂停` 完全是两件事**，别混：
| | 触发 | 干什么 | 用哪个字段 |
|---|---|---|---|
| `暂停` | 这一段演完了 | 回地图，指引线指向**下一段的 `地图入口`** | `地图目标` |
| `去房间` | **这一段还没演完** | 回地图走过去，**回来接着播本段** | `地图目标` + `续播` |

⚠️ `地图交互` 里**先判续播、再判"主线入口"**：目标点有时**同时也是**别的东西
（比如"回我的工位"，那格上挂着交互点、也是下一段的入口），顺序反了会走岔。

现在插了两处：事件二 ①`林总办公室`（周岚说"老板有事叫你"之后）
②`我的工位`（小鹿私聊说了半句，剩下的话走过去听）。
量它：`node tools/去房间体检.mjs`

| 规则 | 怎么做 | 为什么 |
|---|---|---|
| **不自动切会话** | `切会话` 节拍**只改 `活跃会话`**（消息往哪发），不动 `查看会话` | 用户要求"改为自己点击切换会话"|
| **未读数字小红点** | 没在看的会话，来消息就 `未读+1`，列表上渲染 `.wc-badge` | 不跳视图就必须有红点提示，否则玩家不知道去哪儿看 |
| **接受邀请例外** | `接受邀请` **会**切视图（邀请是当场发生的事）| 点了"加入群聊"却看不到群会很怪 |
| **只显示时间提示** | 新的节拍类型 `时间` → 聊天流正中一行小灰字（`.wc-time`）| 用户要求"只显示时间提示，不显示旁白"|
| **剧情旁白直接删掉** | 36 条 `旁白` 节拍全部清掉（时间类的改成 `时间`）| 不是藏起来 —— 剧本里就不该有。`旁白` 类型**保留**在代码里备用 |

> ⚠️ **邀请卡画在"发邀请的那个私聊"里，不是画进被邀请的群**（2026 修订，用户报过两次）。
> - `邀请` 节拍：卡片追加到**当前 `活跃会话`**（林总/小鹿的私聊）；`会话id` 只记"接受后切到哪"。
> - `接受邀请`：**只把私聊里那张卡标成「已接受」+ 建群（若还没有）+ 切到群里**，
>   ⚠️ **绝对不要往群里再塞一张卡** —— 之前就是这一句把"群邀请"塞进了群聊，
>   玩家看到的是"群里多了一张邀请卡"。
> - 反过来，**接受邀请会切视图**（邀请是当场发生的事）；普通 `切会话` 不切。
>
> 量它：`node tools/AVG体检.mjs` 会断言"接受前后，邀请卡都只在 lin 会话里，group 里一张都没有"。
>
> ⚠️ **每个会话的第一条消息前面都要有时间戳**（用户要求："第一次发信息也要有时间显示"）。
> 玩家点开任意一个会话，顶上都能看到"这是什么时候的事"。
> 现在的锚点：事件一 `day-01 · 上午 10:00`（林总）／`上午 11:20`（周岚）／
> 事件二 `day-03 · 下午 14:25`（林总私聊）／`14:21`（群）／`16:11`（小鹿）／
> 事件三 `day-05 · 上午 9:41`（小鹿）／`9:42`（狐说八道群）／`16:30`（收尾）。
> ⚠️ 时间要**顺着剧情走**，别出现"后面会话的时间比前面早"（改剧本时对着上面的表核一遍）。
>
> ⚠️ 地图上的支线小片段（`mapScenes.ts`）也按同一规则处理了：
> 时间/场景描述用 `时间`，纯心理描写删掉，只留"玩家自己发的"（`消息 谁:刘看山`）。
>
> ⚠️ **右下角的卡片不能压住右下角的按钮**（用户报"npc 信息有点遮挡"）：
> 设置齿轮已从"微信正上方"挪到 `right:12px; bottom:62px`（微信按钮上方一小格），
> 人物卡 `bottom:64px` 往上长，两者竖直错开；手机端另有一套值（见 map.css 的 @media）。
> 改这几个 `bottom` 之前先算一遍：卡片底边 vs 齿轮顶边（62+34=96px）会不会打架。
>
> 量它：`node tools/AVG体检.mjs`（验"不跳视图 / 未读红点 / 旁白为 0 且时间提示在"，
> 并出一张截图到 `tools/shots/avg/`）

- `Avg.tsx` 是微信式界面：左侧会话列表 + 右侧聊天区 + 可拖拽分隔条
- 浏览器窗口**横向可拉伸**，不是固定尺寸
- 播放引擎在 `state/story.ts`：队列逐条播，每条 0.8~4s（按字数）
- 「正在输入」只在私聊显示
- 收束顺序固定、A/B/C 选择不可撤销

**键盘**：
```
地图   Tab → 微信   Esc → 设置   Space → 交互/开关门（门优先）
微信   Tab → 回地图              Space → 推进（和点聊天区等价）
```

> ⚠️ **交互键为什么不在 Phaser 里判**：Phaser 的键盘监听依赖画布焦点，
> 真人要先点一下画布、自动化点不到。所以统一由 React 层处理（见 `MapScreen`）。

**全局规则**（从旧工程继承，别丢）：
- G1 消息逐条 0.8~4s ／ G2 「正在输入」只私聊 ／ G3 收束顺序固定
- G4 A/B/C 不可撤销 ／ G5-G6 跑团式（15 轮上限，命中 3/2/0-1）
- G7 调试码 159/258/357 ／ G8 结局（三项≥9 且总分≥29 跳级 / ≥3 且≥14 转正 / 其他结束）
- G9 三项指标：老板信任 / 团队协作 / 职业成长
- **AI 不得改分数**、**不得编造知乎来源**、**断网时主线要能走完**

---

### 4.1 跑团（知乎 AI 写对话）与小游戏（**2026 新增，用户明确要求**）

这两块都是"用户提要求 → 做进剧情"的，看代码之前先看这一节。

#### 跑团：**随机关进笼子**，AI 只负责"说得好听"

用户要求：「跑团剧情增加知乎 AI **不偏离剧情的丰富有趣对话**」。

| 层 | 谁负责 | 能不能交给 AI |
|---|---|---|
| **掷骰 / 判定 / 成败** | `跑团.ts` 纯函数 | ❌ 铁律 |
| **四档后果（指标 / 线索 / 谁说）** | 剧本作者手写（`跑团脚本.ts`） | ❌ 铁律 |
| **一个镜头 + 一句台词** | 知乎 LLM（`llm.ts`） | ✅ 就是给它干的 |
| **过不了闸时的兜底** | 作者预写（15 回合共 60 档） | ❌ 铁律 |

三道锁（改之前先读 `跑团.ts` 顶部注释）：
1. **谁能在这一轮说话**由 `回合.在场` 写死 —— AI 编一个新人物 → `校验对白` 挡下；
2. 镜头和台词**各过各的闸**：镜头不过 → 整条退兜底；**只有台词不过 → 镜头留着、台词退作者的兜底**；
3. **顺序绝不能反**：引擎先算完、兜底那句**立刻上屏**，再去要 AI 的话；
   回来了用 `贴AI话` 只换文字 —— 分数 / 线索 / 局况一个都不动。

**UI 是玩家驱动的**：跑团期间 `StoryClock` 停摆（不然玩家还在读，时间到了就跳下一轮），
玩家点一次骰子走一轮；三个收束（通关/翻车/超时）**都往下走，不改主线**。

- 量它：`node tools/跑团自检.mjs`（**69 条**：判定/修正/四档收束/镜头闸/台词闸/过闸/`贴AI话`/提示词/15 回合结构）
- 看它：`node tools/事件四验收.mjs` → `tools/shots/事件四/`

#### 小游戏：**从"投简历"改成"工作上的事"，画面与操作对齐地图**

用户要求：「小游戏也改为**工作上的事**不是投简历了」+「**画面和操作方式都要改为和现在主题一样**」。

| | 改版前 | 现在 |
|---|---|---|
| 这件事叫什么 | 招聘会 · 投简历 | **把这一版稿子做出来** |
| 取料 | 简历架 | **文件柜**（领任务单） |
| 三道工序 | 排版 / 打印 / 盖章 | **查资料 / 写稿 / 校对** |
| 交付 | 投递箱 | **交稿箱** |
| 帮手 | 学长（`chr/senior_*`） | **相关同事**：按工单客户选（`小鹿 · 校园方向` → 小鹿），都不相关就随机抽一个 |
| 画面 | `assets/chr|prop|tile` 那套老 demo | **`assets/map/`**（和地图同一批瓦片/精灵表/家具） |
| 主角行走 | 正/背面 + 压缩 2px 的踏步帧 | 地图那张 `chr_lks_walk.png`（4 行 × 4 帧） |
| 界面 | 深蓝黑游戏 HUD（顶栏 + 左右边栏） | **全屏**：上 1/5 任务条 + 下 4/5 画布，**米白 + 绿 + 棕**（和 AVG/地图同一套） |
| 稿子的状态 | 看不出来 | **每做完一道工序，在稿子上盖一枚图标**（放大镜/稿纸/对勾，胡闹厨房式） |
| 操作 | WASD + 空格 + Q + Esc | **+ 方向键 + Shift 快走**（和地图一致） |

⚠️ **机制一个字没改**：还是《胡闹厨房》那套（取料 → 不同顺序的加工位 → 交付、随时丢下/捡起、多做了也算过）。
   改的只是"这件事在游戏里叫什么、长什么样"。

⚠️ **三处瓦片编号必须一一对应**：`map/level.ts` 的 `瓦片` 枚举 ⇄ `greybox/level.ts` 的
   `关卡瓦片` ⇄ `game/assets.ts` 的 `瓦片顺序`。改一处要三处一起改。

⚠️ **关卡不进体检套件**（`体检全部.mjs` 跑的是地图与 AVG），全靠 `tools/事件四验收.mjs`（**40+ 条**，含手机竖版与"帮手真的在干活"）。

#### 2026-09 第二轮：全屏 / 1:5 任务条 / 工序图标 / 帮手

用户这一轮的要求（照抄，改之前先看）：
「小游戏做一下优化改为**全屏**，但是上下分为 1:4，上面 1/5 显示需要的任务，
不要这样的 ui，要做成**主题样式**和当前主题一致的 UI，
并且每个文件的**状态在每个阶段都要能肉眼看出来**，就像胡闹厨房一样每添加一样就在这个物体上显示特定的图标，
并且 ai 帮手是**相关的同事**，没有相关同事就是一个人，如果是多个就随机出一个，
然后 ai 帮手**自己干活来完善这个任务但是很慢**，大部分还是要玩家来完成，
ai 帮手又有**自己的工作逻辑**要不然会卡住，
并且都做成和主角一样**有动画有物理实体碰撞**。」

| 事 | 怎么做的 | 在哪 |
|---|---|---|
| 全屏 + 上 1/5 任务条 | 自算"内部格数 × 整数倍 ≤ 可用区域"，画布用 CSS 放大整数倍（**像素完美没破**）| `screens/关卡.tsx` 的 `算尺寸` |
| 关卡**按格数现造** | 全屏 ⇒ 格数随窗口变（PC 15×7 / 手机 12×21），布局不能写死 | `greybox/level.ts` 的 `造关卡` |
| UI 换主题样式 | 任务条 / 工单卡 / 提示 / 飘字全部米白+绿+棕；旧 HUD（`ui/App.tsx`、`Hud.tsx`、`usePixelFit.ts`）已删 | `screens/关卡.css`、`ui/Toasts.tsx` |
| 工序图标 | 每完成一道工序，在**手上和地上的那件东西**上盖一枚（三枚形状完全不同，缩到 12px 也分得清）| `OfficeScene.制作工序图标` / `同步图标` |
| 帮手＝相关同事 | 按工单客户名匹配候选（小鹿/阿麦/韩策/周岚），都不相关就随机 | `OfficeScene.选帮手` |
| **队友只有他一个** | 用户要求「AI 队友**去掉不干活的队友**」—— 原来还摆 2~3 个只会站着（有碰撞体、会挡路）的背景同事，**已全部删掉**；关卡里只有玩家 + 一个真干活的帮手 | `greybox/level.ts`（没有 `crowd` 了）|
| **画布上不许有浮层** | 用户要求「每个机器比如打印机要**显示出来**，并且**提示不要盖住字体和素材**」—— "现在该做什么"和飘字原来 fixed 浮在画布上，把最上面那排工位连名牌一起盖住；现在**整行都排进任务条**，画布区域百分之百干净 | `screens/关卡.tsx` 的 `.lv-help` |
| 帮手真的干活但很慢 | 完整链路（领单 → 逐道工序 → 交稿）；走路比玩家慢、每道工序 **2.4s**（玩家 0.9s）、干完一单歇 4~9s | `NPC_PROCESS_TIME` / `NPC_IDLE_RANGE` |
| 帮手不卡死 | **看门狗**：某状态待太久且位置没变 → 丢掉手上的活、回待命、**并挪回出生点**（卡住多半是被家具/同事顶住）| `OfficeScene.tickNpc` / `帮手重规划` |
| 都有动画 + 物理碰撞 | 帮手是**物理精灵**、会挡人（背景站桩同事已按用户要求删掉）| `buildActor` |
| **帮手走真行走动画** | 同事四方向行走图已到位（阿麦/小鹿/韩策/周岚，128×192 = 4×4）→ 关卡里**播真动画**；没有图的自动退回"朝向帧 + 1px 浮动" | `OfficeScene.制作同事动画` / `走向` |

✅ **同事行走图已到位**（2026-09，阿麦/小鹿/韩策/周岚 四张全有）。完整流水线**照抄这四步**：

```powershell
cd E:\projects\知乎小游戏
# ① 出图工具导出的是"假 PNG"（扩展名 .png、内容是 JPEG）→ 先修格式
cd liukanshan-career; node tools/修图格式.mjs "..\美术\素材库\原图\11-角色动画"; cd ..
# ② 连通块切帧 → 128×192（4 列 × 4 行 × 32×48）
node 工具/处理角色动画.mjs "美术/素材库/原图/11-角色动画/角色_阿麦_四方向行走16帧.png" 4 4 32 48 "美术/素材库/成品/角色动画" --模式=等身
# ③ ⚠️ **必须**：把第 4 行（朝右）做成第 3 行（朝左）的水平镜像，否则"走两步回一次头"
node 工具/修行走表.mjs "美术/素材库/成品/角色动画/角色_阿麦_四方向行走16帧_32x48.png" "liukanshan-career/public/assets/map/chr_mai_walk.png"
# ④ 登记 + 拷进游戏（映射表在 工具/接入地图素材.mjs 的「三、角色动画精灵表」）
node 工具/接入地图素材.mjs
```

- 素材 key 在 `game/assets.ts` 的 `同事行走图`；游戏里文件叫 `chr_<拼音>_walk.png`
  （阿麦 mai / 小鹿 lu / 韩策 han / 周岚 zhou）。
- ⚠️ **不要**拿行走图去覆盖 `npc_<名>.png`：地图上站着的同事用的是那个 6 格姿势表，
  换掉会让地图上的同事**全变成"朝下"的怪姿势**。两者是两套文件。
- ⚠️ `工具/处理角色动画.mjs` 以前会报"7895 色"（它数的是**未量化的中间缓冲**）——
  已修成数真正写出去的那张；判断干净程度用 `工具/图片体检.mjs`。

✅ **这一批 9 件素材全部到位并接入**（2026-09）。除行走图外还有三组，流水线照抄：

```powershell
# 出图都是"假 PNG"（扩展名 .png、内容是 JPEG）→ 永远先修格式
cd liukanshan-career; node tools/修图格式.mjs "..\美术\素材库\原图\21-关卡升级"; cd ..

# 工序图标 3 枚：一张大图横排 3 件 → 切件（会抠品红 + 去毛边 + 量化）
node 工具/切件.mjs "美术/素材库/原图/21-关卡升级/界面_工序图标3枚.png" 3 1 16 "美术/素材库/成品/界面/图标" --帧名=查资料,写稿,校对 --前缀=界面_图标_工序

# 手上那两件纸（成品名以「桌面_」开头，接入工具的自动映射才会给出 prop_desk_任务单_*.png）
node 工具/转像素画.mjs "美术/素材库/原图/21-关卡升级/道具_桌面_任务单_空白.png" "美术/素材库/成品/道具/桌面/桌面_任务单_空白.png" --精确=32x32 --透明
node 工具/转像素画.mjs "美术/素材库/原图/21-关卡升级/道具_桌面_任务单_写过.png" "美术/素材库/成品/道具/桌面/桌面_任务单_写过.png" --精确=32x32 --透明

# 会议室插图（喂给跑团面板左边那块底图）
node 工具/转像素画.mjs "美术/素材库/原图/21-关卡升级/背景_会议室_横版.png" "美术/素材库/成品/背景/会议室_横版.png" --精确=512x288
node 工具/转像素画.mjs "美术/素材库/原图/21-关卡升级/背景_会议室_竖版.png" "美术/素材库/成品/背景/会议室_竖版.png" --精确=288x512

node 工具/接入地图素材.mjs && node 工具/接入开场素材.mjs
```

- 映射表：工序图标与两件纸在 `工具/接入地图素材.mjs`；会议室插图在 `工具/接入开场素材.mjs`（→ `avg/bg_meeting_*.png`）。
- 代码钥匙：`game/assets.ts` 的 `工序图标` 与 `图.任务单/图.稿子`；`story/assets.ts` 的 `插图表.会议室`。
- ⚠️ **工序图标有兜底**：三张真素材读不到时，`OfficeScene.制作工序图标()` 会**现画一枚**顶上（放大镜/稿纸/对勾），
  用的是**同一个 texture key**，所以换素材不用改任何调用点。顶部任务条上的小图标也用同一批 PNG。
- ⚠️ `工具/处理* / 切件 / 转像素画` 的成品**命名要顺着接入工具的自动映射规则**：
  `成品/道具/桌面/<x>_<名>.png` 会被映射成 `map/prop_desk_<名>.png`（**去掉第一个下划线前的那段**）。
  所以两件纸的成品名叫 `桌面_任务单_空白.png` 而不是 `任务单_空白.png`（后者会变成 `prop_desk_空白.png`）。

#### 2026-09 第十五轮：知乎 OAuth 接通 / HUD 四行拉锯 / 行走表重做 / 垃圾桶

**① 知乎 OAuth 登录（已上线，等用户登记回调）**
    · 服务端：新写 `liukanshan-career/server/zhihu-oauth.mjs`（**零依赖**，Node 原生 fetch ✔）
      - `/刘看山/api/zhihu/status|login|callback|logout` ✔
      - 另开**英文别名** `/lks-zhihu/*`（有些平台的回调字段不吃中文 ✗）
    · 部署：systemd 服务 `liukanshan-zhihu`（Restart=always ✔、开机自启 ✔），听 127.0.0.1:5310
    · 密钥：`/etc/liukanshan-zhihu.env`（**600 权限** ✔，不进仓库/代码包 ✗）
    · nginx：`/刘看山/api/zhihu/` 与 `/lks-zhihu/` 两条转发 → 5310 ✔
    · 前端：`src/ui/知乎账号.tsx`（资料卡 + 设置两处 ✗）；登录后显示「刘看山（真实用户名）」+ 头像 + 签名
      ⚠️ 接口不通时**整块不渲染** ✗（保住「离线也能把故事走完」这条铁律 ✔）
    · **待用户做**：去开放平台把回调登记成 `https://zjhxbb.xyz/lks-zhihu/callback` ✔
      （登记什么、服务端就得发什么 ✗ —— 两边必须一模一样 ✔）

**② AI 队友的代理位（`/api/npc` 401）**
    · 根因：`llm.ts` 里写的是根路径 `/api/npc` ✗ → 被 nginx 兜底交给主站 → 401 ✔（见坑 80）
    · 现在：改成 `/刘看山/api/npc` + nginx 给 **204** ✔（将来接直答：把那行换成 proxy_pass ✔）
    · 代码里的模型档位是**知乎直答**（zhida-fast-1p5 / thinking / agent ✔）

**③ HUD「四行」拉锯了四轮（值得复盘）**
    用户先后要过：只排一排 → 四行各占 1 → 删掉两块 → 又都要 → 太挤 → 宽松版 ✔
    我每次都**追加一段 CSS** ✗，前后规则互相打架，量了四轮都没修好挤出（见坑 76）✔
    最终形态：`.lv-top` 用 **grid 四行**（auto 行高 + 贴顶 ✔）、
    中间容器 `display: contents` 摊平 ✔、文字 `nowrap+ellipsis` ✔、
    **PC 与移动端同一套** ✔（移动端同比例缩放，比例不合就留白 ✔）

**④ 小鹿行走表重做（素材是好的，是我的流水线坏了）**
    见坑 71/72/73/75 ✔。新工具 `liukanshan-career/tools/切行走表.mjs`：
    四边泛洪抠白 ✔ → 逐格取边界 → 最近邻缩到 32×48 → 贴底居中 → 第 4 行用 drawImage 镜像 ✔

**⑤ 新增功能：把稿子扔进垃圾桶丢弃**
    垃圾桶是**装饰不是工位** ✗，所以单独记它的坐标 ✔；判定放在「放地上」之前 ✔；
    直接销毁不落地 ✔、**不扣分**（扣分只归工单超时 ✔）

**⑥ 空格触发不了主线（按 Tab 进微信才触发）**
    React 那侧读的是 store 里的 `附近交互点` ✗ —— 那是场景**异步推**过去的 ✔，
    重挂载/重置后可能是空的 → 空格没目标 ✔；开一次微信会让地图屏重新同步 → 之后又灵 ✔
    → 场景新增 `当前交互点()` 作为**权威来源** ✗，空格改成「store 读不到就问场景」✔

**⑦ 真部署与假部署**
    部署脚本现在固定三步：**构建失败不打包** ✔ → **上传重试** ✔ → **核对服务器文件哈希/时间戳** ✔

#### 2026-09 第十三/十四轮：动画素材 / 剧情死锁 / 加载失败 / HUD 固定尺寸

**① 小鹿行走动画（来回折腾了四轮 ✗）**
    · 一开始是**素材**里左/右两行有「头转向反方向」的坏帧 ✗
    · 我先去查朝向逻辑（**猜错了** ✗），再查帧去重（真凶之一 ✗），
      最后按用户要求把朝左/朝右**砍成只播前两帧** ✗ —— 等用户重新出图修好素材后，
      这个补丁反而变成「动画缺帧、一顿一顿」✔
    · **教训**：坏帧要**重新出图**解决 ✗，运行时砍帧只会制造新问题 ✔
      （最终：四方向都播完整 4 帧 ✔）

**② 触发彩蛋会丢主线**（用户报的）
    支线片段原来直接 `队列: [...片段]` —— **把主线队列整个覆盖** ✗，演完回不去 ✔
    → 新增 `支线前` 存主线现场（队列/位置/正在看的会话 ✔），支线演完原样恢复 ✔

**③ 主线死锁**（用户报「进小游戏前面的剧情无法进展」）
    见坑 67：空队列 + 地图目标被误判成「全剧播完」✔ → 刷新后主线不动 ✔

**④ 茶水间回复主管改成手动**
    原来是刘看山**自动**回一句「好，四点前发您。」✗ → 改成 `等玩家发言` ✔

**⑤ React 重复 key**（`Encountered two children with the same key`）
    `下个id()` 光有「计数器 + 随机种子」不够 ✗：恢复存档/HMR/重置 都会让它倒退 ✔
    → 加**已用 id 集合**，发之前先查、用过就往后跳 ✔（见坑 70 那条思路：**从源头保证唯一** ✗）

**⑥ `Failed to process file`（坐姿素材）** → 见坑 65

**⑦ 道具物理碰撞**：工位家具原来**没有物理体** ✗（能穿过去 ✔）
    → 全部补上 ✔，并且**偏移算法和地图统一**（`body.position.set(x-宽/2, y-高)` + `updateCenter` ✔；
      我第一版用 `setOffset` 算错了位置 ✗，实测照样能穿 ✔）

**⑧ HUD 固定尺寸** → 见坑 69；**⑨ 设置面板溢出** → `max-height: 88vh` + 可滚 ✔

#### 2026-09 第十二轮：过场动画 / 知乎真卡 / 重来三层 bug / 移动端横板

这一轮跨度很大，按"用户报什么 → 根因 → 怎么修"记。

**① 过场动画机制（文档 #0/#1/#3）**

新增 `过场` 节拍 + 全局全屏过场层（`src/screens/过场.tsx`，z-index 400，**能盖住地图** ✔）。
时长走完自动继续，玩家不用点。三处接入：

| 接入点 | 做法 |
|---|---|
| **每次任务之间** | 挂在 `地图交互` 的「主线入口」那条路上 —— 一个点管所有段，不用往每段剧本手写 |
| 文档 #3「不要害怕，他又不吃人」之后 | 在剧本里直接插一条 `过场` |
| 文档 #0 第一次坐椅子 | ⚠️ **故意没做成"坐下就过场"** —— 见下面坑 44 |

**② 文档 #7：周岚撤回前要有输入框**

原剧本里那句 `系统: '周岚撤回了一条消息'` **没有可撤回的对象** ✗（玩家没发过消息）。
新增两种节拍：`等玩家发言`（停下来出真输入框）+ `撤回`（把**我方最后一条**标成已撤回，
界面渲染成「周岚撤回了一条消息」）。`待玩家发言` **要进存档**（和 `待选择` 同类 ✗）。

**③ 文档 #8：交互后直接开微信到对应会话**

加 `跟随活跃会话` 开关：**刚进这一段时**第一次 `切会话` 把视图也带过去，用完关掉。
段内后续的切会话**仍然不自动跳**（那是更早明确要求过的"自己点击切换"）。

**④ 文档 #5 / #9**：茶水间标题 (3)→(4)、群头改成**就是成员数**（原来多加了 2 ✗）；
删掉无效时间 14:30；林总「先做吧」→「可以」。

**⑤ 咖啡机物理碰撞**

`buildDecor()` 原来只 `add.image`，**一个物理体都没有** ✗ → 咖啡机/饮水机/垃圾桶全能穿过去。
给 greybox 加了一张 `装饰占地`（只登记"实体家具"，绿植/白板故意不挡 ✔）。

**⑥ AI 队友（卡住 / 装样子）**

先确认"是不是装样子"：**不是** —— 他的活算数（`完成订单` 里 `delivered += 1`、能直接结算）。
真正的问题是两个：**看不出来**（站两秒多一动不动）+ **被玩家堵住**。
· 加 `空接近格()`：主站位被玩家占了就**就近换一格能走的**
· 看门狗判据从"**这一帧动没动**"改成"**有没有比之前更靠近目标**"（贴着墙抖也算动 ✗）
· 头顶加**干活进度条**（只 `working` 时显示）

**⑦ 沙漏动画（改了三轮才对）**

用户先后说：「每变一帧就反转一下」→「一直在摆头」→「上下对调不是左右滑动」。
最终形态：
· 精灵表换成**竖排** `hourglass_v.png`（64×640 = 10 帧上下叠）→ 走帧是 `background-position` **纵向**移动 ✔
· 翻面 `沙漏变面`：**一个循环翻一次**（周期 = 走帧 × 2），`scaleY(-1)` **只在沙漏图上**（卡片和文字不翻 ✔）
· 关卡筹码 32px、过场 64px **共用同一套关键帧**，只用 CSS 变量 `--总高` 区分尺寸

**⑧ 知乎卡：接真实检索 + 可复现烘焙**

写了 `工具/查知乎卡.mjs`（走知乎 CLI，默认返回完整 JSON ✔）。8 张卡全部换成真检索结果，
点赞数从 `0/20/83/145/1/68` 提到 **231 / 79 / 145 / 145 / 59 / 71 / 83 / 1285**。
工具里沉淀了四条规矩，见坑 47。

**⑨ 移动端三件 + 横板**

· 「开始干活」在手机上点不动 → 绑 `onPointerDown`（和"摇杆+空格"同类 ✗）
· 倒计时左侧沙漏不显示 → `.lv-touch .lv-hour-img` 里还写着**横排**的 `260px 26px` ✗
· 设置按钮里能看**游戏介绍与规则** → 复用同一张开场说明（规则只有一个来源 ✔）
· **移动端小游戏 = PC 横屏样式**：先试系统锁横屏；**锁不上（iPhone/微信）就把整个关卡 CSS 转 90°**
  （实测 `transform: matrix(0,1,-1,0,390,0)` ✔ 命中测试跟着转 ✔）

**⑩ 「重新开始」三层 bug（用户："重来有好多 bug"）**

排查发现"清缓存"本身是干净的 ✗（`lks-save` / `lks-save-level` / 段号 / 位置 / 屏 / 队列 / 会话 / 卡牌都对 ✔），
**问题全在内存里**，而且是**三层叠加**：

1. `重置()` 只写了 `{ cards: [STARTER_CARD] }` ✗ → 阶段/工单/交稿数/打印槽/帮手**全留在上一局**
   → 重来后再进小游戏**工单 0 张**、根本没法玩 ✔ → 改成调 `setReady()`（它本来就是"清成待开始"）
2. `bus.on(CMD.start)` 被关在 `if (!keyboard) return` **后面** ✗ → 没键盘就不挂监听
   → 「开始干活」（走 `bus.emit(CMD.start)`）毫无反应 ✗
3. **（决定性）** 旧场景的监听没摘干净 ✗ → 它在**已销毁的场景**上跑 `beginRun()` **抛异常** ✗
   → `Set.forEach` **遇异常中断后续** ✗ → **新场景那个正确的监听永远轮不到执行** ✔
   → 给 `bus.emit` 加**逐处理器 try/catch 隔离** ✔（坏的自坏，好的照跑；出错打日志不吞 ✗）

实测（同一支探针）：重来后再进小游戏 **简报消失 ✔ 工单 3 张 ✔**（原来是简报不消失 + 0 张 ✗）。

#### 2026-09 第十一轮：移动端"上下走却播左右动画" + 摇杆卡顿

用户报：「移动端的走路动画和 pc 端不一样，移动端向上下走只显示左右走动的动画，并且感觉摇杆和其他点击按钮很卡」。

**① 朝向：`左右优先` 遇上模拟摇杆 = 必翻**

地图里的判朝向原来是这样写的（`OfficeMapScene.走()`）：

```ts
if (vx < 0) this.朝向 = 2;        // 左
else if (vx > 0) this.朝向 = 3;   // 右
else if (vy < 0) this.朝向 = 1;   // 上
else if (vy > 0) this.朝向 = 0;   // 下
```

⚠️ **键盘下 `vx` 恰好是 0**（只有 -1/0/1），所以这条"左右优先"一直没暴露；
**摇杆是模拟量** —— 实测"直着往上推、拇指横偏 **12%**"就被判成**朝右走路**。

改成**主轴优先 + 1.25 倍偏置**（两个场景用同一条规则，避免"一边对一边错"）：

```ts
const 横 = Math.abs(vx), 纵 = Math.abs(vy);
if (横 > 纵 * 1.25) this.朝向 = vx < 0 ? 2 : 3;
else if (纵 > 横 * 1.25) this.朝向 = vy < 0 ? 1 : 0;
// 两者接近（斜着推）→ 保持上一次朝向，不左右横跳
```

实测：横偏 0 / 0.12 / 0.3 三种推法，**关卡和地图现在都判成"上"** ✅
（改之前：地图在 0.12 就翻成"右"）

**② 卡顿：小地图每帧重画 1350 个格子**

`画地图()` 每帧把 45×30 = **1350 格** + 40 多件家具 + 6 个头像全部重画一遍 ——
而这些**99% 是永远不动的**。手机上实测最差帧 **48.6ms**（≈20fps 的卡顿感）。

改成**静态层缓存**：地面/建筑/家具/交互点/同事头像画进一张离屏 canvas，**只画一次**；
每帧只 `drawImage` 一次 + 画主角那一小块。
（素材异步加载的情况用 `静态待重画` 标记，下一帧自动补画，所以不需要原来的 400ms 定时器 —— 一并删了。）

同一时间窗口（1.2 秒）里的帧数：

| | 改之前 | 改之后 |
|---|---|---|
| 地图·静止 | 143 帧 / 中位 12.1ms | **295 帧 / 中位 6.1ms** |
| 地图·推摇杆 | 166 帧 / 中位 12.2ms / 最差 **48.6ms** | **374 帧 / 中位 6.1ms / 最差 24.3ms** |
| 关卡·推摇杆 | 最差 24.3ms | **最差 6.2ms** |

**③ 摇杆头不许走 React state**

原来每次 `pointermove`（每秒 60+ 次）都 `set钮(...)` —— 触发的是**整个屏幕**的重渲染
（地图屏那棵树里有小地图 canvas、卡片、按钮）。现在摇杆头用 **ref 直接改 `style.transform`**，
一次渲染都不产生；顺手加了 **0.18 的死区**（拇指搭着不动时角色不会漂）。

**④ 回归断言**（`事件四验收` 竖版那段）：

```
✓ 小游戏里"带 12% 横偏往上推"仍然是朝上（实际 up）
✓ 地图上"带 12% 横偏往上推"也是朝上（实际 上）
```

⚠️ 教训：**判方向这类逻辑，键盘是"离散量"、摇杆是"模拟量"，用键盘测过的写法在摇杆上不一定成立**。
凡是"优先级式"的判断（`if (横) ... else if (纵)`）在模拟输入下都要问一句"横只是小抖动会怎样"。

#### 2026-09 第十轮：移动端两个真问题（底部拥挤 / 侧栏在手机上全没了）

用户截了线上手机端的实拍图，两个问题：

**① 地图屏底部挤成一团**

⚠️ 根因是我上一轮"给摇杆让位"时**把道具卡推到了底部**（`.map-touch .map-prop { bottom: 132px }`），
结果道具卡压着「交互」键、提示条（bottom 96）也压着，微信在 168、齿轮在 250 —— 下半屏全是东西。

重排之后（**手机上半屏放信息，下半屏只放操作**）：

| 东西 | 原来 | 现在 |
|---|---|---|
| 道具卡 / 人物卡 | bottom 118~132（压操作区）| **挪到屏幕上方**（天数徽章下面 `top: 46px`），最多两行、说明超两行截断 |
| 提示条 | bottom 96（压「交互」）| bottom 16，**塞进摇杆和右下三键中间那条空档**，不换行 |
| 右下三键 | 交互 74 / 微信 168 / 齿轮 250（间距不匀）| **等距一条竖列**：交互 18 → 微信 104 → 齿轮 178 |
| 摇杆 | 116px | 100px（窄屏上太占地方）|

实测：道具卡 `top=46`、**与摇杆/交互/微信零重叠**；四个操作件两两不重叠。

**② 手机上侧栏（我的资料 / 通讯录 / 设置 / 更多）全没了，而且"回不去地图"**

⚠️⚠️ 两个叠在一起的洞：
- `screens.css` 的窄屏规则里 **`.wc-rail { display: none }`** —— 整个侧栏在手机上被藏了，
  于是**四个面板入口在手机上根本不存在**；
- `回地图` **只绑在 Tab 键上**（`Tab 收起手机回地图`）—— 手机上点「微信」进了聊天之后
  **就再也回不去地图了**（手机没有 Tab 键）。这条比"挤"严重得多：**会卡死**。

补法（微信自己的排法）：手机上加一条**底部功能栏** `.wc-tabbar`
（`screens/screens.css` 里只在 `max-width: 720px` 显示，桌面端保持原来的左侧竖栏）：
**头像 / 会话 / 通讯录 / 设置 / 更多 / 回地图**，六个等宽 tab，「回地图」用暖色区分（它是"退出去"，不是面板切换）。

实测（线上）：点「回地图」→ 屏幕切回地图、摇杆和交互键出现、底栏消失 ✅；
点「通讯录」→ 面板切到通讯录且有内容 ✅。

#### 2026-09 第九轮：部署上线（https://zjhxbb.xyz/刘看山/）

**已部署**：服务器 `<服务器IP>`（阿里云 ECS），子路径 `/刘看山/`，和同机的「点废成金」同一套路。
静态文件在 `/srv/liukanshan/刘看山/`，nginx 片段在 `/etc/nginx/liukanshan-locations.conf`
（include 在 `agrimind-common.conf` 第 79 行，紧跟 dianfei 那行）。

📖 **完整手册见 `工具/部署说明.md`**（含重新部署命令、回归清单、安全提醒）。
配套脚本：`工具/远程.mjs`（远程执行/上传）、`工具/部署-1/2/3.sh`、
`工具/nginx/liukanshan-locations.conf`、`liukanshan-career/tools/部署验收.mjs`（线上浏览器验收）。

⚠️ **这次为部署改了两处代码**（都影响本地，已验证不影响 dev）：

| 改动 | 为什么 |
|---|---|
| `vite.config.ts` 加 `base: './'` | 原来默认 `'/'`（写死根目录），部署到子路径后 `/assets/x.js` 全 404，**页面 200 但白屏** |
| `screens.css` / `panels.css` 里 3 处 `url('/assets/avg/…')` 改成相对路径（图移进 `src/assets/`）| 同上。这类"写死的绝对路径"**最阴险：不报错，只是背景没了** |

⚠️ **中文路径的处理**（用户特别提的"特殊处理"）：全程**不传中文** ——
目录名用 `printf '\xe5\x88\x98\xe7\x9c\x8b\xe5\xb1\xb1'` 由 UTF-8 字节构造，
落盘后用 `od -An -tx1` 核对；nginx 片段单独上传（ASCII 文件名）。
nginx 那边的坑是反过来的：**location 匹配的是百分号解码后的 URI，必须写中文原形**。

⚠️ **bash 不允许中文变量名**（`名=...` 会被当成命令报 `command not found`）——
远程脚本里的变量名全用 ASCII（这条第一次跑就踩了）。

#### 2026-09 第八轮：顶部新消息提醒 + 浏览器存档

**① 顶部"新消息来自哪里"的提醒**（用户要求：「移动端如果有新消息要在顶部弹出消息给出提示
要不然看不清哪里来的消息」+「改为和微信一样**不在本聊天框才弹**」）

⚠️ 根因：**手机上聊天界面打开时会话列表是隐藏的**
（`screens.css` 的 `.wechat.show-chat .wc-list { display: none }`）——
玩家只看得到一个会话，别处来了消息只会悄悄躺在那个会话里，页面上唯一的变化是**一个看不见的小红点**。
在地图/小游戏上更彻底：什么都没有。

弹出规则**和微信一致**（三个条件同时成立才算"正在看这个聊天框"，那样才不弹）：

| 条件 | 为什么 |
|---|---|
| 在微信屏上（`屏幕 === 'avg'`）| 在地图/小游戏上根本看不见消息 → 必须弹 |
| 看的就是这个会话（`查看会话 === 活跃会话`）| 在别的聊天框里 → 弹 |
| **手机上是"聊天"面板**（`移动端面板 === '聊天'`）| ⚠️ 手机上停在**列表**也算"不在聊天框里"，微信在列表页**是会弹的**。这条只在窄屏成立：PC 上列表和聊天并排显示，看着这个会话就是看得见 |

- 组件在 `screens/顶部提醒.tsx`，**挂在 `Root` 的最外层**（最需要它的场景恰恰是"玩家不在微信屏上"）。
- 从顶部滑下来、4.5 秒自己收；**点一下直接跳过去**（切回微信 + 切到那个会话 + 红点清零）。
- 只给 `消息/贴纸/邀请/知乎卡` 弹；`指标` 不弹（那是系统结算行，不是"有人找你说话"）。

**② 进度存进浏览器**（用户要求：「将游戏进度缓存到浏览器缓存里，使其不删缓存就能保存进度」）

`state/存档.ts`：用 **localStorage**（不是 sessionStorage —— 关掉浏览器还在，
只有"清空站点数据/缓存"才会丢）。

| 存 | 不存 |
|---|---|
| `段号` + `位置`（下一拍）、`会话们`（聊天记录，这是主体）、`指标`、`卡牌`、在看哪个会话 | `队列`（能从段号用剧本重算，存了反而容易对不上）、各种"等玩家操作"的临时标记、小游戏那一局的中间状态 |

- ⚠️ **不存"等待"标记是对的**：它们本来就写在第 `位置` 那一拍上，**重播那一拍会自己重新设上**；
  存下来反而可能读到一个"半等待"的怪状态。
- ⚠️ `存档版本`：剧本结构或字段一变就 **+1**，旧存档**整份丢弃** ——
  宁可让玩家重来一段，也不要读出一个对不上的进度（读半个状态的 bug 最难查）。
- 自动存档：**订阅剧情 store，只认"真进度"变化（段号/位置/会话记录/指标），防抖 700ms**。
  不筛的话随便开个面板都会写一次 localStorage（同步 IO，写太勤会卡）。
- 有存档时 `初始()` 直接把 `屏幕` 给 `'avg'` → **跳过开场动画，一打开就在上次那条消息上**。
- 设置面板加了「存档」一节（显示上次保存时间）+「重来」按钮**同时清存档**（不清的话刷新又回来了）。
- ⚠️⚠️ **多标签页会互相覆盖**（整站共用一份 `lks-save`，后写的赢）。
  当前取舍是"不做多标签协调"，但要知道有这回事 —— 测试里就踩到了：
  另一个页面还在跑剧情时钟，我这边刚存好的进度转眼被它覆盖。

**②b 小游戏那一局也存**（用户追加："可以"）

一局有几分钟，手机上切个后台刷个新就白打 —— 所以单独一个键 `lks-save-level`
（**和剧情存档各管各的版本**，两条进度线的生命周期不一样）。

| 存 | 不存 |
|---|---|
| 剩余时间 · 已交/交错 · 当前三张工单 · 手里那张（含已做工序）· 地上散落的（含工序）· 玩家位置朝向 · 打印机里那一份 | **帮手的状态**（他的计划/目标/工位进度是一串内部状态：恢复成本高、收益低。回来时他从"待命"重新开始，这一局照样能打）|

- 自动存档：**每 3 秒**一次（localStorage 是同步 IO，不能每帧写）；**开局立刻存一次**
  （"刚开始就刷新"也能接着打）。
- 恢复时**不走开场简报**（`beginPlay` 直接进"进行中"），并 toast 一句"接着上次那一局继续"。
- ⚠️ 清存档的**两个入口都要清**：场景的 `endRun`（时间到/交满）**和**故事的 `关卡结束()`
  ——「跳过这一关」走的是后者、**不经过 endRun**，只清一处的话刷新会跳回一个已经被跳过的局。
- ⚠️ 有"这一局没打完"的存档时，`初始()` 把屏幕直接给 `'level'`
  （剧情位置还停在"开小游戏"之后那一拍，打完照常接上 —— 两条线互不干扰）。

**③ 测它**：

```
✓ 新消息在顶部弹出：「林总：欢迎你，刘看山。」
✓ 提醒条画在屏幕顶部、没溢出
✓ 点一下就跳到了那个会话
✓ 正在**聊天框里**看那个会话时不弹
✓ 手机上停在**会话列表**时照样弹
✓ 进度写进了浏览器存档、而且追上了当前（段 3 · 第 52 拍 · 48 条聊天）
✓ 刷新之后接着上次继续（段 3 · 第 52 拍）
✓ 删掉缓存就从头开始（屏=opening 段=0 第 0 拍）
✓ 设置里的「重来」把存档也清掉了
✓ 小游戏那一局也存下来了（已交 3 · 剩 121.1s）
✓ 刷新后直接回到小游戏（屏=level）
✓ 这一局的进度还在（已交 3 · 剩 119s）
✓ 直接接着打，没有再走一次开场简报
✓ 这一局结束后，关卡存档被清掉了（剧情存档留着）
```

⚠️ 这一段的测试**开头必须把两个存档都清掉**：前面的阶段进过关卡，
`lks-save-level` 还留着 —— 不清的话"刷新应该回到微信"会变成"回到小游戏"（第一版就是这么误判的）。

#### 2026-09 第七轮：小地图自适应 / BGM + 音效

**① 移动端小地图做小（PC 不变）**

⚠️ 小地图的"一格几像素"**原来写死 6**（45×30 格 = 270×180），而 CSS 注释里还写着"一格 3px"（过时了）。
后果：390px 宽的手机上它占了 **69% 的屏幕宽**，等于把地图糊在脸上。

现在按屏宽自适应（**必须是整数**，像素画非整数缩放会糊格线）：

| 屏宽 | 一格 | 小地图 | 占屏宽 |
|---|---|---|---|
| < 430（手机）| 3px | 139×94 | **36%** |
| < 760（平板）| 4px | 184×124 | — |
| ≥ 760（PC）| 6px | 274×184 | 19%（**不变**）|

- 放大版也按屏宽收（`14px/格 = 630px` 在手机上会溢出）。
- ⚠️ 同事头像的尺寸下限原来是写死的 8px —— 缩到 3px/格之后，8px 的头会占掉将近 3 格，
  比例完全不对。现在按格像素本身缩放（下限 4px）。

**② BGM：三首，程序合成（和音效同一套合成器）**

| 曲目 | 用在哪 | 风格 |
|---|---|---|
| `bgm_办公室` | 地图 / 剧情 / 开场 | 温暖的办公室日常（C–Am–F–G，92 BPM，21 秒）|
| `bgm_工作关` | 小游戏 | 赶稿子的轻快循环（138 BPM，**走路低音 + 跳音旋律 + 打点**）|
| `bgm_会议室` | 跑团 / 会议 | 慢速克制的悬疑感（Am–F–Dm–E，80 BPM，24 秒）|

- ⚠️ 合成器新加了 **`持续音()`**（起音/保持/释放）——原来那个 `音()` 是 `exp(-4t)` 的
  **打击式衰减**，拿它铺和弦会变成"咚、咚、咚"，是音效堆叠不是音乐。
- ⚠️ BGM 用 **22050Hz**（8-bit 听不出差别，体积省一半）：3 首共 2.5MB。
- ⚠️ **BGM 跟着屏幕走，而且只在一处决定**（`screens/Root.tsx`）：
  屏幕是那里分流的，BGM 也在那里换；分散到各屏的话"切屏时谁负责停下上一首"会变成乱账。
  `播BGM` 内部判了"同一首不重播"，所以可以放心跟着渲染调。
- 静音开关**同时管 BGM 和音效**（关的时候一起停，不然玩家会以为开关坏了）。

**③ 音效：补 9 个工作关专用 + 修 4 个不合格的 + 删 1 个**

⚠️ 工作关**原来一个音效都没有**（`OfficeScene` 里搜不到一处 `播放()`）——
   借的全是 AVG 那套通用点击音。现在按"办公室里真实听得见的声音"补了九个：

```
干活（落笔/盖章）· 工序完成（咔哒+上行）· 放进打印机（进纸闷响）
打印完成（两声叮+纸张）· 拿放稿子（纸张摩擦）· 交稿成功（三音上行）
交稿退回（下行闷响）· 时间告急（钟摆，最后 30 秒每 5 秒一下）· 技能卡（上行琶音）
```

**修掉的 4 个**（都是量出来的，不是感觉）：

| 音效 | 原来 | 问题 | 现在 |
|---|---|---|---|
| `选项悬停` | 峰 **9%** / 0.06s | **等于没响**；而且手机没有 hover，触屏永远不播 → **删掉**（连带 23 处调用）| — |
| `转场` | 0.55s **下行扫频** | 剧情转场大多在"往前走"，下行听着像失败/降级；且被调 10 次，太长拖节奏 | 0.3s 中性"翻页" |
| `指标下降` | 峰 25% / 均 **3.4%** | 扣分比加分（7.7%）**小声一半** —— 玩家会觉得"没扣到我头上" | 峰 38% / 均 5.5% |
| `开场标题` | 峰 **38%**（全场最炸）| 开场第一声炸一下很劝退 | 峰 31% |

⚠️ **一个教训**：我一开始用 `rg "播放\('消息收到'"` 数引用，结论是"5 个音效从没响过"。
   实际代码是 `播放(条.我方 ? '消息发出' : '消息收到')` —— **写死字符串的 grep 会漏掉三元/变量形态**，
   差点让我删掉 4 个其实正常的音效。**数引用要搜名字，不要搜调用形态。**

**④ 测它**：

```
✓ 20 个音效 + 3 首 BGM 都取得到（共 23 个文件）
✓ 小游戏里放的是「工作关」的 BGM（实际 工作关）
✓ 地图上放的是「办公室」的 BGM（实际 办公室）
```

#### 2026-09 第六轮：移动端操作（**原来手机上一步都走不动**）

用户问「现在移动端怎么移动和操作？」—— 查下来的**实际状态**是：

| 场景 | 移动 | 操作 |
|---|---|---|
| 工作关（小游戏）| ❌ 完全没有触摸支持（`tickPlayer()` 只读键盘）| ❌ 只有 E/空格/Q/1-2-3 |
| 地图 | ❌ 没有触摸移动（只有点同事弹卡）| 空格交互（键盘）|

**① 一套输入，两个入口**（`src/game/touch.ts` + `src/screens/摇杆.tsx`）

```
DOM（React）                     Phaser（场景）
虚拟摇杆 / 动作键  ──写──▶  虚拟输入 { x, y, 动作[] }  ──每帧读──▶  和键盘走同一个入口
```

- ⚠️ **关键设计**：摇杆**不自己实现移动**，只写模块级对象；场景每帧读，
  和键盘**共用** `tickPlayer` / `干活` / `interact` / `丢下`。
  两套各写一份的话，键盘 8 向、摇杆要斜走，逻辑会越走越远（而且迟早一边有 bug 一边没有）。
- ⚠️ 模块级**可变对象**而不是 React state：每帧 setState 会炸性能。
- 动作是"读一次就清空"的队列（`取动作()`），不会重复触发。
- `是触屏()`：粗指针 **或** URL 加 `?触屏=1`（后者是**给电脑上核对布局用的**，实测很好使）。

**② 小游戏**：左下摇杆 + 右下 **E 干活 / 空格 交互 / Q 丢下 / ⚙ 设置**（E 最大最绿，在拇指最舒服处）。
操作说明也**跟着输入方式换**（触屏上写"方向键/WASD"只会让人去找键盘）。

**③ 地图**：左下摇杆 + 右下「交互」键，**外加点地面自动寻路走过去**：

- 直接复用场景里已有的 A*（`找路()`）—— 那条路和"玩家真能走的路"是同一套判据，
  所以不会"自动走卡在墙角"。键盘/摇杆一动就放弃自动走（`走()` 里清 `自动路`）。
- ⚠️ 「交互」键和键盘空格**必须走同一个函数**（`按下交互`）：
  坐下 / 门 / 交互点 / 站起来那套优先级，两处各写一份迟早不一样
  （空格那个老 bug"被坐座位抢先、主线触发不了"就是这么来的）。
- ⚠️ 触屏上**键盘提示条要收起来**（`.map-touch .map-keys{display:none}`），
  并把微信/设置往上抬 —— 实测摇杆直接压在提示条上、交互键和微信挤成一团。

**④ 测它**（`事件四验收` 竖版那一段，4 条断言）：

```
✓ 手机上出现了摇杆 + 动作键（⚙ / Q丢下 / 空格交互 / E干活）
✓ 推摇杆人真的走了（挪了 80px）—— 手机上终于是"能玩"的
✓ 点触屏的「E 干活」按钮也在加进度（25%）
✓ 地图上点一下地，人就自己走过去（挪了 114px）
```

⚠️ 用**真鼠标事件拖摇杆**来测：pointer 事件是触屏和鼠标**共用**的那一层，
   所以桌面就能验完整条链路（`?触屏=1` 打开 UI）。真机仍建议手动过一遍。

#### 2026-09 第五轮：四台机器散开成一圈 / 打印机改成"放进去等出来"

用户要求：「将需要的**四个工具机器不要放在一排上**，尽量复杂不在一块」，
以及「打印机是**直接可以把文章放进去等文章出来**就行了，**每次只能放一个**」。

**① 机器散成一圈动线**（原来是 4 台全在第 1 行，从左到右一条直线）：

```
######################
#.D........=...=...D.#   ← 文件柜(左上) · 我的工位(右上)
#..........=...=.....#
#......D.............#   ← 资料库(工位区左下)
#.~~~~~~~~~~~~~~~~~~.#
#.~~~~~~~~~~~~~~~~~~.#
#......=.======......#   ← 中央玻璃壁龛
#......=......=......#
#.==...............D.#   ← 文印区(打印机 · 右下)
#.=.=.......D........#   ← 交稿箱(下中)
######################
```

一圈动线：**文件柜 → 资料库 → 我的工位 → 打印机 → 交稿箱**，横跨整间办公室。

- ⚠️ **加了 `安放()` 守卫**：每台设备先给一个"首选格"，再从首选格往外**螺旋找第一个
  能走且没被占的格子**。地图上现在有隔断/壁龛/储物间/桌岛，硬写坐标迟早会把某台机器
  塞进墙里或塞进小房间的死角。交稿箱额外排除**储物间/壁龛**那两间小屋（交稿不该跑死角）。
- ⚠️ 原来的"工位区隔断"是**夹在相邻工位正中间**算出来的 —— 机器散开之后这个规则失效了，
  改成**三片固定位置的独立隔间墙**（33% / 50% / 67%，只占行 1-2，自动避开资料库那一列）。
- 竖版（12×21）和横版（22×11）都验过：**5 个工位全可达**（横版 162/163 格、竖版 170/171）。

**② 打印机：从"站着按住等"改成"**一个槽**"**

| | 以前 | 现在 |
|---|---|---|
| 怎么用 | 站在旁边按 E，**人不能走开** | 按 E **放进去就完事**，人可以走开干别的 |
| 走开会怎样 | **中断，进度清零** | 照跑（`tickPrinter()` 和玩家位置无关）|
| 出来以后 | 直接盖在手上的稿子上 | 从**出纸口吐出一件地面物**，过去按空格拿 |
| 一次几份 | — | **只能一份**：槽里有 / 出纸口那份没拿走，都不让再放 |

- ⚠️ 放进去时**把已经做过的工序一起存进槽里**（`打印槽.已有`），出纸时原样带回 ——
  否则"打印一下进度全没了"。测试实测出纸那份带着 `查资料、写稿、校对` 三道 ✔。
- ⚠️ 倒计时**必须挂到顶部任务条**（`store.打印`）：玩家既然能走开，
  信息就不能只画在打印机旁边。站位提示里也写了"**你可以先干别的**"。
- 出纸口位置 = `approachOf('print')`（打印机旁边能站的那一格），保证拿得到。
- 顺带把地面物的生成抽成 `造地面物()`，**丢下 / 打印机出纸** 共用一条路
  （免得两边的"图标跟着落地""超过上限清最旧的"逻辑各写一份、迟早走岔）。

#### 2026-09 第四轮：手感（走路 / 拿东西）+ 操作（E 键 / 打印机）+ 技能卡

**① 助手走路"一卡一卡" —— 量出来是素材问题，但可以在代码里救**

把走表每一行**4 帧之间的像素差**打出来（`node tools/查深度.mjs` 同类思路，写了个一次性脚本量）：

| 朝向 | 相邻帧像素差 | 结论 |
|---|---|---|
| 下（正面）| 11 / **33** / **32** / 12 | **A A B A** —— 同一姿势连着两帧，播起来就是"走一下卡一下" |
| 上（背面）| 7 / 11 / 13 / 9 | 几乎没动（背面走路等于原地踏步）|
| 左 / 右 | 15 / 27 / 26 / 22 | ✅ 正常 |

即 `OfficeScene.制作同事动画` 现在会**自动去掉"近似重复帧"**（`取不同帧()`）：
把整张走表画进 canvas 读像素、算行内两两帧差，阈值取"这一行最大帧差的 45%"，
只播真正不同的姿势。实测结果（每行剩几帧）：

```
阿麦 down:2 up:4 left:4 right:4      小鹿 down:2 up:2 left:3 right:3
韩策 down:3 up:4 left:2 right:2      周岚 down:3 up:3 left:3 right:3
```

⚠️ 阈值为什么是"相对这一行的最大差"：`上`那种整行几乎不动的表（最大差只有 13），
   用绝对阈值会被判成"全是重复帧"→ 只剩一帧 → **反而一点动画都没有了**。
⚠️ 想彻底好，**下/上两行还是得重出图**（侧面两行是好的，可以直接参照）。
   这个去重是"在现有素材上能做到的最好"，不是替代品。

**② 助手拿东西像"隔空取物" —— 是显示原点，不是偏移算错**

两个人用的是**同一个** `携带位置()`，但截图里主角把稿子抱在胸前、助手的稿子掉在脚边。
量出来一目了然：

```
玩家  origin (0.5, 0.5)  displayOrigin (16, 24)
助手  origin (0.5, 1  )  displayOrigin (16, 48)   ← ✘
```

根因是 `sortDepth()` 里那句**无条件**的 `this.npc.setDisplayOrigin(16, 48 + 浮动)`：
有行走图时浮动是 0，于是显示原点被设成 `(16,48)`，**"y = 精灵中心"变成了"y = 脚底"**。
后果两个：① 助手看起来凭空高了 24px；② 所有按"y 是中心"算的偏移整体下移 24px。
修法是只在**没有行走图的兜底路径**上动显示原点。

**③ 每台机器挂自己的图标 + 名字（不许重叠）**

- `StationLabel` 多了 `图标`（`public/assets` 下的路径），由 `OfficeScene` 的 `工位图标` 表给出：
  加工位用**和稿子上盖的同一枚**工序图标；取料用任务单、交付用对勾。
- 名牌变成 `inline-flex` 的小牌子：`[图标] 名字`，图标高 14px、像素锐利。
- 断言：5 台机器**都挂上图标且真加载出来**（`naturalWidth > 0`）+ **两两不重叠**（矩形相交检测）。

**④ 操作：E 键加进度 / 只有打印机吃时间（8 秒）**

按用户要求重做：`StationDef` 多了 `机制: '连按' | '等待'`。

| 工位 | 工序 | 机制 |
|---|---|---|
| 资料库 | 查资料 | **连按 E**（`连按几下` = 4 下），**不吃时间** |
| 我的工位 | 写稿 | 同上 |
| 文印区（打印机）| 校对 | **等待**：送进去后自己走 `打印秒数` = **8 秒**，人不能走开 |

- **空格**在工位上不再开工，只提醒"按 E"；空格仍然是领单/捡/交稿/放下。**Q** 丢下。
- `TIME_LIMIT` 180 → **240 秒**（地图变大 + 打印机变慢，不然是"跑不赢"而不是"玩不赢"）。
- 断言：连按 4 下做完查资料、**干等 1.5 秒进度不动**、打印机 1.6 秒只走到 ~20%、任务条在倒计时。

**⑤ 右下角沙漏 + 左下角技能卡**

- 沙漏用现成素材 `avg/hourglass.png`（640×64 = 10 帧），32px 一帧、`steps(10)` 走帧，
  时间紧张时翻得更快、边框变红。**这两个角上的浮层是唯一允许压在画布上的东西**
  （那片是墙根/空地），断言只管"它们不许爬到画布上 1/4"。
- **技能卡拿回来了**，而且是**关内可用**的：每交 `每几版发一张`(=2) 版发一张，最多 3 张，
  按 **1/2/3** 或点卡片用。每张对应剧情里学过的一课：

| 卡 | 类别 | 关内效果 |
|---|---|---|
| 不懂就问（起始就有）| 提问 | 当前这台机器**立刻做完** |
| 带着笔记去汇报 | 举证 | **+15 秒** |
| 先说我这边的时间 | 拒绝 | **交稿箱不再被暂停** |
| 坐在最前面 | 复盘 | **20 秒内帮手快 3 倍** |

⚠️ 关键设计：卡**不是外挂**——每一张都能对回剧情里已经学过的那一课（事件二"问清需求"、
   事件三"边界感/有据可依"、事件四复盘），所以"用卡"= "把学到的东西用出来"。

**⑥ Esc 开设置：从 Phaser 挪到 React**

⚠️ 用户报「按 esc 键没有出现设置页面」。根因：**Phaser 的键盘事件要画布拿到焦点**，
而玩家进关卡后未必点过画布。现在场景里**不再绑 ESC**（两边都响会"开了又立刻关"），
唯一入口是 `关卡.tsx` 里挂 `window` 的监听 → `场景.切换设置()`。
店里也补了 `开设置()` + `暂停前`：**简报阶段按 Esc 也能开设置**，继续时回到简报
（不会把还没开始的游戏直接推进"进行中"）。

#### 2026-09 第三轮：地图更有结构 / 帮手会寻路 / 拿东西的手感

用户要求：「小游戏的地图做得**复杂一点**来增加趣味性」，以及
「**拿文件的动画要优化，不是和现在一样直接贴在头上**」。

**① 地图从"空房间"变成有结构的办公室**（`greybox/level.ts` 的 `造关卡`）：

```
######################
#.D.=..D..=..D..=..D.#   ← 4 个工位，之间夹玻璃隔断（= 玻璃）
#...=.....=.....=....#
#....................#
#.~~~~~~~~~~~~~~~~~~.#   ← 横向走廊（材质不同，把地图分上下两半）
#.~~~~~~~~~~~~~~~~~~.#
#......=.======......#   ← 中央玻璃壁龛：顶墙留一个门
#......=......=......#   ← 壁龛里面（放咖啡机）
#.==.................#   ← 左下储物间（右边留口）
#.=.=...........D....#   ← 储物间里面 + 交稿箱
######################
```

- **竖向预算是算好的**：外墙上(1) + 工位行(1) + 工位通道(2) + 走廊(2) + 壁龛(2) + 储物间(2) + 外墙下(1) = **11 行**。
  所以 7 行的图**只会有工位隔断**，11 行起才结构齐全。
- 高图（手机竖版 21 行）上半区还会多两样：**带门的竖隔断** + **3×2 的办公桌岛**（从工位区下到走廊必须绕）。
- ⚠️⚠️ **整数倍不是"越大越好"**：选法是**优先 k=2**（1440×900 → **22×11 格**；手机 → 12×21）。
  原来一味取最大倍数，PC 上只有 15×7 —— **结构根本塞不进去**（实测只挤出 2 格玻璃，地图还是空房间）。
  上限锁在 26×15（再大就成了越野跑）。
- ⚠️ **加结构必须配可达性断言**。`tools/事件四验收.mjs` 的 `可达性检查()` 从玩家出生格 **BFS**，
  要求 5 个工位的相邻可走格全都到得了；**横版和竖版各验一次**（竖版只有 12 列，更紧）。
  实测：横版 19 格玻璃 · 可达 160/161；竖版 14 格玻璃 · 可达 175/176。

**② 帮手会寻路了**（`OfficeScene.找路` + `走向`）：

⚠️ **这是加结构逼出来的**：帮手原来只会 `physics.moveTo` 朝目标**直线走**，
   地图一加玻璃他就**直接撞墙卡住** —— 实测只加了 2 格玻璃，他就从 `toFiles` 掉进 `idle`（被看门狗救回来）。
   现在：目标格变了就 **BFS 出一条格子路径**（缓存，不每帧算），沿着"下一格中心"走，到了再换下一格。
   看门狗仍然保留 —— 那是最后的保险，不是第一道防线。

**③ 拿东西的手感**（`OfficeScene.携带位置`）：

⚠️ 原来一律画在 `(x, y-18)` —— 那正好是**头**的位置，而且深度写死 9500（永远压在最上面），
   所以看着像一张纸糊在脸上。现在按朝向**抱在身前**，并给正常深度（脚底 y+11）：

| 朝向 | 位置 | 缩放 |
|---|---|---|
| 正面（下）| 胸腹前 `(x, y+8)` | **0.75** |
| 左 / 右 | 朝向那一侧的腰前 `(x∓8, y+8)` | 0.75 |
| 背面（上）| **右肩外侧** `(x+10, y-13)`（不是顶在头上）| 0.55 |

- ⚠️ **必须缩小**：那件东西是 32×32、人是 32×48 —— 原尺寸**放不下**：抱在肚子高度会盖住下半张脸，
  抱低一点又掉到脚底下。0.75 正好卡在"胸口到胯之间"，头上留一整个脑袋、脚下还露着腿。
- ⚠️ 工序图标**盖在文件本身上**（像盖在纸上的章，缩到 0.62），不再飘到纸的上方 —— 飘上去正好压脸。
- 走路时再加 1px 上下浮动（`携带相位`），看着是"抱着走"。
- 调这几个数：`node tools/拍携带.mjs`（四张朝向特写 → `tools/shots/携带/`，和 `拍坐姿.mjs` 同类：
  **视觉手感只能拍出来看，光读代码判断不了**）。

⚠️⚠️ **窄屏上任务条最容易出的事**：三张工单卡 + 手上 + 帮手挤在 1/5 的高度里 ——
   第一版**纵向裁掉了第三张卡**，第二版改成横向滚又**把"手上这一版"推到屏幕外**。
   现在分两层（`.lv-cards` 一排横向滚 + `.lv-extra` 一行），窄屏上工单卡只留图标、客户名截断。
   **改这块必须跑 `事件四验收`**：它专门断言"工单卡/手上/帮手都在任务条里（不用滚就能看见）"。

#### 剧情怎么把这两块串起来的（`events3.ts`）

```
群里派活 → 三选一 → { 去房间: 会议室 }
  → { 跑团: 事件四跑团 }        ← 玩家点骰子，一轮一轮；收工后由 跑团收工() 把结果写进会话
  → 周岚私聊"把稿子做出来"
  → { 开小游戏: ... }          ← 切到 屏幕='level'；打完由 关卡结束() 把成绩写进会话、切回 avg
  → 交稿收束 + 知乎卡 ×2 → 暂停
```

⚠️ 两个新节拍（`types.ts`）：`跑团` / `开小游戏`。它们都**先推进 `位置` 再停**，
   所以收工之后从下一条继续，**这一条不会被重播**。

## 五、美术流水线

### 5.1 出图规格（用户工具的限制，写提示词时必须遵守）

- **只支持 5 种比例**：`1:1` `4:3` `3:4` `16:9` `9:16`
  （`3:2` 和 `4:1` 会返回 **502**）
- **不能写负面提示词** → 所有禁止项写成「排错检查：如果 XXX，这张图就是错的。」
- 出图**在右下角带「豆包AI生成」水印** → 检测内容边界时必须排除那一块，
  否则第 4 帧边界被撑到右下角，四格错位

### 5.2 处理链

```
原图/批次-XX/xxx.png  →  [工具/处理*.mjs]  →  成品/  →  [工具/接入地图素材.mjs]  →  public/assets/map/
                                              ↑
                              ★ 就地修复必须写回这里，否则下次同步会被覆盖
```

**踩过的大坑**：修行走表只写游戏目录、没写"成品"，后来一跑 `接入地图素材.mjs`
就被覆盖，bug 复发。**规矩：只要还有脚本从成品同步，就地修复就必须写回成品。**

### 5.3 素材进度

**总计 52/67（78%）**

| 批次 | 内容 | 状态 |
|---|---|---|
| 01~09 | 主角/全家福/其他角色/场景/瓦片/道具/界面/特效/瓦片v2 | ✅ |
| 10 | 界面v2（14 个）| ✅ |
| 11 | 角色动画：8 个行走表 + **坐姿 ×7** | ✅ 已接入 |
| 12~16 | 办公家具 / 小游戏道具 / 转场结局 / 界面背景 / 开场插图 | ✅ |
| 17 | 前台 / 文化墙 / 工位名牌 | ✅ 已接入 |
| 18 | 后续剧情（场景×3 + 贴纸 + 文件图标）| ⬜ **未生成** |
| **19 / 提示词-10** | **墙面元素 6 枚**（安全出口牌/灭火器/消防栓/挂钟/窗/指示牌）| ⬜ **未生成** |
| **19 / 提示词-10** | **开场插图灰地版**（方案 A 收尾，让开场和地图地面一致）| ⬜ **未生成** |
| **20 / 提示词-11** | **正面坐姿 ×7**（`正面坐姿_<角色>.png` → `sitfront_<角色>.png`）| ⬜ **未生成** |

**提示词位置**：`美术/提示词-10-一键复制.md` —— **9 条完整提示词，可整段复制**，
不用拼装。规格/垫图/文件名都写在每条的标题行。
正面坐姿那套单独一个文件：**`美术/提示词-11-正面坐姿.md`**（7 条，生成脚本 `工具/生成正面坐姿提示词.mjs`）。

### 5.3b 坐姿为什么有**两套**（别搞混）

| 套 | 文件名 | 用在哪 | 视角 | 画到哪 |
|---|---|---|---|---|
| **背面坐姿** | `sit_<角色>.png`（源：`坐姿_<角色>.png`）| 同事**在自己的工位**（开放办公区 / 总监办公室）| **纯背面**（背对走廊打字）| **只到腰**（下面 14px 留给椅子）|
| **正面坐姿** | `sitfront_<角色>.png`（源：`正面坐姿_<角色>.png`）| 剧情把同事**传送到茶水间 / 会议室**，围着桌子跟主角说话 | **正对面**（要看到全脸）| **从头到脚**（用户要求"要有下半身有腿"）|

> 为什么要两套：剧情换房间时同事是**瞬移**过去的（见 3.5），
> 到了茶水间他们是**面对主角说话**的 —— 拿"背面打字"那套摆过去，就是六个后脑勺对着玩家。
>
> ⚠️ **正面那套必须画到脚**（2026 改）：
> 背面那套只画到腰是**故意的**（腰部以下被椅子/桌子挡住，画了也看不见，还容易被裁）；
> 但正面这套在茶水间/会议室是**坐在小圆桌旁**、腿露在外面，只画半个身子就是"飘在半空"。
> 所以两套的**画布规格一样**（1024×1024 的 2×2 → 切 4 帧 32×48），但**锚点不同**：
> ```
> 背面：内容高 34，贴画布上沿（腰线固定）→ 人在 32×48 里偏上，下面留椅子位
> 正面：内容高 46，脚底贴画布底边（48 那条线）→ 整个人更小，但站/坐基线一致
> ```
> 处理脚本已经按 `--正面` 自动切这段逻辑，见 `工具/处理坐姿.mjs` 的 `内容H` / `内容顶`。
>
> ⚠️⚠️ **出图工具导出来的经常是"假 PNG"**（扩展名 .png、内容其实是 JPEG，文件头 `FF D8 FF`）。
> `工具/图像库.mjs` 是零依赖 PNG 解码器，读到会直接报「不是 PNG」。
> **先转格式**：`cd liukanshan-career; node tools/修图格式.mjs <绝对路径目录>`
> （用无头浏览器解码 → canvas → 真 PNG，原文件备份到该目录的 `_jpg原始/`）
>
> ⚠️ **同事坐哪 = 用哪套动画**（`OfficeMapScene.坐哪套()`）：
> **开放办公区/总监办公室**的转椅 → `坐_<名>`（背面）；
> **茶水间/会议室**的椅子 → `坐正_<名>`（正面）。
> 以后加房间只要**往 `座位表` 里加一行**，动画自己会对。
> 缺正面素材的角色，`坐正_` 自动退回背面那套（宁可姿势不对，也不能不动）。
>
> ⚠️ 两套的**处理方式一样**：
> ```powershell
> node 工具/处理坐姿.mjs           # 背面那套 → sit_*.png
> node 工具/处理坐姿.mjs --正面     # 正面那套 → sitfront_*.png
> ```
> ⚠️ 处理完要**对比两套的角色大小**：缩放比例是各自按内容框算的，
> 正面那套人物天生更小（要装下全身），别以为是算错了。
>
> ⚠️ 正面那套最大的坑是**排错检查要反过来**：背面要求"绝不能看到脸"，
> 正面要求"必须能看到脸"；而且正面还多两条 ——
> **"没有下半身/腿就是错的"**、**"腿或脚画成白色就是错的（四肢是黑的）"**。
> 直接复制背面那套提示词去改，最容易忘的就是这几条
> （生成出来又是一堆后脑勺、或者没有腿的半个身子）。
> 所以那 7 条是用脚本 `工具/生成正面坐姿提示词.mjs` 生成的，
> 七条逐字相同，改就七条一起改。


### 5.4 角色设定（画图/写文案都要遵守）

**刘看山解剖结构**（模型最容易画错的地方）：
- 整个身体是一块**上窄下宽的圆润白色蛋形**，臀部比肩部宽
- **没有脖子、没有肩线**，头和身体不分家
- 头顶两个白色**尖三角耳朵**
- 脸的上部一块**很大的黑色圆鼻子**（体宽的 1/3~2/5，接近圆形）
- 鼻子两侧**两个很小的黑色圆点眼睛**
- **没有嘴、没有胡须、没有眉毛**
- **四肢全黑**
- 米白衬衫 `#f7efdd` + 知乎蓝领带 `#4a8fd4`

**角色表**：

| 角色 | 特征 |
|---|---|
| 刘看山 | 白色，米白衬衫 + 知乎蓝领带 |
| 林总 | 白色，深灰西装 + 深蓝领带 |
| 周岚 | 白色，深绿外套 `#2f5d3a` |
| 阿麦 | 白色，橙色连帽卫衣 `#d9a066` |
| 韩策 | 白色，深蓝格纹衬衫 `#1b3a6b` |
| 小鹿 | 白色，浅绿毛衣 `#7cc26b` |
| **程女士** | **灰蓝色毛 `#a4acbb`**，全片唯一非白角色（客户）|
| 学长 | — |

**32 色调色板**：
```
#ffffff #f7efdd #f4f0ea #e8dcc0 #d8d2c8 #a8a29a #6e7686 #454b57 #262a33
#1b3a6b #2c5fa8 #4a8fd4 #8ec3ee #2f5d3a #4a8f4f #7cc26b #a8d98a
#d9a066 #b87b4a #8f5a33 #6b4423 #e04f3f #a4acbb #241a12 …
```

---

## 六、★ 工具脚本（这个项目最值钱的资产）

> **核心经验：截图和缩略图会骗人，量出来的数据不会。**
> 本次会话修的 5 个视觉 bug 里，**4 个是靠脚本量出来的**，靠眼睛只看出来 1 个。
>
> ⚠️ **跑之前先看清工作目录**（这个坑我踩了两次）：
> - `工具/*.mjs`（仓库根那套）→ 必须在**仓库根** `E:\projects\知乎小游戏` 跑
>   （它 `resolve('liukanshan-career/src/...')`）
> - `liukanshan-career/tools/*.mjs` → 必须在**游戏目录** `E:\projects\知乎小游戏\liukanshan-career` 跑
>   （它 `resolve('src/...')`）
>
> 懒得记就一条命令全跑（**在仓库根**）：`node 工具/体检全部.mjs`（加 `--快` 跳过最慢那步）

| 脚本 | 作用 | 什么时候用 |
|---|---|---|
| **`工具/体检全部.mjs`** | **一键跑完下面所有检查项**，自动切对工作目录 | **每轮收尾** |
| **`工具/查压墙.mjs`** | 量每件道具的像素包围盒 vs 挡路瓦片；**顺带查"道具挡门"** | **改完布局第一件事就跑它**（仓库根跑）|
| `工具/挪压墙道具.mjs` | 按上面的结果批量挪（离墙至少留 1 格）| 查出来有问题之后 |
| `工具/修行走表.mjs` | 把"朝左"行水平镜像成"朝右"行（治回头帧）| 行走动画出现怪帧 |
| `工具/处理坐姿.mjs` | 切 2×2 坐姿表 → 4 帧动画（重心对齐 + 排除水印）| 有新坐姿素材时 |
| `工具/重切办公道具.mjs` | 按区域分块 + 取**最大连通块**切图 | 一张表切出来有残边 |
| `工具/处理地图补充.mjs` | 地图补充素材的通用处理 | 有新家具素材时 |
| `工具/生成瓦片.mjs` | 程序生成 17 张瓦片 | 改瓦片配色/形状 |
| **`liukanshan-career/tools/指引线测试.mjs`** | 两张图验证指引线是**直线穿墙**（附"直线 vs A* 绕路"长度对照）| **改完指引线/相机第一件事就跑它** |
| **`liukanshan-career/tools/读网格.mjs`** | 把 `level.ts` 的网格打成**字符画** + 左右中轴对称检查（转述版，逻辑要和 level.ts 同步）| **改完布局第一件事**（截图会骗人，网格不会）|
| **`liukanshan-career/tools/读运行时网格.mjs`** | 直接读 **Phaser 里真实那份网格**（`图层.getTileAt`），不含任何转述 | 怀疑"门到底存不存在"时用它 |
| **`liukanshan-career/tools/查重叠.mjs`** | 查**道具 vs 道具**的占地重叠（`查压墙.mjs` 只管道具 vs 墙）| 摆完家具；"两台机器糊在一起"就是它 |
| **`工具/查座位.mjs`** | 查 `座位表` 和 `道具表` **对不对得上**（座位格上有没有那件家具 / 家具有没有登记座位）| **改座位表或家具后必跑**（"空地上凭空能坐"就是它抓的，见 §3.11）|
| **`工具/处理墙面元素.mjs`** | 切「墙面元素 6 枚」那张 3×2 表 → 6 张墙饰（格内找边界 + 避水印 + 量化描边）| 有新的墙饰素材时 |
| **`工具/生成正面坐姿提示词.mjs`** | 生成 7 条「正面坐姿」提示词（`美术/提示词-11-正面坐姿.md`）| 改坐姿提示词时（**别手改 md，改脚本再生成**）|
| **`liukanshan-career/tools/拍坐姿.mjs`** | 把三种家具各拍一张**放大图** + 打印 `人y / 人深度 / 同格家具深度` | **调坐姿偏移/遮挡的第一件事**（本次全靠它） |
| **`工具/查挡路.mjs`**（仓库根跑）| 把 10 组关键连通对（前台↔走廊↔各房间）各 A* 走一遍 + 打出网格字符画 | **摆完家具必跑**（"家具把走道堵住"只有它查得出来）|
| **`liukanshan-career/tools/微信开关体检.mjs`** | 验 Tab 掏手机不被强制关闭、剧情播到「去房间」会停住等玩家点、打开微信能看到消息 | **改键盘/切屏/`去房间` 逻辑之后** |
| **`liukanshan-career/tools/坐姿体检.mjs`** | 验主角能坐椅子、**工位坐背面 / 茶水间坐正面**、一按方向键就起身 | **改坐姿或座位表之后** |
| **`liukanshan-career/tools/查深度.mjs`** | 把**某个矩形区域里所有对象**的"贴图 / 坐标 / 深度"按深度排好打出来（含 NPC）。**"谁挡住了谁"只能靠它定位**，光看截图看不出来 | **看到"被挡住/穿模/头没了"时** |
| **`liukanshan-career/tools/空格优先级体检.mjs`** | 验空格的优先级：工位上「坐下 → **再按开主线**」、空沙发能下来、干净地板不误触。**用真键盘，每个场景一张新页面** | **改空格 / 座位 / 主线入口之后** |
| `liukanshan-career/tools/修图格式.mjs` | 把"扩展名 png、其实是 JPEG"的图转成真 PNG | **新素材读不进来时先跑它** |
| **`liukanshan-career/tools/墙面元素体检.mjs`** | 查每件"挂墙"的东西**压在什么瓦片上**（白墙 ✅ / 玻璃 ❌ / 门 ❌ / 地板 ❌）| **改完墙面元素必跑**（用户要求"把玻璃上的去掉"就是它抓的）|
| **`liukanshan-career/tools/AVG体检.mjs`** | 验"切会话不跳视图 / 未读小红点 / 只显示时间不显示旁白" | **改剧本或 story.ts 之后** |
| **`liukanshan-career/tools/去房间体检.mjs`** | 验「去房间」换场景：切回地图、停在原地、走到目标才续播、走错地方不带跑剧情 | **加/改 `去房间` 节拍之后** |
| **`liukanshan-career/tools/跑团自检.mjs`** | 跑团逻辑单测（**不需要浏览器**，69 条）：判定、指标只从脚本读、**镜头闸 + 台词闸**、`过闸`、`贴AI话` 只改文字、提示词带剧情进度、**事件四 15 回合结构** | **改 `跑团.ts` / `llm.ts` / `跑团脚本.ts` 之后** |
| **`liukanshan-career/tools/事件四验收.mjs`** | 事件四**整条链路**的实拍 + 20 条断言（跑团面板 → 收工 → 开小游戏 → 工作关 → 关卡结束）。要 dev server | **改剧本 / 跑团 / 关卡之后** |
| `liukanshan-career/tools/道具卡体检.mjs` | 验道具卡/人物卡（抽查 + 3×3 格全扫 + 五位同事）| 改卡片数据或靠近判定后 |
| `liukanshan-career/tools/卡片截图.mjs` | 站到家具/同事前截卡片实拍 | 看卡片观感 |
| **`liukanshan-career/tools/NPC站位体检.mjs`** | 按段号列每个同事的格子/所在房间/**坐姿或站立**；查同格叠人；核对"这段该在茶水间的人齐没齐" | **改 `NPC排布` / `座位表` 之后** |
| `liukanshan-career/tools/房间实拍.mjs` | 放大截某个房间（`node tools/房间实拍.mjs 茶水间`）| 看同事在房间里坐没坐对 |
| **`liukanshan-career/tools/对称体检.mjs`** | 算每扇门/竖廊/接待区家具**离地图中轴多少 px** | 谁再说"歪了"，先跑它 |
| **`liukanshan-career/tools/量包围盒.mjs`** | 零依赖读 PNG，量每张素材的**内容包围盒/重心/上下留白** | 摆位对不对、素材偏没偏 |
| `liukanshan-career/tools/工位放大.mjs` | 工位/前台**放大截图**（相机 stopFollow + 手动 scroll）| 看家具之间对齐 |
| `liukanshan-career/tools/工位体检.mjs` | 逐块统计画布像素（找"飘着的白块"之类）| 肉眼找不到的异物 |
| `工具/接入地图素材.mjs` | 把成品拷进 `public/assets/map/` | 素材更新后 |
| `工具/看新素材进度.mjs` | 统计批次进度 | 想知道还差多少素材 |
| `工具/图片体检.mjs` | 看图尺寸/色彩数 | 快速看一眼素材 |
| `工具/图像库.mjs` | 零依赖 PNG 读写 + 裁剪/缩放/量化/描边/拼图 | 写新处理脚本时 import 它 |

### 常用组合

```powershell
# ⓪ 最省事：一条命令跑完全部检查（在仓库根）
node 工具/体检全部.mjs --快

# ① 改完布局（家具/房间/门）——五件都要跑
node 工具/查压墙.mjs              # 道具 vs 墙 + 道具有没有挡门（必须 0）
node 工具/查座位.mjs              # 座位表 vs 道具表（别有空座位 / 别漏登记）
node liukanshan-career/tools/查重叠.mjs       # 道具 vs 道具 的占地重叠（必须 0）
node liukanshan-career/tools/读运行时网格.mjs 13 22   # 游戏里真实那份网格（看墙/门/走廊）
node liukanshan-career/tools/镜像体检.mjs 18   # 左右是否镜像（按区段打印，别自己数格子）

# ② 改完剧本 / story.ts ——两件都要跑
node liukanshan-career/tools/AVG体检.mjs       # 不跳视图 / 未读红点 / 只有时间没有旁白 / 邀请卡位置
node liukanshan-career/tools/微信开关体检.mjs  # Tab 开微信 + 播到「去房间」会停住等玩家点
node liukanshan-career/tools/剧情快照.mjs      # PC + 手机各 7 张，肉眼看一遍
node liukanshan-career/tools/事件四验收.mjs    # 事件四：跑团面板 + 工作关，7 张图 + 20 条断言

# ③ 改完道具卡 / 人物卡
node liukanshan-career/tools/道具卡体检.mjs    # 抽查 + 每件道具周围 3×3 格全扫 + 五位同事
node liukanshan-career/tools/卡片截图.mjs      # 出卡片实拍

# ④ 改完坐姿 / 座位表 / 椅子素材 ——两件都要跑
node liukanshan-career/tools/坐姿体检.mjs      # 能坐哪、背面还是正面、有人的椅子不坐
node liukanshan-career/tools/空格优先级体检.mjs  # 工位上"坐下 → 再按开主线"（真键盘）
node liukanshan-career/tools/拍坐姿.mjs        # 三张放大图 + 人/家具的深度对照（调偏移必看）

# ⑤ 改完指引线 / 相机
node liukanshan-career/tools/指引线测试.mjs    # 直线穿墙 + 长度对照

# ⑥ 类型与构建（每轮收尾都跑）
cd liukanshan-career; npx tsc --noEmit; npm run build

# ⑦ 素材更新后
node liukanshan-career/tools/修图格式.mjs <目录绝对路径>   # 先修"假 PNG"（扩展名 png 其实是 JPEG）
node 工具/处理坐姿.mjs --正面      # 或对应的处理脚本
node 工具/接入地图素材.mjs
```

---

## 七、踩过的坑（写代码前先扫一遍，能省几小时）

> 1~11 是最早那一轮留下的；**12~43 是 2026 这一轮新增的**（地图重排 / 卡片 / AVG / NPC / 工具链）。
> 带 ⚠️ 的都是**真的踩出血**的，不是"理论上要注意"。

### 素材 / 图像处理

1. **水印**：出图右下角有「豆包AI生成」，检测内容边界时要排除那块区域
2. **公共裁剪框跨整图**：四格边界必须换算成**相对本格**再取并集。
   用绝对坐标取并集会横跨整张图（实测 1869×1907，等于整张图）
3. **重心对齐 > 边界居中**：某格有不对称杂点时，按边界居中会明显偏（实测 8/1）。
   重心对杂点不敏感
4. **连通块切图会切残**：一张图里多件物品时，用"按区域分块 + 取最大连通块"
5. **缩放到别用块众数**：会产生品红毛边，用面积平均 + `去毛边`
6. **AI 会把面板留白**：画"资料架"这类带面板的东西，模型常留空面板，
   需要程序补内容（见 `工具/补道具内容.mjs`）
7. **`找空白` 的陷阱**：透明像素读出来是 `#000000`，会被误判。要跳过 alpha<128

### Phaser

8. **`putTileAt` 不继承碰撞**：换上去的新瓦片不会带"挡路"属性
   （我是用 `setCollision(索引数组)` 设的，不是靠 tileset 属性）→ 要手动 `setCollision`
9. **静态体开关只能用 `body.enable`**，`setActive` 对静态体无效
10. **键盘监听依赖画布焦点** → 交互键统一放 React 层
11. **深度是视觉 bug 的高发区**：桌面小件 +0.5、转椅 +2、墙按行算（见 3.6）

### TypeScript / 工具

12. **字面量联合类型**：`export const X = [瓦片.白墙, 瓦片.玻璃]` 会被推断成 `(6|7)[]`，
    `X.includes(变量)` 直接报类型错 → **显式标 `number[]`**
13. **keydown 的闭包是旧的**：读 React state 会拿到过期值 → 用 ref
14. ⚠️⚠️ **改代码一律用 `edit` 工具，绝不要用 PowerShell 做批量替换**（原话是"CRLF 字符串替换会静默失败"，
    但 2026 又踩了更狠的一版）：
    ```powershell
    # 我干的（想改两个类型名）：
    (Get-Content f.ts) -replace 'A','B' | Set-Content -NoNewline f.ts
    # 结果：整份 834 行的文件**被压成一行**（Get-Content 出来是数组，
    #       Set-Content -NoNewline 把它拼成一条没有换行的字符串）→ 文件废了
    ```
    - 后果：`git diff` 显示 `1 insertion, 834 deletions`，只能 `git checkout` 回滚**整份文件**，
      然后把这轮所有改动**手工重做一遍**
    - 真要批量替换：用 `edit`（可以 `replace_all`），或者 `node` 脚本读写
    - 任何时候看到"文件行数突然变成 1"，先 `git checkout -- <file>`，别想着修
15. **`.mjs` 是纯 JS**，不能写 TypeScript 类型标注
16. **私有字段只是编译期私有**：`private 查附近物()` 在 `page.evaluate` 里**能调到**，
    但**别调** —— 那会绕开"换格才重算"的真实路径，量出来的结论是假的（我差点被这个骗）。
    测法：`传送像素` 之后**等一两帧**，让场景自己跑 `update()`

### 地图 / 布局

19. ⚠️ **`造网格()` 里的顺序是硬约束**：`地面 → 所有墙（含外墙圈） → 竖廊 → 开门`。
    原来写成"先开门、后砌外墙"，**正门被外墙整格覆盖成白墙** ——
    游戏里那个门**根本不存在**，`门表` 里却明明写着有。
    实测：`node tools/读运行时网格.mjs 26 29`（读**游戏里那份网格**，不是抄源码的逻辑）
20. ⚠️ **"对称"和"走廊中心压中轴"在这个尺寸下不可兼得**：
    45 格、内宽 41、4 间房 + 2 格走廊 → 只能做到"左右房间等宽、镜面落在格上"，
    想再让 2 格走廊的中心落在 720px 中轴上就得改成 3 格走廊，那时房间宽会变成 8/9。
    **先问清楚用户要哪个**，别自己拍。
21. ⚠️ **别拿截图当事实**：我一度"看到"地图南边全是地板、以为门在那儿，
    实际是相机被 bounds 夹住 + 我算错了 scroll。**网格 / `getTileAt` 才是唯一事实来源。**
    同理：**截图里数格子会数错一格**，用 `镜像体检.mjs` 那种按区段打印的脚本。
22. ⚠️ **挪房间必须一起挪里面挡路的道具**，否则"打印机压墙"。
    `node 工具/查压墙.mjs` 要跑，**它只查"道具 vs 墙"**；
    **"道具 vs 道具"要另外跑 `node tools/查重叠.mjs`**（两台打印机糊在一起那次就是它抓到的）。
23. ⚠️ **大件精灵会互相遮挡，但占地不一定重叠**：
    投递箱(49px 宽)和打印机(42px 宽)占地不重叠（差 104px），
    可打印机的**精灵上半部分**还是被投递箱盖住 → 看着像"两台机器糊在一起"。
    判断"要不要删一个"看的是**精灵视觉**，不是只有占地。

### React / 交互层

24. ⚠️ **两张卡片不能写成"二选一"**：工位格上本来就有桌面小件（名牌/键盘），
    写成 `道具卡 ? A : 人物卡 ? B : null` 的话，**人物卡永远弹不出来**（实测踩过）。
    要各占一角（左下 / 右下）。
25. ⚠️ **"靠近"半径要按实际距离量**：NPC 半径写 30px 就永远弹不出来 ——
    同事坐的椅子格与它上面那格**正好差 32px**。写 40 才对。
26. ⚠️ **`占地表` 的高度写太大会让"靠近"误判**：沙发写了 18px 高，
    结果站在**前台**前面弹的是「双人沙发」的卡。大件的占地要贴着它画出来的那一小条。
27. ⚠️ **摆在不可走格子上的桌面小件，卡片永远显示不出来**（笔筒在桌面上，玩家站不到）。
    这种道具**故意不配卡**，否则覆盖率体检会一直报一件永远触发不了的东西。
28. ⚠️ **底部角落的 UI 会互相压**：设置齿轮原来在微信按钮**正上方**，
    正好压住右下角人物卡的右下角（用户报"npc 信息有点遮挡"）。
    改 `bottom` 之前先算一遍：卡片底边 vs 齿轮顶边。
29. ⚠️ **`wc-badge` 小红点本来就有**，别再造一个；只是以前"切会话自动跳视图"
    导致永远看不到未读（一跳过去就清零了）。**不跳视图之后红点才有意义。**

### 剧本 / 状态机

30. ⚠️ **邀请卡只画在"发邀请的那个私聊"里**（用户报过**两次**）。
    两处都会往会话里塞卡，改的时候**两处都要改**：
    - `邀请` 节拍：追加到**当前 `活跃会话`**
    - `接受邀请`：**只把私聊里那张标「已接受」+ 建群 + 切视图**，
      ⚠️ **绝对不要往群里再塞一张**（第一次漏的就是这里）
    体检脚本里已经有断言："接受前后，邀请卡都只在 lin 会话、group 里一张都没有"。
31. ⚠️ **`切会话` 与"跳视图"是两件事**：
    `切会话` 只改"消息往哪发"（不跳，玩家自己点、积未读红点）；
    `接受邀请` 才跳（邀请是当场发生的事）。把这两件混在一起改会连环出 bug。
32. ⚠️ **每个会话的第一条消息前面都要有时间戳**（用户要求"第一次发信息也要有时间"）。
    时间要**顺着剧情走**，别出现"后面的会话时间比前面早"（改剧本时对一遍文档里的时间表）。
33. **删旁白那次的教训**：用户说"不显示旁白"，我是**把 36 条 `旁白` 节拍全删/转成 `时间`**，
    而不是在渲染层藏起来。**"不显示"往往意味着"剧本里就不该有"** ——
    藏起来的话，剧本、搜索、会话列表预览里还全是它。

### 测试

34. **Puppeteer 的 `clickCount:2` 只发一对 down/up**，双击要发两次独立点击
35. **`pointerdown` + `preventDefault()` 会吞掉 click/dblclick** → 用时间差判双击
36. **测试断言别拿像素和瓦片号比**（我犯过，误判"被挡住"）
37. **量测脚本要读"运行时真数据"**：抄一遍源码逻辑的脚本（`读网格.mjs`）会因抄错而骗人；
    能读 Phaser 里的真实对象就读真的（`读运行时网格.mjs`、`图层.getTileAt`）
38. ⚠️ **"换格才重算"这类缓存会让测试假阴性**：同一格里反复 `传送像素`，
    场景根本不会重算 → 断言全挂。测试要么换格，要么等一帧再读。
39. ⚠️ **选择器写错会让人怀疑功能**：`document.querySelector('.map-prop.who')`（两个 class）
    写成了 `.map-prop-who`，害我查了半天"为什么人物卡不渲染"。**先打印 DOM 的 className 再断言。**
40. ⚠️ **脚本的工作目录**：仓库根的 `工具/*.mjs` 和游戏里的 `tools/*.mjs` **要求的 cwd 不一样**，
    跑错目录会报"找不到文件"，很容易误判成"脚本坏了"（我两次都差点去改脚本）。
    统一入口：`node 工具/体检全部.mjs`（它会 cd 到正确的目录）。
41. ⚠️ **相机别手动 `setScroll` 去"看某块地方"**：相机有 `bounds` 夹取、又和自己的 `zoom` 耦合，
    实测 `setScroll` 会被夹到完全不是你要的位置（我截了三次茶水间，前两次都是别的地方）。
    **要拍哪就 `传送像素` 把主角放过去、让相机跟随**（跟随本来就是它的本职）。
42. ⚠️ **`是椅子()` 只认转椅是不够的**：茶水间/会议室的**会议椅**没算进去，
    结果坐在那儿的同事会**站着**。已改成查 `座位表`（转椅 + 会议椅都登记）。
43. ⚠️ **NPC 坐标不是"看着差不多就行"**：`(6,19)` 这种"桌子旁边"的格子其实在**过道**上，
    人站在那儿不会播坐姿、还容易和吧台叠在一起。**必须正好落在 `座位表` 的格子上。**

### 2026-09-14 这一轮新增（44~55，全是真踩出来的）

44. ⚠️⚠️ **`座位表` 和 `道具表` 必须对得上**（本会话最值钱的一条）：
    `座位表` 里曾登记两张**根本不存在的"单人沙发"** `(17,4)(23,4)` ——
    `道具表` 从来没摆过它们（素材也只有 `prop_ws_双人沙发`）。
    用户看到的怪现象是「**自己工位左侧没有座位，按空格却坐下了**」：
    人凭空"坐"在空地上。**引擎逻辑没错，是数据对不上。**
    已删掉那两条，并写了 `工具/查座位.mjs` 常驻守（按**占地**判，不要求格坐标相等 ——
    双人沙发宽 44px 会压到隔壁一格，那格登记座位是**对的**）。
    **教训：凡是"表 A 说这里有什么"的约定，都要有一条自动检查去比对表 B。**
45. ⚠️ **`走()` 每帧重设主角深度** → 任何"特殊深度"都必须在 `走()` 里也能算出来。
    正面坐要 `+40`，我一开始只在 `坐下()` 里设 —— 坐下瞬间 700、**下一帧变回 660**，
    家具又把整个人盖住。修法：把 `套` 存进 `座位表`，`走()` 里按它算。
46. ⚠️ **Space 是浏览器的"激活键"**：焦点在某个 `<button>` 上时（右下角"微信"很容易拿到焦点），
    空格会去**点那个按钮**。`preventDefault()` 必须**进分支第一件事**就调，早于所有判断。
47. ⚠️ **`<button>` 不能套 `<button>`**：React 会刷一屏 hydration 报错。
    本项目踩了两处，都是"头像既自己可点、又被放进按钮里"——
    给头像组件加 `静态` 参数渲染成 `<span>`。
    自查：`document.querySelectorAll('button button').length` 必须是 **0**。
48. ⚠️ **一次按键可能触发两个监听器**：切屏是 React 重渲染、**卸载/挂载在这个键盘事件之后**，
    所以按一次 Tab 会先后跑到"地图"和"微信"两边的 handler（实测屏幕停在 map）。
    两个 handler 都要先判"我现在还在不在这一屏"。
49. ⚠️ **自动推进不能依赖"玩家在看哪个会话"**：原来它挂在 `Avg` 里、条件是"正在看的==发消息的"，
    结果玩家看私聊时**剧情卡死**。抽成 `StoryClock`（挂 `Root`，条件只有"屏幕是微信"），
    不是当前会话的消息就进未读红点。**`Avg` 里不要再留第二份定时器**（两个会打架、一次跳两步）。
50. ⚠️ **自动推进也不能直接播进「去房间」节拍**：那条会把屏幕切到地图，
    玩家刚打开微信就被"拽回办公室"。改成**只登记 `待去房间`、等玩家点按钮**。
51. ⚠️ **抬高精灵后，所有"按像素反推格号"的地方都会差一行**（实测坐姿 4,20 → 4,19）：
    体检脚本读同事位置要用场景给的 `NPC所在格()`（逻辑格）；
    `查附近人()` 判距离也要按**逻辑格的脚底**算，不然坐着的同事"远 20px"、站在旁边弹不出卡
    （实测 5 位同事全漏）。
52. ⚠️ **"幽灵座位"会让左右判断全错**：我曾按用户要求加了"自己工位左侧不能坐"的特殊规则，
    结果删掉那两张幽灵沙发后，**四个方向本来就都正常**。
    **先查数据对不对，再考虑加规则** —— 加规则只是把数据 bug 掩盖住。
53. ⚠️ **素材"扩展名 png、其实是 JPEG"**：出图工具经常这样导出，
    零依赖 PNG 解码器会报「不是 PNG」。先跑 `tools/修图格式.mjs <目录>`（无头浏览器转真 PNG）。
54. ⚠️ **PowerShell 读中文会乱码**：`Invoke-WebRequest ... .Content` 默认按 ANSI 解，
    中文全变 `åå
¬`。**别用它判断"服务端代码是不是旧的"**（我用错这个结论，
    白重启了一次 dev server）。要验就用 `New-Object System.Net.WebClient` +
    `$wc.Encoding = [Text.Encoding]::UTF8`，或者干脆 `read` 工具读磁盘文件。
55. ⚠️ **给精灵"设置数据"的时机**：我把"取角色名"从 `getData('名')` 改成**显式传参**，
    因为同事那边是**先建精灵、后挂名字** —— 结果全部退回 `刘看山` 的动画
    （小鹿/阿麦 都变成刘看山）。**依赖顺序的东西，宁可显式传参。**

---


### 2026-09 第十二轮新增（44~58）

> 这 15 条几乎每一条都**真的把功能搞坏过**，不是"理论上要注意"。

44. **挂在"任何一次坐下"上的过场会连累别的椅子** ✗
    "第一次坐椅子进过场"如果挂在 `坐完了` 上，**茶水间椅子 / 沙发 / 空工位**也会触发过场并跳微信，
    还让坐姿体检红了 9 项。→ 过场要挂在**语义明确的那一步**（这里是"坐工位 → 按空格开这一章"）。

45. **`bus.on(...)` 写在 `if (!keyboard) return` 后面 = 没键盘就永远没监听** ✗
    「开始干活」走的是 `bus.emit(CMD.start)`，和键盘**毫无关系** —— 却因为这道闸在手机上点了没反应。
    → **按钮/剧情这类通道，不能关在键盘的 if 里面**。

46. **`Set.forEach` 遇到异常会中断后续** → 一个坏监听能让**整条命令失灵** ✗✗
    重来之后旧场景的监听没摘干净，它在已销毁的场景上抛错 → 新场景那个正确的监听永远轮不到执行。
    → `bus.emit` **逐处理器 try/catch 隔离**（坏的自坏、好的照跑；出错 `console.error` 别吞）。

47. **知乎检索的四条实测规矩**
    · **结果每次调用都不一样**（同一个查询词，两次给的赞数可以完全不同）→ 必须"**查完立刻落盘**"；
    · **接口没有「观看量」**：全 JSON 只有 `VoteUpCount` 和 `CommentCount`，没有任何 view/visit/read 字段
      → **别承诺"挑观看量高的"**；
    · **两个主题很容易查到同一篇** → 要**按链接跨主题去重**；
    · **只准变好、不准变差**（新的赞数没旧的高就别动），否则跑一次就把好卡换成差卡。

48. **`tsc --noEmit` 说 clean，Vite 却报"模块没有那个导出"** ✗
    我按"tsc 过了就没事"写代码，结果小游戏**直接加载不出来**。
    → 改完 import/导出，**以运行时报错（或 `npm run build`）为准**，别只信 tsc。

49. **手动连调 `tickNpc(50)` 量出来的"队友卡住"是假故障** ✗
    手动循环调用时**物理引擎根本没步进**（速度瞬间被清零）→ 量出"14 秒一步没走"。
    → 要在**真实时间**里观察（rAF 跑着）。这条坑让我白查了一轮。

50. **探针先把状态设成非目标值、再触发动作 → 量出假故障** ✗（我犯了三次）
    例：先 `屏幕='avg'` 再调 `地图交互`（那是地图动作）→ 量出"到不了待去房间"。
    → 写探针前先问一句"**这个动作的前置条件我满足了吗**"。

51. **CSS 里用相对路径引用 public 资源 = dev 正常、打包后 404** ✗✗
    `background-image: url('./assets/avg/hourglass.png')` 在 dev 下被 Vite 重写成对的，
    **打包后**变成 `/刘看山/assets/assets/...` → 线上过场**没有沙漏**。
    → public 资源一律用 `new URL('assets/...', document.baseURI)` 拼绝对路径内联。

52. **移动端的尺寸覆盖是按"旧精灵表方向"写死的** ✗
    `.lv-touch .lv-hour-img { background-size: 260px 26px }` 是横排算法；
    精灵表换成竖排之后，**手机上沙漏一片空白**（PC 端走基础规则所以看不出来）。

53. **React Hooks 规则：`if (...) return` 不能插在钩子前面** ✗
    → `Rendered more hooks than during the previous render`，一打开弹层就整屏崩。
    **所有钩子必须在任何 return 之前**（这次是 `StartOverlay`）。

54. **局部变量遮蔽模块级函数** ✗
    我在组件里写了 `const 切设置 = useStory(s => s.开设置)` ——
    不但动作名错（实际叫 `开关设置`），还把**模块级的 `切设置()` 遮蔽**了 → 构建 `TS2339`。

55. **构建失败还继续打包部署 = 发的是上一次的旧包** ✗✗（踩过两次）
    `npm run build` 报错 → 没产出新 dist → `tar` 打的是旧的 → "部署成功"但线上没变。
    → 部署脚本必须 **`if ($LASTEXITCODE -ne 0) { 不打包不部署 }`**，
      而且**核对包里的文件名和服务器上的文件名一致**。

56. **`onClick` 在触摸路径下不一定合成** ✗
    「开始干活」用 `onClick` → 手机上点不动。→ 游戏内的按钮一律 `onPointerDown` + `preventDefault`。

57. **"等玩家操作"的守卫写太严 = 整段主线静默不启动** ✗
    原来要求"2.4 秒后屏幕还得停在 map"，玩家在过场那两秒点一下微信 → **这一段永远不启动**。
    → 只在"进了小游戏 / 又有过场"时才放弃。

58. **连按交互键会排多个定时器 → 每个都把队列重置回第 0 拍 → 剧情循环** ✗
    用户报的"触发不了林总那段、一直循环"就是这个。
    → 进场加一个"**入场中**"令牌：已登记就直接返回，定时器醒来先核对"还是不是我这次"。


### 2026-09 第十三批（59~64）—— 这一批全是"我自己挖的坑"

59. **JSX 注释有两条禁令，违反就是整站白屏** ✗✗（我犯了两次）
    · 不能当 `return (` 或三元的**第一个节点**：`return ({/* 注释 */} <div/>)` ✗
    · **注释里不能出现花括号**：我在注释里写了一个左花括号描述 JSX，解析器照样去配对 →
      `PARSE_ERROR` → **整个站点起不来**（连入口都没了）✔
    · ⚠️ 更气人的是：**`tsc --noEmit` 两次都说 clean** ✗ —— 必须以 **Vite/运行时报错**为准
    · 排查手法（好使）：**逐个请求模块 URL**，谁 500 就是谁 ✗
      `curl http://127.0.0.1:5273/src/ui/ResultPanel.tsx`

60. **"帧去重"把正常步态砍成 2~3 帧 → 变成抽搐** ✗
    素材里 "A A B A" 是**美术冗余**，我用运行时按像素差丢帧去"修"它 ✗ ——
    结果生成出来的动画是 `下:[0,2]`、`左:[8,9,10]`，播起来就是**高频抖**（用户报的"一直摆头"）✔
    **正确路径是重新出图**（提示词见 `美术/提示词-13-AI队友行走表-重新生成.md`），
    运行时**永远不要偷偷丢帧** ✗

61. **部署"成功"不能只看脚本没报错 —— 要看服务器文件的时间戳/哈希** ✗（踩过两次）
    上传超时 → tar 是旧的 → 解包也是旧的 → 脚本照样打印"✅ 部署完成" ✔
    现在流程里必做：**核对服务器上 `index-*.js` 的哈希/时间戳** ✔

62. **`plink` 上传也要重试** ✗
    这台服务器 SSH **偶发超时**（远程执行和上传都会），重试只包住一边没用 ✔
    → 上传 + 执行**各自** `for` 重试 5 次，失败就**直接不部署**（别装作成功 ✗）

63. **未使用的变量/钩子会让构建失败** ✗（`TS6133`，我踩了 4 次）
    删掉一块 UI（比如技能卡条、⚙ 按钮）之后，**它对应的变量/钩子就没人用了** ✔
    → 构建检查（`$LASTEXITCODE`）**拦住了 4 次**把坏包发上线 ✗ —— 这条闸必须留着 ✔

64. **改 `flex-wrap` 之前先看那一排里还有谁** ✗
    用户要"工单只排一排"，我把 `.lv-orders` 设成 `nowrap` ✗ ——
    可**同一排里还塞着"手上/帮手"块**，不换行就被卡片挤到重叠 ✔
    → 正确做法：要保的块 `flex: none`（不许压缩）+ 整排 `overflow-x: auto` ✔


### 2026-09 第十四批（65~70）—— 部署/加载/HUD/自查纪律

65. **写 guard 时别顺手加 `|| true`** ✗✗
    地图 preload 里坐姿素材那段原来是：
    """
    if (this.textures.exists(键) || true) { this.load.spritesheet(键, …) }
    """
    那个 `|| true` 把唯一的判断**废掉**了 ✗ —— 纹理是**全局的**（跨场景存活 ✔），
    而 preload 每次进地图都会再跑 ✗ → 重复加载已存在的 key →
    Phaser 直接报 **Failed to process file** ✔（日志里那一串 sit_/sitfront_ ✗）
    同一个文件里的道具、NPC 都写了 `if (!exists)` ✔，只有坐姿漏了 ✗
    → **加 guard 时先看旁边同类代码怎么写的** ✗

66. **探针构造状态之前，先问一句：这个状态在真实流程里由谁产生** ✗
    我在这件事上**连续错了 7 次** ✗，每次都白烧一轮：
      · 手写 localStorage 存档 → 被存档校验拒掉，根本没加载 ✗
      · 直接 setState 改 store → 和 store 自己推导的队列打架 ✗
      · 用错误的入口点调 地图交互 → 量出「剧情卡住」的假故障 ✗
      · 用 25ms 死循环推 推进一步 → 空队列被推成「播完」，也是假的 ✗
    → **能走真实路径就走真实路径**（点按钮、调真实 action ✗）；
      非要注入状态，先确认它**能通过校验/能被 store 接受** ✗

67. **「空队列 + 还挂着地图目标」不等于「全剧播完」** ✗
    设计上：一段演完会**回地图**（队列清空 ✗）、把 地图目标 指向下一段入口 ✔，
    等玩家走过去交互才播下一段 ✔。
    但 初始() 里写的是 `播完: 位 >= 队.length` ✗ —— 空队列必然为 true ✗，
    屏幕又优先回 avg ✗ → **刷新之后主线彻底不动** ✔（用户报的「进小游戏前面的剧情无法进展」）
    → 判「播完」时必须**先看有没有 地图目标** ✔

68. **长缓存规则千万别盖到「文件名不变」的素材上** ✗✗
    我一开始图省事写了：`location /刘看山/assets/ { expires 30d; immutable }` ✗
    可是美术素材**名字是固定的**（chr_lu_walk.png ✗）——
    用户重新生成素材、我也重新部署了 ✔，**他那边 30 天都拿的是旧图** ✔
    （他连着报「小鹿还在摆头」，真凶是这个 ✗）
    → 只给**带哈希的** index-<hash>.js/css 长缓存 ✔，其余素材一律 `no-cache` ✔
    → 验法：`curl -sSI <素材url> | grep -i cache-control` 看是不是 no-cache ✗

69. **HUD 每一块都要给「固定宽度 + 省略号」** ✗
    用户报「一开始是对的，后面随着变化就开始相互挤压重叠」 ✔
    根因是那一排全靠内容自适应 ✗：工单文案变长、交错数进位、提示条出现……
    任何一处**都能把邻居顶开** ✔
    → 修法不是调间距 ✗，而是**每块固定 flex-basis + overflow hidden +
      text-overflow ellipsis** ✔（内容再长也撑不开 ✗）
    → 以后往任务条加字段：**先给它一个固定宽度** ✔

70. **给自己写代码也立两条纪律**（我这几轮各违反多次 ✗）
    · **不要嵌套同类引号**：在 JS 双引号串里再写双引号 ✗ → 程序直接语法错，白烧一轮 ✔
      （我犯了 5 次 ✗ —— 一律改用「」✔）
    · **模板字符串里不要出现反引号**：CSS 注释里写 `尺寸.顶高` ✗ 会把模板串截断 ✔
    · 改 JSX 之后**别只信 tsc** ✗：注释写错位置/注释里有花括号 → Vite PARSE_ERROR →
      **整站白屏**，而 tsc 说 clean ✔（排查：逐个请求模块 URL，谁 500 就是谁 ✗）

### 2026-09 第十五批（71~83）—— 素材 / 部署 / CSS / OAuth / 我自己的纪律

71. **白底素材 + 靠 alpha 找边界的流水线 = 整体失效** ✗✗
    用户新生成的行走表是**白底不透明**（JPEG 转过来的 ✗），
    而 `工具/处理角色动画.mjs` 是靠 **alpha 通道**找角色边界的 ✔
    → 它把**整个格子**当成内容 → 16 个角色被缩成 **7 像素**挤在左上角 ✔
    成品看着像「没处理、直接贴原图」✗，其实比原图还糟 ✔
    → 先看源图的 alpha 分布 ✗，别只看「工具跑成功了」 ✔

72. **全图抠白会把白色角色抠没** ✗✗（我在同一个功能上连栽两次）
    角色是**白色北极狐** ✗，我写「凡是接近白的都变透明」
    → 脸、肚子、手全没了，只剩描边和绿衣服 ✔（用户原话：**脸都没有了**）
    → 正确做法：**从四边泛洪**，只吃掉「和画面边界连通的背景」✔
      被描边围住的脸自然保留 ✔（`liukanshan-career/tools/切行走表.mjs` 就是这么写的 ✔）

73. **`putImageData` 会忽略 canvas transform** ✗
    我用 `translate + scale(-1,1)` 做第 4 行镜像，再用 `putImageData` 贴回去
    → 镜像**根本没生效**（第 4 行只是第 3 行的复制）✔
    → 镜像要用 `drawImage`（尊重 transform ✔）或手工翻像素 ✔

74. **仓库根的 `工具/*.mjs` import 不到 `puppeteer-core`** ✗
    它只装在 `liukanshan-career/node_modules` ✔
    → 需要无头浏览器的脚本放 `liukanshan-career/tools/` ✔（我白跑一轮 ✔）

75. **「工具说它写出成功了」≠「文件真的变了」** ✗✗（这条我栽了两次）
    · 一次是上传超时，tar 是旧的 → 解包还是旧的 ✔
    · 一次是换行走表，工具打印了目标路径，可文件**一个字节都没变** ✔
      （用户看到的还是旧素材，连续报了三轮「还在摆头」✗）
    → **凡是替换文件的操作，前后各打一次 sha256** ✔（唯一可信的验证 ✗）

76. **别靠「一层层往后追加 CSS」去打架** ✗
    我前几轮每改一次就 Add-Content 一段 ✗，前后的 `flex-basis`/`display`/字号互相覆盖 ✔
    每次只量最新那条，看不出全局冲突 → 修了四轮都没修好「挤压」✔
    → 改用**结构性约束**：`.lv-top` 用 grid 等分 ✔、中间容器 `display: contents` 摊平 ✔、
      文字一律 `nowrap + ellipsis + min-width:0` ✔

77. **systemd 的 `ExecStart` 路径必须真实存在** ✗
    我写 `/usr/bin/node` ✗，这台机是 **`/usr/local/bin/node`** ✔
    → 服务状态 `activating` 无限重启 ✗，日志里是 **`status=203/EXEC`** ✔
    → 排 systemd 先看 `journalctl -u <服务> -n 20` ✔

78. **Node 的 `req.url` 是百分号编码的** ✗
    我拿编码后的 pathname 去和中文 `/刘看山/api/zhihu` 比对 → **永远匹配不上** ✔（接口全 400/空）
    → 先 `decodeURIComponent(url.pathname)` 再匹配 ✔

79. **别用中文路径直接 curl 测** ✗
    shell 会把命令里的中文搞坏 ✗，害我一度以为「服务坏了」✔
    → 用百分号编码测：`curl http://127.0.0.1:5310/%E5%88%98%E7%9C%8B%E5%B1%B1/api/zhihu/status` ✔

80. **根路径的接口会被 nginx 兜底交给主站** ✗✗
    `src/story/llm.ts` 里 AI 代理写的是 **`/api/npc`**（根路径 ✗）
    → 它不在 `/刘看山/` 下 → 被 `location /` 交给**主站 agrimind 的后端** → 主站要鉴权
      → 控制台一串 **401 Unauthorized** ✔（用户报的就是这个 ✗）
    → **自己项目的东西一律放 `/刘看山/` 命名空间** ✔；没实现的接口先给 **204 空响应**
      （成功状态 ✗ 不是错误 → 控制台干净 ✔ 游戏走兜底 ✔）

81. **知乎 OAuth 的落地要点**（照 hackathon skill 的 SKILL.md + hello-world-oauth 抄 ✗）
    · 授权：`GET https://openapi.zhihu.com/authorize` + `redirect_uri` / `app_id` / `state` ✔
    · 回调参数名是 **`authorization_code`**（不是 code ✗）；官方**不保证返 state** ✔
    · 换 token：`POST https://openapi.zhihu.com/access_token`（表单 app_id/app_key/grant_type/redirect_uri/code）
    · 资料：`GET https://openapi.zhihu.com/user` + `Authorization: Bearer <access_token>` ✔
    · **回调必须是公网 HTTPS** ✗（127.0.0.1 / localhost 不算 ✔）
    · **app_key 只进服务端**（我们放 `/etc/liukanshan-zhihu.env`，600 权限 ✗）；token **只存进程内存** ✔
    · 授权页最后那一下**必须用户本人点** ✗，不许代点 ✔
    · ⚠️ 页面 500 时先怀疑「**回调地址没在开放平台登记**」或对方/网络（日志里那条
      `118.89.204.198/resolv?...` 的 CORS 拦截就是本机网络层面的 ✗）

82. **别为了「画面大」把 HUD 压到装不下** ✗
    用户嫌任务条占地方，我就把顶高从 4/12 压到 2/12、字号也缩 ✗
    → 卡片被截成「阿麦·…」✗、工序块和「手上空」**叠在一起** ✔
    （用户发了对比图我才定位到 ✗ —— **他说「太挤」时，先量内容需要多少高，别先动字号**）

83. **写代码时的引号纪律**（我自己犯的，共 **11 次** ✗✗）
    · 在 JS 双引号字符串里再写双引号 ✗ → **程序直接语法错**，那一轮全白跑 ✔
      （`Expected ',', got 'xxx'` 就是这个，我一开始还以为是编辑器的锅 ✗）
    · 模板字符串里不要出现**反引号** ✗（我在 CSS 注释里写 `尺寸.顶高`，把模板串截断了 ✔）
    · 从这轮起：**插入的文本一律用「」** ✔，绝不嵌套同类引号 ✔

### 2026-09 第十六批（84~85）—— 知乎 OAuth 的两个坑

84. **微信登录的知乎账号，不能走 OAuth 授权** ✗（平台侧限制，不是我们的问题）
    用户实测：**手机号登录**的知乎账号授权正常 ✔；**微信登录**的账号在授权页直接报
    「出错了！请稍后再试。」✗ —— **压根回不到我们的 /callback** ✔
    · 证据（服务端日志）：反复 /login 8 次，只有 2 次走到 /callback ✔；
      而**每一次到达 callback 的都换 token 成功**（成功 6 次、失败 0 次 ✔）
    · 结论：问题在**知乎那一步** ✗，代码里绕不过去 ✔
    · 解法（只能在账号侧）：在知乎 App 里**给账号绑定手机号** ✔，或换手机号账号 ✔
    · 前端已加提示（.zm-note）：授权失败时告诉用户去绑手机号 ✔
    · ⚠️ 另外：**微信内置浏览器**也开不了第三方 OAuth ✗ —— 登录门已按 UA 识别
      （MicroMessenger / QQ / 微博 / 抖音 / 支付宝 / 钉钉 ✔）并引导到系统浏览器 ✔

85. **第三方 OAuth 的日志：记流程、不记密钥** ✗
    服务端每一步记一行 ✔（/login、/callback、换 token 成功/失败 + 错误码 ✔），
    **绝不记 code / token / app_key** ✔（官方 Skill 的硬要求 ✗）。
    · 排障口诀：**有 /login 没 /callback → 平台没跳回来**（多半是回调登记或账号限制 ✗）；
      **有 /callback 但换 token 失败 → 密钥或回调不匹配** ✔
    · 这次就是靠三行日志一眼定性的 ✔（否则又会去乱改前端 ✗）

### 2026-09 第十七批（86~90）—— 今天这几轮最值钱的五条

86. **「有物理体」≠「会碰撞」** ✗✗（我在这上面白折腾两轮）
    用户报「打印机、咖啡机、工位等道具没有物理碰撞」✗
    量出来的真相：那些道具**静态物理体 13 个全都有** ✔，
    但 `create()` 里只挂了三行 collider（瓦片墙 + 队友 ✗），
    **没有任何 collider 引用道具** ✔ → 有体、却谁也撞不上 ✔
    · 规矩：`physics.add.existing(obj, true)` 只是给它一个体 ✗，
      **必须再 `physics.add.collider(A, B)` 才能真的挡人** ✔
    · 排查手法：先数 `obj.body` 有没有 ✗，再数 collider 里有没有它 ✔

87. **`flex: 0 1 auto` 会让文字溢出盒子 = 视觉上重叠** ✗
    用户报「提示文字相互重叠」✔ —— 根因不是行高不够 ✗，
    而是那一行四个子元素**允许被压缩** ✔：压小之后文字**溢出自己的盒子** ✔，
    看起来就和邻居叠在一起 ✔
    · HUD 里的块一律 `flex: 0 0 auto` ✔；超了各自 overflow hidden + ellipsis ✔
    · 想给某块限宽：用 `max-width` ✗，**别用压缩** ✔

88. **grid 写死行数 = 定时炸弹** ✗（这是我第三次踩）
    我给任务条写了 `grid-template-rows: auto auto 22px` ✗（3 行 ✔），
    而手机端实际有 **5 块** ✔ → 多出来的落进隐式自动行、被挤扁 ✔
    → 工单卡被遮挡 ✔
    · 规矩：**不写行数** ✔ —— `grid-template-rows: none` + `grid-auto-rows: auto`
      + `align-content: start` ✔（有几块排几行、各自按内容高 ✔）
    · 同类坑还有：`.lv-extra` 的 `display: contents`、移动端隐藏两块之后的轨道数 ✗

89. **样式问题先量 computed style，不要推理** ✗✗（我连猜了四轮）
    「第三行文字出框」「设置滑不动」这两个 ✗，我先后猜过：
      vh 单位 ✗ → 飘字换行 ✗ → 反向旋转 ✗ → 又撤回旋转 ✗（全错 ✔）
    最后**把真实数据打出来**（offsetHeight / scrollHeight / clientHeight /
    getComputedStyle().transform / touchAction ✗）→ **一量就定位** ✔
    · 规矩：改样式之前先跑一段探针，把这几项打出来 ✔
      （`offsetHeight` **不受 transform 影响** ✗，判断板上尺寸要用它 ✔）

90. **反向旋转必须和屏幕单位尺寸同时发** ✗
    手机横板是把整个关卡转了 90° ✗ —— 弹窗的滚动轴也跟着转 ✔
    → 手指竖着拖滚不动 ✔（用户：不能上下滑动 ✗）
    · 解法：弹窗 `transform: rotate(-90deg)` ✔（净旋转 0° → 屏幕上是正的 ✔）
      **并且**尺寸换成 `vw/vh`（屏幕单位 ✗）✔
    · ⚠️ 我两次各只发了一半 ✗：只转正不发尺寸 → 文字超出屏幕 ✔；
      只改尺寸不转正 → 竖着还是滑不动 ✔
    · ⚠️ 更糟的是：那次两个一起修的部署被 `abort` 打断 ✗，
      **我没核对服务器文件就往下走了** ✔ → 又白折腾一轮 ✔
      （老坑重演：工具说成功 ≠ 真发出去了，要核对哈希/时间戳 ✗）

### 2026-09 第十八批（91~94）—— 收尾这几轮

91. **兜底必须有边界条件** ✗ ——「任何情况下都兜」等于把 bug 从一种变成另一种 ✔
    用户报「按空格不能推进」✗，我加了兜底：剧情在等去哪儿时，
    按空格就按「到了那儿」处理 ✔ —— **但没加距离判断** ✗
    → 用户立刻报「**没走到特定位置也能按空格触发剧情**」✔
    · 正解：场景加 `在点附近(id, 半径)` ✔（用交互点表 + 格到像素算距离 ✔），
      只有真的走近了才认 ✔
    · 规律：**凡是兜底/放宽的改动，都要同时写清「什么时候不许生效」** ✗
      （同类：`|| true` 废掉 guard ✗、有体没 collider ✗、状态注入没考虑 sync 覆盖 ✔）

92. **「按空格不推进剧情」这一整类的根因是剧情时钟** ✗✗（我打了好多轮补丁都治不好）
    症状（用户陆续报了四次 ✗）：茶水间对话不触发 / 去会议室找周岚不触发 /
    小鹿邀请不触发 / 只有手动开微信才继续 ✔
    真因：`StoryClock` 第一行就是 `if (屏幕 !== 'avg') return;` ✗
      —— **剧情只在微信屏推进** ✔；某一步把屏幕留在了 map ✔
      → 时钟停摆 → 怎么按空格都不动 ✔ → 一开微信立刻往下播 ✔
    · 我前面都在「按空格那一下」打补丁 ✗（查交互 id ✗、加目标兜底 ✗）——
      那些只让「能走的路」变多 ✔，**根没治** ✗
    · 正解：在 StoryClock 里加**反向兜底** ✔ ——
      屏幕在地图、队列却还有待播的拍、且没有任何「该待在地图」的状态
      （待选择/待接受邀请/待暂停/待去房间/待开小游戏/跑团/过场/续播/地图目标/支线中 ✗）
      → **自动切回 avg** ✔

93. **推送到 GitHub 失败，先查代理环境变量** ✗（不是仓库的问题）
    报错：`Failed to connect to github.com port 443 via 127.0.0.1` ✔
    真因：机器上有 `HTTP_PROXY/HTTPS_PROXY = http://127.0.0.1:7897` ✗（代理软件没开 ✔），
    但 `git config` 里**没有**代理配置 ✔ —— git 读的是**环境变量** ✔
    · 解：`$env:HTTP_PROXY=''; $env:HTTPS_PROXY=''; git push origin main` ✔
    · 另：**git 的报错走 stderr** ✗ —— 我用 `Select-String` 过滤 stdout 时把它吃掉了 ✔，
      看到的是「没有任何输出」✔ → 一度以为推送成功了 ✔（要 `2>&1 | ForEach-Object` ✗）

94. **探针注入 store 前，先确认「每帧 sync 会不会覆盖它」** ✗
    我为了复现「打印机倒计时偏移」✗，直接 `__lksStore.setState({打印: {...}})` 注入 ✔
    但场景**每帧都 sync** 一次 ✔（把 打印: null 推回来 ✗）→ 注入 1 帧内被清掉 ✔
    → 量出来是「打印机块没渲染」✔（探针又白跑一轮 ✔）
    · 规矩：注入状态后**先量一次「它还在不在」** ✗，再拿它做判断 ✔
      （和坑 66「探针构造的状态在真实流程里由谁产生」是同一条 ✔）

⚠️ 另外两条**我自己的坏习惯**（这轮暴露得很清楚 ✗，一并记下）：
  · **没读代码就下结论** ✗：我说「`.lv-printer` 从来没样式」✔，
    结果 grep 一看，它在 CSS 里**有完整一套**（蓝色卡片 ✔）——
    我不仅判断错了 ✗，还顺手把它的配色改成了橙色 ✔（多余改动 ✗）
  · **明知有风险还是没加约束** ✗：加兜底时我在注释里写了「有风险 ✔」，
    但没写任何守卫 ✔ → 下一轮用户就撞上了 ✔
## 八、当前待办

> ⚠️ **这一节按 2026-09-14 的状态重排过**。下面"优先级 1"是**已经修完的历史记录**
> （留着是为了说明"为什么是 (24,4)"），真正**还没做的**从"优先级 A"开始看。

### 已经做完的（别重复做）

| 事 | 状态 |
|---|---|
| 工位错位（「我的工位」从 (17,4) 改到 (24,4)）| ✅ 见下面「优先级 1」的复盘 |
| 同事人物卡（靠近 40px 弹卡，七个角色都有）| ✅ `src/story/人物卡.ts` |
| 道具卡（靠近家具弹"这是干嘛的"）| ✅ `src/story/道具卡.ts`，约 30 种 |
| **主角坐家具**（工位/空椅/空沙发，四方向，有人的坐不下）| ✅ §3.11 |
| **同事和主角共用坐姿逻辑** | ✅ `放到座位上()` |
| **剧情"去房间"**（先传送同事、玩家走过去续播）| ✅ §4.0 |
| 剧情时钟 `StoryClock`（不再依赖"在看哪个会话"）| ✅ §4.0 |
| 未读红点只数真消息（时间/系统提示不算）| ✅ §4.0 |
| 七套正面坐姿素材接入 + 按房间自动切正/背面 | ✅ §5.3b |
| 墙面元素 6 枚（出口牌/消防栓/窗户…）| ✅ §3.10 |
| 事件一 / 二 / 三 剧本 | ✅ `src/story/opening.ts` / `events2.ts` |
| **事件四剧本**（开小游戏的那一段）| ✅ `src/story/events3.ts`，§4.1 |
| **跑团能写台词**（镜头 + 在场限定的一句对白）| ✅ `src/story/跑团.ts` + `跑团脚本.ts`（15×4 档兜底台词），§4.1 |
| **跑团面板 UI**（掷骰 / 回看 / 收工）| ✅ `src/screens/跑团面板.tsx` |
| **小游戏挂上剧情并改成工作关**（查资料/写稿/校对 → 交稿）| ✅ `屏幕='level'`，§4.1 |
| **小游戏画面与操作对齐地图**（改用 `assets/map/` 那一套）| ✅ 同上 |
| **修「工位上按空格开不了主线」**（空格被坐座位抢死）| ✅ §3.11 + `tools/空格优先级体检.mjs` |
| **过场动画机制**（全屏过场层 + 过场节拍 + 任务之间自动放）| ✅ §4.1 第十二轮 |
| **文档 #7**：周岚撤回前出真输入框（`等玩家发言` + `撤回` 两种新节拍）| ✅ 第十二轮 |
| **文档 #8**：交互后直接开微信并进到对应会话（`跟随活跃会话`）| ✅ 第十二轮 |
| **文档 #5/#9**：群人数 (3)→(4) / 删 14:30 /「先做吧」→「可以」| ✅ 第十二轮 |
| **咖啡机等装饰的物理碰撞** | ✅ 第十二轮（`装饰占地`）|
| **AI 队友：备选站位 + 看门狗判据 + 干活进度条** | ✅ 第十二轮 |
| **沙漏：竖排表 / 纵向走帧 / 每循环翻一次 / 不过场不 404** | ✅ 第十二轮 |
| **知乎卡全部换成真检索结果（点赞最高 1285）** + 可复现烘焙工具 | ✅ `工具/查知乎卡.mjs` |
| **移动端：输入框尺寸 / 小游戏横屏（含 iPhone 的 CSS 转板）/ 规则按钮** | ✅ 第十二轮 |
| **「重新开始」三层 bug**（内存状态 / 总线监听 / 异常中断链）| ✅ 第十二轮 |

### 优先级 A0-1：**知乎登录 —— 只差用户登记回调**（其余已就绪 ✔）

· 服务端 `server/zhihu-oauth.mjs`：✅ 已部署（systemd `liukanshan-zhihu`，127.0.0.1:5310 ✔）
· 密钥 `/etc/liukanshan-zhihu.env`：✅ 600 权限（**不进仓库** ✗）
· nginx 两条转发：✅ 已生效（主站 / 点废成金 均 200 ✔）
· `/status` 200 ✔、`/login` 302 ✔（Location 指向 openapi.zhihu.com ✔）
· 前端入口：✅ 资料卡 + 设置里的「用知乎账号登录」✔
· ⬜ **等用户**：开放平台登记回调 `https://zjhxbb.xyz/lks-zhihu/callback` ✔
· ⬜ 登记后真机验收：点登录 → **用户本人**在知乎页点确认 → 回来显示真实用户名 ✔

### 优先级 A0-2：**AI 队友接知乎直答**（代理位已留好 ✔）

· 前端已指向 `/刘看山/api/npc` ✔，nginx 现在返回 **204**（不报红 ✔ 走兜底台词 ✔）
· 要真接：把那行 `return 204` 换成 `proxy_pass` 到 AI 服务 ✔
· 模型档位代码里已写好**知乎直答**三档 ✔
· ⚠️ 需要**开放平台 Access Secret 放服务端**（现在只在开发机 `~/.dsh/zhihu-access-secret` ✗）

### 优先级 A00：小的尾巴（都很小，但别忘）

| 事 | 说明 |
|---|---|
| **会话列表的预览**还显示被撤回的原文 | 微信此时应显示「周岚撤回了一条消息」。改渲染层一处即可 |
| **文档 #0 的严格版**没做 | "第一次**坐上椅子**就进过场" —— 我做成了"坐工位 → 按空格开这一章"时放（见坑 44）。
如果要严格按文档，得先能判断"这个座位是不是玩家自己的工位"，再在 `坐完了` 里放过场 |
| 沙漏那套"胶片"没变成体检项 | 现在只有我临时写的探针。值得固化成断言：`scaleX` 恒为 1、`scaleY` 只在 0/±1、翻面周期 = 走帧 × 2 |

### 优先级 A0：**知乎 OAuth 登录**（前置：**要先部署到知乎项目里才能做**）

> 用户 2026-09 明确定的要求：「接入知乎的 oauth 接口」「**登录要先部署到知乎项目里才能做，先写入待办任务**」

**为什么现在做不了**（这是**前置条件**，不是"懒得做"）：

| 前置 | 说明 |
|---|---|
| **应用凭证** | OAuth 要 `app_id` + `app_key`。通用应用得**发邮件**给 `product-platform@zhihu.com` 申请；黑客松/活动作品从**赛事页面**领。**都不是即时的** |
| **必须先落到知乎的项目里** | 用户明确说了这一点 —— 也就是说**得先把这个作品部署成知乎项目**，才有凭证入口 |
| **服务端回调** | OAuth 的"收码 + 换 token"**必须在服务端**（密钥不能进浏览器 ✗，而且有 CORS ✗）。需要一个能跑 Node 服务的回环端口 + nginx 一段 `location /刘看山/api/zhihu/` |

**协议要点已经抄好了**（来自技能里的权威文档 `references/oauth.md`，用户给的那个
`zhihu.com/ring/moltbook/api/community/quickstart` 我这边 **403** 抓不到）：

```
① 跳授权：GET https://openapi.zhihu.com/authorize?redirect_uri=<URL编码>&app_id=<app_id>&response_type=code
② 回调  ：{redirect_uri}?authorization_code=<code>
   ⚠️ 参数名是 authorization_code（不是 code）；**不返 state**；接收端两种都兼容
③ 换token：POST https://openapi.zhihu.com/access_token（表单：app_id/app_key/grant_type=authorization_code/redirect_uri/code）
   → { access_token, token_type, expires_in }
④ 基础信息：GET https://openapi.zhihu.com/user   + Authorization: Bearer <access_token>   ← 只用 OAuth token
⑤ 创作/关注/收藏：https://developer.zhihu.com/api/v1/user/...
   + Authorization: Bearer <Access Secret> + X-OAuth-Token + X-Request-Timestamp
```

**没有凭证时怎么办**：文档明确说**先完成代码 + 配置模板 + Mock 测试** ✔ ——
所以拿到"可以开始"的信号后，先做 **Mock 版**（前端入口 + 回调骨架 + 配置模板），
凭证一到填两个值就通 ✔

**⚠️ 做不了的那部分**：「挑**观看量**高的知乎内容」**做不到** —— 接口不返回观看量，只有点赞/评论。见坑 47。

### 优先级 A：**事件五~十剧本**（最大的缺口）

现在四个事件能玩（事件四是跑团 + 工作关）。往下写之前**先看 `设计/剧本-新剧情参考.md`** ——
里面已经写好了六个候选本子（三行周报 / 署名的顺序 / 第二个群 / 谁的问题 / PPT 小游戏 / 另一个时间线），
每条都带定位、情绪、三选项、指标、收束、知乎卡主题和**该挂哪个零件**（跑团 / 去房间 / 工作关）。

- 「事件九 · PPT 小游戏」是**唯一需要新写引擎的一段**，建议**做成第二个工作关**（只换关卡数据）。
- 「事件七 · 第二个群」最适合展示跑团引擎的设计意图（`在场` 名单随轮次在两边群之间切换）。
- 要用**知乎直答**找素材（`.agents/skills/zhihu/` 有完整用法）。

⚠️ **知乎卡仍然是占位**（`已接入: false`）—— 不编造内容，等接入真实检索结果。

### 优先级 B：知乎卡接入真实内容（**✅ 2026-09 已完成，见第十二轮**）

> 已完成：写了 `工具/查知乎卡.mjs`（走知乎 CLI，默认返回完整 JSON），8 张卡全部换成真检索结果，
> 点赞数提到 **231 / 79 / 145 / 145 / 59 / 71 / 83 / 1285**。
> ⚠️ 但「按**观看量**挑」做不到 —— 接口只返回点赞和评论（坑 47）。
> 下面是当时写的需求，留作背景。

`知乎卡` 现在只是"主题占位"。要接**知乎开放平台的搜索**，把真实回答
（作者 / 标题 / 摘要 / 链接）填进卡片。⚠️ **来源绝对不能编**（这是项目红线）。

### 优先级 C：用 AI 生成"多样但不跑偏"的对话（**2026 已完成，见 §4.1**）

> 下面这一段是**上一轮的设计记录**，留着说明"为什么是这个结构"；
> 现在的实现已经从"一句描写"升级成"**一个镜头 + 一句台词**"，闸门也从 35 条加到 69 条，
> 并且**已经有 UI 了**（`screens/跑团面板.tsx`）—— 最新状态看 §4.1，别只看这里。

用户问过："能不能接 AI 接口、在**不影响主线**的前提下让对话更多样？"
以及"跑团那种能不能用知乎 LLM 做**不偏离剧情**的随机对话？"

**结论：能做。关键不是"让模型别乱说"，而是把随机关进笼子。**
本次会话已经把笼子做好了（`src/story/跑团.ts`）：

```
引擎（跑团.ts，纯函数、可离线单测）
   掷骰 → 判定 → 结果只有四种（大成功/成功/失败/大失败）
   每种结果的后果（指标、线索）**作者写死**
        ↓ 只把"结果"告诉 AI
AI（llm.ts，可插拔；没配/超时/断网 = 没有）
   只把这一轮的描写说得好听：一轮一句、JSON 输出
        ↓ 过闸（校验叙述）
兜底（作者预写的那句话）
```

**四道闸**（`tools/跑团自检.mjs` 里逐条断言过，35 条）：
1. 判定**只看骰子和难度** —— 顺序上 AI 永远在判定之后
2. 指标/线索**只从脚本读**，AI 的话完全不参与
3. AI 那句要过 `校验叙述`：禁词（知乎/分数/骰子…）、数值增减、越界人名、
   把失败写成翻盘、混英文 —— 一条不过就退兜底
4. **超时 900ms**；超了 / HTTP 错 / 断网 / 返回垃圾 → 一律 `null`，用作者预写的叙述

**所以离线也一定是完整一局，AI 只是同一局换一层皮。**

| 层 | 谁负责 | 能不能交给 AI |
|---|---|---|
| **主线**（事件一~十）| `src/story/*.ts` 手写剧本 | ❌ **绝对不行**（节奏、分数、知乎卡的落点都在这）|
| **支线寒暄** | `地图片段表` + 人物卡 | ✅ 可以。AI 只补"说什么"，不决定"发生什么"|
| **跑团（事件 4）** | `跑团.ts` 已落地 | ✅ 最合适：本来就是"掷骰 + 随机反应"|
| **指标 / 结局 / 知乎卡来源** | zustand + 离线库 | ❌ 铁律：AI 不得改分数、不得编造来源 |

⚠️⚠️ **密钥绝不能进前端**：这是纯静态项目，浏览器里放 Access Secret 等于公开。
`llm.ts` 默认**打自己控制的代理**（`/api/npc`）—— 代理端点还没起（要一个 Serverless 函数），
`直连调试` 那个字段只给本机开发用，**别带进部署的那份**。

**接口**：知乎开放平台对话接口，OpenAI 风格
`POST https://developer.zhihu.com/v1/chat/completions`
Header `Authorization: Bearer <Access Secret>` + `X-Request-Timestamp`，
Body `{model, messages, stream}`；档位 `zhida-fast-1p5` / `zhida-thinking-1p5` / `zhida-agent`。

**还没做**：代理端点、（NPC 寒暄那 120 条文案可以按这套改写成"AI 生成 + 兜底"）。
~~跑团 UI~~ 已经做了（`src/screens/跑团面板.tsx`）。

⚠️ **代理没起的时候不是 bug**：`要描写` 会直接返回 `null`，跑团用作者预写的兜底台词，
   一局照样完整走完。验收脚本 `tools/事件四验收.mjs` 就是在**没有代理**的情况下全绿的。

#### 复盘：工位错位那一轮（**价值在于"先量素材、再量坐标"这个顺序**）

用户报"工位坐标错位"。我第一次只量了素材本身：

```
ws_转椅      44×48   内容中心 21.0 vs 画布 21.5   偏 -0.5px
ws_办公桌    44×48   内容中心 21.5 vs 画布 21.5   偏  0.0px
ws_隔断屏风  44×48   内容中心 21.0 vs 画布 21.5   偏 -0.5px
sit_刘看山   4 帧内容中心 16.0 / 16.0 / 16.0 / 16.0
sit_周岚 15.5~16.0   sit_小鹿 15.0~16.5   sit_林总 16.0~16.5
```

**素材本身全部居中（偏差 ≤1px）**，所以当时结论是"需要用户指明哪张工位"。
后来用户圈了截图，真正的问题不在素材，而在**交互点坐标**：

- `交互点表` 里「我的工位」原来写的是 **(17,4)** ——
  那**根本不是工位**，是 15,4 和 18,4 两张桌子**之间的过道**：
  指引线圈会飘在两排桌子中间，玩家走过去也是站在空地上（"定位不准"就是这么来的）。
- 这一段的四个同事常驻 `(15,4) 阿麦 / (18,4) 周岚 / (21,4) 小鹿 / (24,8) 韩策`，
  **只有 (24,4) 是空着的工位** → 已改成 **(24,4)**。
- ⚠️ 改这类坐标时记住：**必须正好压在 `prop_ws_转椅` 那一格** ——
  场景里的 `是椅子()` 判断靠"NPC 站的那格是不是转椅"来决定播不播坐姿动画。
- 量它的办法：`node tools/读网格.mjs 2 9`（看椅子的 x 坐标）+ `道具表` 里的转椅项。


### 优先级 D：素材（**大部分已完成，剩这些**）

| 素材 | 状态 |
|---|---|
| 墙面元素 6 枚 | ✅ 已生成 + 已接入（§3.10）|
| **正面坐姿 ×7** | ✅ 已生成 + 已接入（§5.3b）|
| 开场插图"灰地版" | ⬜ 待生成（`美术/提示词-10-一键复制.md` 里有规格）|
| 贴纸补全 | ⬜ `林总` 发「点赞」现在渲染成**黑方块**（贴纸按 `成品/界面v2/贴纸_<名>.png` 找，缺的补上或换成已有名字）|

### 优先级 E：其他没做的

- **BGM**（12 个音效已接入，见 `story/audio.ts`）
- 开场 TTS 旁白
- ~~**小游戏整合**：`screen='level'` 那套旧 Phaser 小游戏还没挂上~~
  → **2026 已做完**：挂上了，并且改成**工作关**、画面与操作对齐地图（§4.1）
- `/api/events/evaluate` 还没接（旧 Godot 工程里有实现可参考；
  AI 对话接法见「优先级 C」）
- 地图上的**门开合动画**、房间名浮标

### 历史遗留（原来记的，已查明不是 bug）

原来记着"工位区右下角同事身上压着一个白色方块 + 白字"。
后来查明：那是**同事坐在工位上的头和名牌**正常叠出来的效果，
以及后来做的"道具卡/人物卡"曾经压住右下角按钮（已修）。
**如果用户又看到异物，先跑 `node liukanshan-career/tools/工位体检.mjs` 量像素，别靠眼睛。**

---

## 九、给下一个人的六条建议

1. **先跑工具，再看截图。** 现在一条命令就够：
   `node 工具/体检全部.mjs`（在仓库根跑）。改布局 → 跑它；改剧本 → 跑它。

2. **改任何深度/层级之前，先想清楚"谁该在谁前面"。** 这个项目的视觉 bug
   基本都是深度引起的（人压椅子、穿墙、桌面小件被桌子吃掉）。

3. **地板/墙/门的"层"是分开的**：地面在 tilemap，墙在遮挡层，门在物理体。
   改一块之前先确认它属于哪一层 —— 混着改最容易出问题。

4. **量不出来就别改。** 这一轮所有真 bug 都是脚本/网格量出来的；
   我凭眼睛和"算 scroll"得出的结论**错过至少三次**（南墙是不是地板、门有没有画出来、
   邀请卡到底在哪个会话）。**能读运行时数据就读运行时数据。**

5. **★ 先查数据对不对，再考虑加规则。**（本会话最贵的一课）
   用户说"自己工位左侧没有座位却能坐下"，我先按字面**加了一条"左侧不能坐"的规则** ——
   查下去才发现根因是 `座位表` 里登记了**两张根本不存在的沙发**，
   于是人坐到空地上。删掉幽灵数据之后，**规则完全不需要**。
   ⚠️ **加规则会把数据 bug 掩盖住，下次换个地方再犯。**
   凡是"表 A 说这里有什么"的约定，都该配一条**自动比对表 B** 的检查（`工具/查座位.mjs` 就是）。

6. **调"视觉手感"这种数，必须拍图对，不能只读代码。**
   坐姿偏移量我按"椅背顶距脚底多少 px"算了一个值，推了五次才接近用户要的：
   **18 → 14 → 10 → 6**（用户连续说"太高"）。最后靠 `tools/拍坐姿.mjs`
   把"人 y / 人深度 / 家具深度"打出来才定位准。

---

*本文档由多轮会话整理：地图重做 → 素材接入 → 寻路/门物理 → 剧情重写 → 道具卡/人物卡
→ 坐姿系统 / 去房间传送 / 剧情时钟。*
*2026-09-14 增补：坐姿系统（§3.11）、`座位表` 与 `道具表` 一致性检查、
剧情时钟 `StoryClock`、`去房间` 传送同事、正面坐姿素材接入、第七节 44~55 条踩坑。*
*2026-09 增补（第二轮）：**事件四剧本（开小游戏的那一段）**、
**跑团会写台词了**（镜头 + 在场限定的一句对白、`贴AI话`、兜底 60 档）、
**跑团面板 UI**、**小游戏挂上剧情并改成工作关（查资料/写稿/校对 → 交稿）**、
**小游戏画面与操作对齐地图**（`assets/map/` 那一套）、
新增 §4.1、`tools/事件四验收.mjs`、`设计/剧本-新剧情参考.md`、第七节 56~57 条踩坑。*
*2026-09 修订（第三轮）：**修「工位上按空格开不了主线」** ——
`MapScreen` 的键盘分支漏了 `!坐着`，空格在"坐 ↔ 站"里死循环，把「我的工位」的主线锁死了。
现在两处判据一致（§3.11 有对照表），提示行也跟状态变（没坐下→"坐下看看"，坐下后→"开始这一段"），
新增盯这个的 `tools/空格优先级体检.mjs`（真键盘、每场景一张新页面）。*
*提交历史：`git log --oneline -25`*
