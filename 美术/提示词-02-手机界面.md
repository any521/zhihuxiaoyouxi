# 提示词 02 · 手机界面（13 张）

> 这一批决定"手机模式好不好看"。
> 存到 `美术/素材库/原图/10-界面v2/`
> 除注明外，**垫图统一用 `美术/素材库/原图/00-风格基准/参考_风格基准图.png`，权重 0.50**
>
> ⚠️ **风格基准图的背景是纯黑**，不是品红。所以每条提示词里都再强调了一次品红背景，不要删。

---

## U01 · 消息气泡 · 我方

`4:3 · 1024×768 · 垫风格基准图 0.50 · 界面_气泡_我方.png`

```
温暖手绘感的 16-bit 像素画，清晰的方块像素与锐利的像素边缘，纯粹的像素画风格。
cozy hand-painted 16-bit pixel art, crisp square pixels, pure 2D pixel art.

内容：一个微信聊天气泡的空白模板，横向的长方形。
气泡是浅绿色的圆润矩形，四角是半径很小（约 24 像素）的圆角，不要大圆角、不要胶囊形；
气泡的外缘有一圈宽约 8 像素的深棕色描边；
气泡的右下角伸出一个小的三角形尖角（尾巴），尖角大小约 40×32 像素，指向右下方；
气泡内部是完全均匀的纯色，没有任何内容。

视角与构图：正视，气泡居中，四周留出约 80 像素的空白。

光线：光从左上角 45 度打入，气泡上缘有一道极细的浅色高光，下缘有一道极细的深色暗边。

配色：只使用 #a8d98a #7cc26b #241a12 三种颜色。

背景：纯净均匀的品红色（#FF00FF）平面色块。

【画面约束】气泡内部必须完全均匀纯色，不出现任何文字、符号、图标、表情；
四角是小圆角（半径约 24 像素），不要大圆角、不要胶囊形、不要尖锐直角；
描边粗细处处一致，都是约 8 像素；
画面中不出现任何文字、字母、数字、水印、签名、辅助线或网格线；
整幅画是平面的二维手绘像素画，不是三维渲染、不是照片。
排错检查：如果气泡里出现了"你好""示例"之类的文字，这张图就是错的。
排错检查：如果气泡的尾巴在左下角或者没有尾巴，这张图就是错的——尾巴必须在右下角。
排错检查：如果气泡四角是尖的直角、或者圆角大到接近胶囊形，这张图就是错的。
```

---

## U02 · 消息气泡 · 对方

`4:3 · 1024×768 · 垫风格基准图 0.50 · 界面_气泡_对方.png`

```
温暖手绘感的 16-bit 像素画，清晰的方块像素与锐利的像素边缘，纯粹的像素画风格。
cozy hand-painted 16-bit pixel art, crisp square pixels, pure 2D pixel art.

内容：一个微信聊天气泡的空白模板，横向的长方形。
气泡是米白色的圆润矩形，四角是半径很小（约 24 像素）的圆角，不要大圆角、不要胶囊形；
气泡的外缘有一圈宽约 8 像素的深棕色描边；
气泡的左下角伸出一个小的三角形尖角（尾巴），尖角大小约 40×32 像素，指向左下方；
气泡内部是完全均匀的纯色，没有任何内容。

视角与构图：正视，气泡居中，四周留出约 80 像素的空白。

光线：光从左上角 45 度打入，气泡上缘有一道极细的浅色高光，下缘有一道极细的深色暗边。

配色：只使用 #ffffff #f4f0ea #241a12 三种颜色。

背景：纯净均匀的品红色（#FF00FF）平面色块。

【画面约束】气泡内部必须完全均匀纯色，不出现任何文字、符号、图标、表情；
四角是小圆角（半径约 24 像素），不要大圆角、不要胶囊形、不要尖锐直角；
描边粗细处处一致，都是约 8 像素；
画面中不出现任何文字、字母、数字、水印、签名、辅助线或网格线；
整幅画是平面的二维手绘像素画，不是三维渲染、不是照片。
排错检查：如果气泡里出现了"你好""示例"之类的文字，这张图就是错的。
排错检查：如果气泡的尾巴在右下角或者没有尾巴，这张图就是错的——尾巴必须在左下角。
排错检查：如果气泡四角是尖的直角、或者圆角大到接近胶囊形，这张图就是错的。
```

---

## U03 · 消息气泡 · 群聊

`4:3 · 1024×768 · 垫风格基准图 0.50 · 界面_气泡_群聊.png`

```
温暖手绘感的 16-bit 像素画，清晰的方块像素与锐利的像素边缘，纯粹的像素画风格。
cozy hand-painted 16-bit pixel art, crisp square pixels, pure 2D pixel art.

内容：一个微信群聊气泡的空白模板，横向的长方形。
气泡是米白偏暖（带一点点米黄）的圆润矩形，四角是半径很小（约 24 像素）的圆角；
气泡的外缘有一圈宽约 8 像素的深棕色描边；
气泡的左下角伸出一个小的三角形尖角（尾巴），尖角大小约 40×32 像素，指向左下方；
气泡内部是完全均匀的纯色，没有任何内容。

视角与构图：正视，气泡居中，四周留出约 80 像素的空白。

光线：光从左上角 45 度打入，气泡上缘有一道极细的浅色高光，下缘有一道极细的深色暗边。

配色：只使用 #ffffff #e8dcc0 #241a12 三种颜色。

背景：纯净均匀的品红色（#FF00FF）平面色块。

【画面约束】气泡内部必须完全均匀纯色，不出现任何文字、符号、图标、表情；
四角是小圆角（半径约 24 像素），不要大圆角、不要胶囊形、不要尖锐直角；
描边粗细处处一致，都是约 8 像素；
画面中不出现任何文字、字母、数字、水印、签名、辅助线或网格线；
整幅画是平面的二维手绘像素画，不是三维渲染、不是照片。
排错检查：如果气泡里出现了文字，这张图就是错的。
排错检查：如果气泡的尾巴在右下角或者没有尾巴，这张图就是错的——尾巴必须在左下角。
```

---

## U04 · 聊天区背景

`1:1 · 1024×1024 · 垫风格基准图 0.50 · 界面_底_聊天区.png`

```
温暖手绘感的 16-bit 像素画，清晰的方块像素与锐利的像素边缘，纯粹的像素画风格。
cozy hand-painted 16-bit pixel art, crisp square pixels, pure 2D pixel art.

内容：一个深色聊天窗口的空白背景。
整体是深灰蓝色的平整底面，带极轻微的、大块的深浅变化（每块约 128 像素宽），
看起来干净、沉稳、不抢眼，适合在上面叠加白色文字和气泡。

视角与构图：正视，整张画布就是背景本身。

光线：光照绝对均匀，没有明暗渐变、没有中心亮四周暗、没有暗角。

配色：只使用 #262a33 #1b2028 两种颜色。

背景：无背景（整张画布就是聊天区背景）。

【画面约束】不要任何图案、不要任何纹理细节、不要任何装饰，只要平整的深色底面；
光照绝对均匀；
画面中不出现任何文字、字母、数字、图标、水印。
排错检查：如果画面上有任何图案、格纹、花纹、装饰线，这张图就是错的。
排错检查：如果背景有明暗渐变或中心发亮，这张图就是错的。
```

---

## U05 · 会话列表项（普通 + 选中两态）

`3:4 · 1024×1365 · 垫风格基准图 0.50 · 界面_列表项_两态.png`

```
温暖手绘感的 16-bit 像素画，清晰的方块像素与锐利的像素边缘，纯粹的像素画风格。
cozy hand-painted 16-bit pixel art, crisp square pixels, pure 2D pixel art.

内容：两个微信会话列表项的空白底条，上下排布，间距约 60 像素。
上面一条是普通态：深灰色平整底面，四角是很小的圆角（约 16 像素）；
下面一条是选中态：比普通态明显提亮一档的灰蓝色，四角同样是小圆角，
左右两端各有一道浅色的竖向高光。
两条都是长方形，左右各留出约 40 像素空白。

视角与构图：正视，上下排布。

光线：光照绝对均匀，只有选中态左右两端的竖向高光。

配色：只使用 #262a33 #3a4658 #6e7686 三种颜色。

背景：纯净均匀的品红色（#FF00FF）。

【画面约束】两条底条内部完全均匀，不出现任何文字、头像、图标、未读红点；
两条底条的宽度完全一致；
画面中不出现任何文字、字母、数字、水印。
排错检查：如果底条里出现头像、名字、数字、红点，这张图就是错的。
排错检查：如果两条底条宽度不一致，这张图就是错的。
```

---

## U06 · 左侧功能栏底

`1:4 竖图（用 3:4 · 1024×1365 出，只画一条竖栏）· 垫风格基准图 0.50 · 界面_底_功能栏.png`

```
温暖手绘感的 16-bit 像素画，清晰的方块像素与锐利的像素边缘，纯粹的像素画风格。
cozy hand-painted 16-bit pixel art, crisp square pixels, pure 2D pixel art.

内容：一条竖向的深色功能栏空白底面，这是微信电脑版最左边那一竖条。
整体是比聊天区更深一档的深灰蓝色平整底面，
右边缘有一道宽约 8 像素的浅色分界线，贯穿上下。

视角与构图：正视，整张画布就是这条竖向功能栏。

光线：光照绝对均匀。

配色：只使用 #1b2028 #14161c #3a4658 三种颜色。

背景：无背景（整张画布就是功能栏）。

【画面约束】不要任何图标、头像、按钮、文字，只要平整的底面与右侧一条分界线；
画面中不出现任何文字、字母、数字、图标、水印。
排错检查：如果画面上有任何图标或头像，这张图就是错的。
排错检查：如果右侧的分界线不在右侧、或者不贯穿上下，这张图就是错的。
```

---

## U07 · 头像框（方形 + 圆形，四款）

`1:1 · 1024×1024 · 垫风格基准图 0.50 · 界面_头像框_四款.png`

```
温暖手绘感的 16-bit 像素画，清晰的方块像素与锐利的像素边缘，纯粹的像素画风格。
cozy hand-painted 16-bit pixel art, crisp square pixels, pure 2D pixel art.

内容：四个头像框的空白模板，2×2 网格排布，间距均匀。
左上：方形头像框，四角小圆角（约 12 像素）
右上：圆形头像框
左下：方形头像框，带一圈加粗的强调色描边
右下：圆形头像框，带一圈加粗的强调色描边
四个框的内部都是完全均匀的纯色，这是将来放头像的位置，没有任何图形。
每个框都有一个宽约 8 像素的深棕色外描边。

视角与构图：正视，2×2 网格排布，每格居中。

光线：光照绝对均匀，每个框左上角有一小道极细的高光。

配色：只使用 #ffffff #d8d2c8 #454b57 #241a12 四种颜色。

背景：纯净均匀的品红色（#FF00FF）。

【画面约束】四个头像框内部必须完全均匀纯色，不出现任何脸、人物、图案、文字；
四个框大小完全一致；
画面中不出现任何文字、字母、数字、水印。
排错检查：如果框里出现了脸或者人物，这张图就是错的。
排错检查：如果四个框大小不一致，这张图就是错的。
```

---

## U08 · 微信功能图标 6 枚

`4:3 · 1024×768 · 垫风格基准图 0.50 · 界面_图标_微信6枚.png`

```
温暖手绘感的 16-bit 像素画，清晰的方块像素与锐利的像素边缘，纯粹的像素画风格。
cozy hand-painted 16-bit pixel art, crisp square pixels, pure 2D pixel art.

内容：六个微信界面用的图标，3×2 网格排布，从左到右、从上到下依次是：
① 对话气泡：一个圆润的方形气泡，下方一个小尖角
② 通讯录：一本合上的小本子，封面上一个人形剪影
③ 齿轮：设置
④ 放大镜：搜索
⑤ 三个横向排列的小圆点：更多
⑥ 一个向左的箭头：返回
六个图标线条粗细完全一致，都是粗描边加扁平填充，尺寸完全一致。

视角与构图：正视，3×2 网格排布，每个图标居中，间距均匀。

光线：光照绝对均匀，不加阴影。

配色：只使用 #ffffff #a4acbb #241a12 三种颜色。

背景：纯净均匀的品红色（#FF00FF）。

【画面约束】六个图标尺寸与线条粗细必须完全一致；
画面中不出现任何文字、字母、数字、水印、辅助线。
排错检查：如果某个图标里有文字或数字，这张图就是错的。
排错检查：如果六个图标的尺寸或线条粗细不一致，这张图就是错的。
```

---

## U09 · 移动端聊天顶栏

`16:9 · 1920×1080（横条画在正中，后处理裁出来）· 垫风格基准图 0.50 · 界面_顶栏_移动端.png`

> ⚠️ **不要请求 1024×256 这种 4:1 的尺寸**——生图服务普遍不支持，会返回 502。
> 做法：画在标准比例的 16:9 画布正中，后处理时裁掉上下多余的品红。

```
温暖手绘感的 16-bit 像素画，清晰的方块像素与锐利的像素边缘，纯粹的像素画风格。
cozy hand-painted 16-bit pixel art, crisp square pixels, pure 2D pixel art.

内容：画面正中横放着一条**手机聊天界面顶部的标题栏空白模板**。
这条横条的**宽度约占画布宽度的九成，高度约占画布高度的六分之一**，
横条的上下方都是大片纯净的品红色背景。
横条本身是深灰蓝色的平整长条，上下各有一道宽约 12 像素的深色分界线；
横条的左端有一个宽约 240 像素的留白区（将来放返回箭头），
中间是完全均匀的空白（将来放群名）。

视角与构图：正视，横条水平居中、垂直居中，左右两端都留有品红空白。

光线：光照绝对均匀，没有明暗渐变。

配色：只使用 #262a33 #14161c #3a4658 三种颜色。

背景：纯净均匀的品红色（#FF00FF）。

【画面约束】顶栏内部完全均匀，不出现任何文字、箭头、图标、头像；
横条必须是**水平的一条**，上下各留出约画布高度四成的品红空白；
画面中不出现任何文字、字母、数字、水印。
排错检查：如果横条占满整个画布高度、没有上下品红空白，这张图就是错的。
排错检查：如果顶栏里出现文字、箭头或图标，这张图就是错的。
```

---

## U10 · 移动端底部输入栏

`16:9 · 1920×1080（横条画在正中，后处理裁出来）· 垫风格基准图 0.50 · 界面_底栏_移动端.png`

> ⚠️ 同上，不要请求 4:1 的尺寸。

```
温暖手绘感的 16-bit 像素画，清晰的方块像素与锐利的像素边缘，纯粹的像素画风格。
cozy hand-painted 16-bit pixel art, crisp square pixels, pure 2D pixel art.

内容：画面正中横放着一条**手机聊天界面底部的输入栏空白模板**。
这条横条的**宽度约占画布宽度的九成，高度约占画布高度的六分之一**，
横条的上下方都是大片纯净的品红色背景。
横条本身是深灰蓝色的平整长条，上下各有一道宽约 12 像素的深色分界线；
横条内部，中间偏左有一个长方形的浅色输入框，四角小圆角；
输入框右侧有两个正方形的浅色小按钮，分别是将来放表情和加号的位置。

视角与构图：正视，横条水平居中、垂直居中，左右两端都留有品红空白。

光线：光照绝对均匀，没有明暗渐变。

配色：只使用 #262a33 #3a4658 #d8d2c8 #14161c 四种颜色。

背景：纯净均匀的品红色（#FF00FF）。

【画面约束】所有元素内部完全均匀，不出现任何文字、图标、符号、表情；
横条必须是**水平的一条**，上下各留出约画布高度四成的品红空白；
画面中不出现任何文字、字母、数字、水印。
排错检查：如果横条占满整个画布高度、没有上下品红空白，这张图就是错的。
排错检查：如果输入框里有文字、按钮上有图标，这张图就是错的。
```

---

## U11 · 地图上的手机按钮（两态）

`1:1 · 1024×1024 · 垫风格基准图 0.50 · 界面_手机按钮_两态.png`

```
温暖手绘感的 16-bit 像素画，清晰的方块像素与锐利的像素边缘，纯粹的像素画风格。
cozy hand-painted 16-bit pixel art, crisp square pixels, pure 2D pixel art.

内容：两个手机形状的小图标，上下排布，间距均匀。
上面一个是普通态：一个竖长方形的小手机，深灰机身，浅色屏幕，屏幕上是空白的；
下面一个是有未读消息态：与上面完全相同，但右上角多一个朱红色的小圆点。
两个手机的形状、尺寸、配色必须完全一致，只有红点的区别。

视角与构图：正视，上下两格居中，间距均匀。

光线：光照绝对均匀，左上角一道极细高光。

配色：只使用 #454b57 #d8d2c8 #e04f3f #241a12 四种颜色。

背景：纯净均匀的品红色（#FF00FF）。

【画面约束】手机屏幕上不能有任何图标、文字、图案，必须是空白屏幕；
两个手机尺寸完全一致；
画面中不出现任何文字、字母、数字、水印。
排错检查：如果手机屏幕上有图标或文字，这张图就是错的。
排错检查：如果红点出现在左上角，这张图就是错的——必须在右上角。
```

---

## U12 · 聊天小件 6 种

`4:3 · 1024×768 · 垫风格基准图 0.50 · 界面_小件_六种.png`

```
温暖手绘感的 16-bit 像素画，清晰的方块像素与锐利的像素边缘，纯粹的像素画风格。
cozy hand-painted 16-bit pixel art, crisp square pixels, pure 2D pixel art.

内容：六个聊天界面用的小件，3×2 网格排布，从左到右、从上到下依次是：
① 一条居中的浅色小胶囊（将来放时间戳，内部完全空白）
② 一张入群邀请卡：米白色卡片，四角小圆角，卡片内部有一条横向的浅色分隔线，其余留白
③ 一个文件气泡：米白色卡片，左端有一个折角的纸张图标，其余留白
④ 一条系统消息：比背景略亮的细长横条，四角小圆角，内部留白
⑤ 一个未读红点：朱红色的实心小圆点
⑥ 一个小箭头：指向右方的深色实心小三角
六件线条粗细一致，都是粗描边加扁平填充。

视角与构图：正视，3×2 网格排布，每件居中，间距均匀。

光线：光照绝对均匀。

配色：只使用 #f7efdd #a8a29a #e04f3f #454b57 #241a12 五种颜色。

背景：纯净均匀的品红色（#FF00FF）。

【画面约束】所有小件内部必须留白，不出现任何文字、数字、图标，
只有第③件的折角纸图标和第⑥件的三角是允许的图形；
画面中不出现任何文字、字母、数字、水印。
排错检查：如果任何一件里出现文字或数字，这张图就是错的。
排错检查：如果小件的数量不是六个，这张图就是错的。
```

---

## U13 · 贴纸 / 表情包 12 枚

`1:1 · 1024×1024 · 垫图：角色_刘看山_三视图.png（0.50）+ 参考_风格基准图.png（0.30）· 贴纸_刘看山12枚.png`

```
温暖手绘感的 16-bit 像素画，清晰的方块像素与锐利的像素边缘，纯粹的像素画风格。
cozy hand-painted 16-bit pixel art, crisp square pixels, pure 2D pixel art.

角色：白色北极狐拟人角色：整个身体是一块上窄下宽的圆润白色蛋形，
没有脖子、没有肩线、头和身体不分家；
身体顶端两个白色尖三角耳朵；
脸的上中央有一块很大的黑色实心圆形色块作为鼻子（宽度约为身体宽度的三分之一），
鼻子两侧各有一个很小的黑色圆点眼睛；没有嘴；
两只手臂与两条腿都是黑色，末端是黑色圆拳与黑色圆脚。

内容：十二个围绕这个角色的小贴纸，4×3 网格排布，从左到右、从上到下依次是：
① 探头：只露出头和两只耳朵，从画面下缘探出来
② 竖起大拇指：一只黑色的圆拳竖起拇指
③ 大哭：眼睛变成两道向下的弧线，旁边两大滴浅蓝泪珠
④ 震惊：眼睛变成两个小圆圈，头顶一道金色感叹号
⑤ 点赞：两只黑色的圆拳在胸前合十
⑥ 递咖啡：两只手捧着一杯冒热气的咖啡
⑦ 抱头：两只手抱住头，两只耳朵下垂
⑧ 举手：一只手臂高高举起
⑨ 晕：眼睛变成两道横线，头顶一圈小星星
⑩ 比心：两只手在胸前组成一个心形
⑪ 累了：两只耳朵完全下垂，整个身体向后仰，眼睛变成两道横线
⑫ 鞠躬：整个身体向前倾约三十度
每个贴纸只画最必要的部分，构图饱满、居中，四周留出约 40 像素空白。

视角与构图：平视，4×3 网格排布，每格约 256×341。

光线：光从左上角 45 度打入，暖白色顶灯。

配色：只使用 #ffffff #f4f0ea #d8d2c8 #a8a29a #14161c #8ec3ee #ffb020 七种颜色。

背景：纯净均匀的品红色（#FF00FF）。

【画面约束】十二个贴纸的角色体型与配色必须完全一致，不能有的胖有的瘦；
每个贴纸背景必须是纯净的品红色，各自独立不重叠；
画面中不出现任何文字、字母、数字、水印、辅助线或网格线。
排错检查：如果角色脸上只有两个小黑点、而没有那块占据中央的大黑色圆块，这张图就是错的。
排错检查：如果出现十二个以外的贴纸、或者某个格子里有两个贴纸，这张图就是错的。
排错检查：如果贴纸里出现文字（例如"赞""哭"），这张图就是错的。
排错检查：如果角色画出了嘴巴，这张图就是错的——这个角色没有嘴。
```
